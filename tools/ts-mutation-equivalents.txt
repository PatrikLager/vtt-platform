# Adjudicated EQUIVALENT mutants in the TypeScript client: surviving
# mutations that no test can kill, because the mutated code is observationally
# identical to the original.
#
# Format:  <file> <line>:<col> <MUTATOR> <replacement>
# followed by an indented reason line. The replacement runs to end of line
# because it can contain spaces.
#
# The REPLACEMENT is part of the key for a reason found the hard way: Stryker
# emits both `-> true` and `-> false` for one ConditionalExpression at one
# location, and without it those two collapse into a single entry — so
# adjudicating the harmless one silently excuses the other. The gate's own
# duplicate check caught that on client/src/player.ts:20:51. Both are required — a bare entry is a
# parse error, and so is a reason with no entry above it.
#
# ------------------------------------------------------------------------
# READ THIS BEFORE ADDING AN ENTRY.
#
# An "equivalent" verdict asserts that NO test can ever kill this mutant, so
# it forecloses writing one. A wrong entry is therefore WORSE than an
# unadjudicated survivor: it converts a real gap into a permanently excused
# one, in writing, with a confident-sounding justification.
#
# That is not hypothetical — see tools/mutation-equivalents.txt, where four
# mutants were adjudicated on the Go side and TWO were wrong. The reasoning
# was about JSON marshalling when the actual observable was reflect.DeepEqual
# over decoded structs, where nil map != empty map. A two-line fixture killed
# both.
#
# The test to apply, learned from that mistake: do not ask "is the output the
# same?" Ask "is there ANY observable — under deep equality, under identity,
# under undefined-ness, under iteration order, under what reaches the DOM —
# that distinguishes them?"
#
# An empty file is the GOAL STATE, not a problem. Prefer killing a mutant to
# excusing it; reach for an entry only when the mutation genuinely cannot be
# observed.
#
# Entries are keyed by file, line:col AND mutator, so two different mutations
# at one location are two separate claims. A stale entry — one whose mutant no
# longer survives, because it was killed or the line moved — is a hard error,
# since it would otherwise pre-approve whatever mutant lands there next.
#
# RE-KEYED 2026-08-21 (visibility Task 7), in two passes, and it was a
# re-derivation rather than an offset. 28 of the 64 keys moved; 36 were already
# right. The drift has two separate causes and the earlier drafts of this note
# ran them together: SOME of these keys were already wrong on main (fold.ts's
# eleven lines, view/spectator.ts's one, app.ts's two), while OTHERS were
# correct on main and were moved by this branch (view/grid.ts, wire.ts,
# session.ts). Neither group is "all of them".
#
# WHAT THE FIRST PASS COVERED, stated because the first draft of this note said
# "wholesale" and that is a completeness claim it did not have. It swept only
# the files Task 7 edited: all fourteen fold.ts entries (eleven distinct lines),
# the seven in view/grid.ts (two lines), and the one in view/spectator.ts. It
# left six stale
# keys standing in files it never opened, which review found:
#
#   entry said           actually at   note
#   wire.ts 175:13       292:13        moved by this branch; a COMMENT line
#   wire.ts 199:7        316:7         moved by this branch
#   app.ts 64:32         66:32         stale on MAIN; a COMMENT line
#   app.ts 127:32        129:32        stale on MAIN; a COMMENT line
#   session.ts 66:12     67:12         moved by this branch; a COMMENT line
#   session.ts 67:12     68:12         moved by this branch; landed on the
#                                      SIBLING comparison, right shape wrong
#                                      operand — the worst of the six, because
#                                      it looks plausible at the new location
#
# Four of the six named a line Stryker generates no mutant on at all. A key that
# points at a comment is not a near miss; it is an adjudication for a mutant
# that does not exist, beside a real survivor nobody has excused.
#
# THE "actually at" COLUMN ABOVE IS SUPERSEDED (2026-08-24). It records where
# those keys sat on 2026-08-21 and is kept for the reasoning, not as a current
# address. The perch UI moved six of them again and they are now:
#
#   wire.ts       292:13 -> 460:13   landed on a COMMENT line a SECOND time
#   wire.ts       316:7  -> 484:7     a COMMENT line too, this time round
#   app.ts        66:32  -> 67:32    re-keyed, then KILLED 2026-08-25 and
#   app.ts        129:32 -> 130:32   removed outright — see the app.ts note below
#   session.ts    67:12  -> 68:12
#   session.ts    68:12  -> 69:12
#   spectator.ts  399:59 -> 501:59
#
# Read the entries themselves, never this table. That comment landings hit this
# same wire.ts pair in TWO episodes three days apart is the argument for keying
# on something other than a line number, and it is why the gate's own MOVED
# detection could not rescue the app.ts pair: their replacement text is
# `"Stryker was here!"`, which is identical for EVERY string literal in a file,
# so the pairing is ambiguous by construction and the gate correctly refused to
# guess. It told the reader to DELETE two sound adjudications instead.
#
# THE SECOND PASS IS MECHANICAL AND COVERS EVERY ENTRY. All 64 were parsed and
# each one's file:line:col was read out of the working tree and compared against
# the expression its own replacement text names. It re-pointed the six above,
# plus the two fold.ts keys its OWN edit to that file had just moved.
#
# WRITE THAT CHECK CAREFULLY IF YOU RE-RUN IT. The first version here had two
# holes and reported "0 suspect" over a key that had drifted three lines onto a
# `/**` line: it tested `startswith("//")`, which a `/**` doc comment does not
# match, and it had no branch for the mutator on that entry so it silently
# skipped it. A checker that cannot fail is worse than no checker, and this one
# said everything was fine twice. Test it against a key you have deliberately
# broken before believing it.
#
# THE DELTAS ARE NOT UNIFORM, which is exactly why this file's own procedure
# says to READ the new location rather than assume the shift. Measured by
# comparing this file against main entry by entry, in order: the 28 moved keys
# span +1 (session.ts 66 -> 67) to +208 (fold.ts 350 -> 558), and every one of
# the 64 kept its COLUMN — only lines moved. Per file the shifts are
# app.ts +2, session.ts +1, wire.ts +117, view/grid.ts +31,
# view/spectator.ts +144, and fold.ts a spread of +3/+133/+157/+208. Anyone repeating that comparison
# should pair the entries POSITIONALLY, not by (file, mutator, replacement): the
# file contains repeated triples on purpose, twelve of them — for instance
# app.ts's two `StringLiteral
# "Stryker was here!"`, view/grid.ts's two `StringLiteral ""` (129:45 and
# 129:57) and its two `EqualityOperator a.tokenId <= b.tokenId` (139:32 and
# 139:61) — and a dict keyed that way silently drops them and reports 22 instead
# of 28. That happened here. NOTE the four entries at 139:61 are NOT an example:
# they are four DISTINCT triples at one location, which is exactly what keying
# on the replacement text is for.
#
# Task 7's own contribution, across its landing commit and three fix rounds,
# measured as two hunks against 1f34407: +20 in fold.ts's sceneSeen arm
# (@@ -212,6 +212,26 @@) and +16 in its dump (@@ -489,6 +509,22 @@), which sum
# to fold.ts's total of +36; +31 in view/grid.ts; +10 in view/spectator.ts; and
# nothing at all in wire.ts, session.ts or app.ts — those three it never
# touched.
#
# "THE SMALLER HALF" IS TRUE OF fold.ts AND FALSE OF view/grid.ts, and an
# earlier draft said it of everything. grid.ts is byte-identical between main
# and 1f34407, so Task 7's +31 is the WHOLE of that file's shift; fold.ts's +36
# sits under a further +157 that Tasks 1-6 put in. The dump figure was +11 in
# that draft, carried forward unexamined from before the round that was supposed
# to re-measure all of it — it had already been +16 at 15cd2c7.
#
# THIS HEADER'S OWN CITATIONS ARE PART OF THE RE-KEY, and round 2 found them
# stale after round 2's edits: it re-pointed the entries and left the numbers
# quoted up here pointing at the old lines. A preamble that disagrees with its
# own contents is the failure this file exists to prevent, so when you move an
# entry, grep this header for its old line before you stop.
#
# CONFIRMED BY A GREEN RUN, 2026-08-25 14:08 (recorded in 76e45c2's message):
# 2777 mutants, 2673 killed, 31 timed out, 73 adjudicated equivalent, zero
# unadjudicated survivors. Run in a Linux container, which is the only place
# this gate executes — it skips itself on macOS (kernel bug FB21686886, see
# Taskfile.yml) and CI is workflow_dispatch-only. Extended the same day by
# 5ac05e9 at 14:52, which re-pointed three wire.ts keys (now at 474:13,
# 475:13 and 499:7) without a fresh Stryker run backing them at the time.
#
# CONFIRMED AGAIN, 2026-08-30 ~15:06 (Task 4, dm-hands-and-retraction, run
# against the tree frozen after fix round 3): zero ADJUDICATION MOVED, zero
# stale entries, across every entry in this file — including the wire.ts
# three above, which this run backs with a fresh Stryker agreement for the
# first time. Nine keys had moved since the 2026-08-25 run and needed this
# confirmation specifically: the five fold.ts keys fix round 2 re-pointed
# (419:7→423:7, 509:34→513:34, 527:32→531:32, 619:7→623:7, 619:21→623:21 —
# Task 3's `doorKey` export shifting them, not this arc's own edits), and
# four keys this arc re-pointed itself across fix rounds 1-2 and re-verified
# in fix round 3: view/spectator.ts's 388:10 and 389:10, and view/dm.ts's
# 506:62 and 551:36 (the latter pair found to have ALREADY been stale on
# HEAD before this task ever touched the file — see those entries' own
# reasoning for the detail). No entry in this file is currently unconfirmed
# by a Stryker run; there is no post-date list to maintain as of this
# writing. (A later edit that shifts a key or adds a new one reopens that
# question for exactly the keys it touches — this note is not a standing
# exemption.)
#
# TWO ADJUDICATIONS WERE FILED AND WITHDRAWN during this same arc:
# view/player.ts 95:59 and view/spectator.ts 533:59, both `?? ""`
# equivalence claims for the same expression shape
# (`Object.keys(st.Scenes).sort().at(-1) ?? ""`), both eventually falsified
# by the same technique — a hand-built state where st.Scenes has no own
# keys but a prototype answering "" with a real scene, making the fallback
# string observable after all. Notable because the second one was re-keyed
# and re-confirmed TWICE (fix rounds 1 and 2) without anyone re-reading its
# claim against the first, which had just been disproved in the same round.
# Full correction and the killing tests are in each file's own section
# below; neither carries an entry now.
#
# Fix round 1's own review separately found eight UNADJUDICATED survivors
# (not stale entries) in this arc's new code, accounted for here rather than
# left an open question: six were killed with tests added that round — see
# player.test.ts's "no scenes at all means no toggle", "the current scene is
# the lexicographically GREATEST id" and "the toggle's class and label
# reflect whether doors are currently armed" — one was adjudicated equivalent
# at the time (view/player.ts 95:59 above, later disproven and withdrawn),
# and one was removed outright as dead code rather than tested or excused:
# spectator.ts's renderGrid losing its unreachable `doorsArmed = false`
# default parameter (a mutant with no observable is still a mutant if
# nothing forces the default to be read; deleting it removed the mutant).

# --- client/src/undo.ts: THE FILE IS GONE ------------------------------------
#
# REMOVED 2026-08-31 (retraction leaves the platform, Patrik 2026-08-30): the
# two entries that stood here, `undo.ts 39:26` and `undo.ts 63:28`, both
# EqualityOperator on a `>` guarding a running maximum. They are deleted rather
# than re-keyed because the FILE is deleted — lastUndoable and
# retractableRange do not exist, so neither do their mutants. Nothing else in
# this file may cite them; the wire.ts 475:13 entry that did now states the
# shape inline instead.
#
# Their shape is worth keeping in words, because it recurs: `x > best` -> `>=`
# where the branch's only effect is `best = x`. At equality the mutant performs
# an assignment writing the value the field already holds, and bigint is
# compared by value, so no reader can tell a re-written 4n from an untouched
# one. Both were adjudicated on that reasoning and both died with their code,
# unfalsified.

# --- client/src/fold.ts -----------------------------------------------------
#
# fold.ts went 93 survivors -> 14 by writing the rejection and dump suites
# (client/test/fold-rejections.test.ts, fold-dump.test.ts). The 14 below are
# the residue, and they fall into four shapes, none of which any test can
# observe. Each was checked against the "is there ANY observable?" rule rather
# than "is the output the same?".
#
# RE-KEYED 2026-08-31 (retraction-leaves Task 8, fix round 1). All eighteen
# entries below shifted by the SAME +24, because both edits that caused it
# landed entirely ABOVE every one of them: the tokenRemoved fold arm Task 8
# added between tokenMoved and tokenHidden (+15, measured by the review that
# found this file stale) and fix round 1's own correction to that arm's
# comment, which grew it by 9 more lines answering a false claim the first
# draft made about tokenHidden ("never reaches this fold" — it does, every
# time a token leaves a player's view). 15+9=24. Every one of the eighteen
# keys was re-derived by reading the quoted expression at its NEW location in
# the tree and computing the column from there, not by adding 24 to the old
# number — the method this file's own header prescribes, and the one the
# stale keys this round found (in tools/mutation-equivalents.txt, not here)
# were missed by skipping.

# REMOVED 2026-08-31 (retraction leaves the platform): `fold.ts 61:9
# ConditionalExpression false` and `fold.ts 61:30 StringLiteral ""`, the pair
# that adjudicated the mutants on `if (env.payload.case === "eventsRetracted")
# continue;` in fold's second pass. Both were equivalent because apply()'s
# `default: return` skipped the marker anyway; the line itself is now deleted,
# along with the pass it stood in, so there is no mutant left to adjudicate.
# Deleted, not re-keyed: the code is gone, not moved.

client/src/fold.ts 378:48 ArrayDeclaration ["Stryker was here"]
    `st.Conditions[v.actorId] ?? []` -> `?? ["Stryker was here"]` in conditionRemoved. The fallback fires only when the actor has NEVER had a condition (an emptied list is retained as [], which is not nullish). findIndex then compares `c.ID` on a string element — undefined — against the condition id, yielding -1, so the fold throws the same "not present" error it would have thrown. The assignment that would leak the sentinel is after that throw. Re-pointed 2026-08-23 (actor-kind Task 2) from 271:48 to 287:48, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 287 was byte-compared against HEAD's 271 and is identical, and col 48 was READ at the new location rather than offset: `[];`. Re-pointed 2026-08-24 (actor-kind Task 4) from 287:48 to 300:48, +13. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 300 was byte-compared against HEAD's 287 and is identical, and col 48 was READ at the new location rather than offset: `[];`. Re-pointed 2026-08-25 (prototype-safe maps) from 300:48 to 318:48, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 318:48 was byte-compared against HEAD's 300 and is identical, and col 48 was READ at the new location rather than offset: `[];`. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 318:48 to 308:48, -10. fold.ts's SHIFT IS UNIFORM: -10 for every key in it, all of which sit below `fold` itself, and 646 -> 636 lines is the whole of it. Three edits, each read off the diff rather than estimated: the header lost the internal/harness/fold.go citation, two lines becoming one (-1); the doc comment went from four lines to seven (+3); and the body collapsed to a single `for (const env of envelopes) apply(st, env);` — 13 lines deleted (the eight-line retracted-set pass with its trailing blank, and the five-line second loop) against 1 added, so -12 net, not -13. Line 308 was byte-compared against HEAD's 318 and is identical, and col 48 was READ at the new location rather than offset: `[];`. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 308:48 to 332:48, +24 — see the RE-KEYED 2026-08-31 file-level note above this file's entries for why the shift is uniform across all eighteen fold.ts keys. Verified by reading the tree directly (not by offset): the quoted expression above was located at the new line and the column re-derived from it, not carried over. Re-pointed 2026-08-31 (retraction-leaves Task 9) from 332:48 to 378:48: Task 9 inserted the actorRemoved arm into fold.ts's switch, 46 lines at line 240, and a Remove-actor button into view/dm.ts's Add-actor group, 15 lines at line 337 — both ABOVE this entry. Located by DIFFING the line BYTE FOR BYTE against HEAD's 332 (identical) and re-reading column 48 at the new location, never by trusting an offset: the two files moved by different amounts, and dm.ts's own 158:7 entry did not move at all.

client/src/fold.ts 407:19 OptionalChaining actor.resources[v.resource]
    `actor.resources?.[v.resource]` -> `actor.resources[v.resource]`. Removing the guard only matters if a stored Actor can have undefined resources. It cannot: line 137 is the only writer to st.Actors and it always goes through copyActor, which builds `resources` as an object literal before returning. Re-pointed 2026-08-23 (actor-kind Task 2) from 300:19 to 316:19, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 316 was byte-compared against HEAD's 300 and is identical, and col 19 was READ at the new location rather than offset: `actor.resources?.[v.re`. Re-pointed 2026-08-24 (actor-kind Task 4) from 316:19 to 329:19, +13. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 329 was byte-compared against HEAD's 316 and is identical, and col 19 was READ at the new location rather than offset: `actor.resources`. Re-pointed 2026-08-25 (prototype-safe maps) from 329:19 to 347:19, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 347:19 was byte-compared against HEAD's 329 and is identical, and col 19 was READ at the new location rather than offset: `actor.resources?.[v.re`. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 347:19 to 337:19, -10. fold.ts's SHIFT IS UNIFORM: -10 for every key in it, all of which sit below `fold` itself, and 646 -> 636 lines is the whole of it. Three edits, each read off the diff rather than estimated: the header lost the internal/harness/fold.go citation, two lines becoming one (-1); the doc comment went from four lines to seven (+3); and the body collapsed to a single `for (const env of envelopes) apply(st, env);` — 13 lines deleted (the eight-line retracted-set pass with its trailing blank, and the five-line second loop) against 1 added, so -12 net, not -13. Line 337 was byte-compared against HEAD's 347 and is identical, and col 19 was READ at the new location rather than offset: `actor.resources?.[v.r`. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 337:19 to 361:19, +24 — see the RE-KEYED 2026-08-31 file-level note above this file's entries for why the shift is uniform across all eighteen fold.ts keys. Verified by reading the tree directly (not by offset): the quoted expression above was located at the new line and the column re-derived from it, not carried over. Re-pointed 2026-08-31 (retraction-leaves Task 9) from 361:19 to 407:19: Task 9 inserted the actorRemoved arm into fold.ts's switch, 46 lines at line 240, and a Remove-actor button into view/dm.ts's Add-actor group, 15 lines at line 337 — both ABOVE this entry. Located by DIFFING the line BYTE FOR BYTE against HEAD's 361 (identical) and re-reading column 19 at the new location, never by trusting an offset: the two files moved by different amounts, and dm.ts's own 158:7 entry did not move at all.

client/src/fold.ts 416:11 EqualityOperator computed <= 0
    `computed < 0` -> `<= 0` in the resource clamp. The arms differ only at computed === 0, where the mutant assigns 0 over 0. Re-pointed 2026-08-23 (actor-kind Task 2) from 309:11 to 325:11, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 325 was byte-compared against HEAD's 309 and is identical, and col 11 was READ at the new location rather than offset: `computed < 0) computed`. Re-pointed 2026-08-24 (actor-kind Task 4) from 325:11 to 338:11, +13. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 338 was byte-compared against HEAD's 325 and is identical, and col 11 was READ at the new location rather than offset: `computed < 0) c`. Re-pointed 2026-08-25 (prototype-safe maps) from 338:11 to 356:11, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 356:11 was byte-compared against HEAD's 338 and is identical, and col 11 was READ at the new location rather than offset: `computed < 0) computed`. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 356:11 to 346:11, -10. fold.ts's SHIFT IS UNIFORM: -10 for every key in it, all of which sit below `fold` itself, and 646 -> 636 lines is the whole of it. Three edits, each read off the diff rather than estimated: the header lost the internal/harness/fold.go citation, two lines becoming one (-1); the doc comment went from four lines to seven (+3); and the body collapsed to a single `for (const env of envelopes) apply(st, env);` — 13 lines deleted (the eight-line retracted-set pass with its trailing blank, and the five-line second loop) against 1 added, so -12 net, not -13. Line 346 was byte-compared against HEAD's 356 and is identical, and col 11 was READ at the new location rather than offset: `computed < 0) compute`. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 346:11 to 370:11, +24 — see the RE-KEYED 2026-08-31 file-level note above this file's entries for why the shift is uniform across all eighteen fold.ts keys. Verified by reading the tree directly (not by offset): the quoted expression above was located at the new line and the column re-derived from it, not carried over. Re-pointed 2026-08-31 (retraction-leaves Task 9) from 370:11 to 416:11: Task 9 inserted the actorRemoved arm into fold.ts's switch, 46 lines at line 240, and a Remove-actor button into view/dm.ts's Add-actor group, 15 lines at line 337 — both ABOVE this entry. Located by DIFFING the line BYTE FOR BYTE against HEAD's 370 (identical) and re-reading column 11 at the new location, never by trusting an offset: the two files moved by different amounts, and dm.ts's own 158:7 entry did not move at all.

client/src/fold.ts 417:26 EqualityOperator computed >= res.max
    `computed > res.max` -> `>=`. Differ only at computed === res.max, where the mutant assigns max over max. Re-pointed 2026-08-23 (actor-kind Task 2) from 310:26 to 326:26, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 326 was byte-compared against HEAD's 310 and is identical, and col 26 was READ at the new location rather than offset: `computed > res.max) co`. Re-pointed 2026-08-24 (actor-kind Task 4) from 326:26 to 339:26, +13. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 339 was byte-compared against HEAD's 326 and is identical, and col 26 was READ at the new location rather than offset: `computed > res.`. Re-pointed 2026-08-25 (prototype-safe maps) from 339:26 to 357:26, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 357:26 was byte-compared against HEAD's 339 and is identical, and col 26 was READ at the new location rather than offset: `computed > res.max) co`. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 357:26 to 347:26, -10. fold.ts's SHIFT IS UNIFORM: -10 for every key in it, all of which sit below `fold` itself, and 646 -> 636 lines is the whole of it. Three edits, each read off the diff rather than estimated: the header lost the internal/harness/fold.go citation, two lines becoming one (-1); the doc comment went from four lines to seven (+3); and the body collapsed to a single `for (const env of envelopes) apply(st, env);` — 13 lines deleted (the eight-line retracted-set pass with its trailing blank, and the five-line second loop) against 1 added, so -12 net, not -13. Line 347 was byte-compared against HEAD's 357 and is identical, and col 26 was READ at the new location rather than offset: `computed > res.max) c`. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 347:26 to 371:26, +24 — see the RE-KEYED 2026-08-31 file-level note above this file's entries for why the shift is uniform across all eighteen fold.ts keys. Verified by reading the tree directly (not by offset): the quoted expression above was located at the new line and the column re-derived from it, not carried over. Re-pointed 2026-08-31 (retraction-leaves Task 9) from 371:26 to 417:26: Task 9 inserted the actorRemoved arm into fold.ts's switch, 46 lines at line 240, and a Remove-actor button into view/dm.ts's Add-actor group, 15 lines at line 337 — both ABOVE this entry. Located by DIFFING the line BYTE FOR BYTE against HEAD's 371 (identical) and re-reading column 26 at the new location, never by trusting an offset: the two files moved by different amounts, and dm.ts's own 158:7 entry did not move at all.

client/src/fold.ts 448:10 StringLiteral ""
    `case "attackRolled":` -> `case "":`. The case body is a bare `return`, and an attackRolled event whose label no longer matches falls through to `default:`, which is also a bare `return`. A no-op reached two ways. Re-pointed 2026-08-23 (actor-kind Task 2) from 341:10 to 357:10, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 357 was byte-compared against HEAD's 341 and is identical, and col 10 was READ at the new location rather than offset: `"attackRolled":`. Re-pointed 2026-08-24 (actor-kind Task 4) from 357:10 to 370:10, +13. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 370 was byte-compared against HEAD's 357 and is identical, and col 10 was READ at the new location rather than offset: `"attackRolled":`. Re-pointed 2026-08-25 (prototype-safe maps) from 370:10 to 388:10, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 388:10 was byte-compared against HEAD's 370 and is identical, and col 10 was READ at the new location rather than offset: `"attackRolled":`. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 388:10 to 378:10, -10. fold.ts's SHIFT IS UNIFORM: -10 for every key in it, all of which sit below `fold` itself, and 646 -> 636 lines is the whole of it. Three edits, each read off the diff rather than estimated: the header lost the internal/harness/fold.go citation, two lines becoming one (-1); the doc comment went from four lines to seven (+3); and the body collapsed to a single `for (const env of envelopes) apply(st, env);` — 13 lines deleted (the eight-line retracted-set pass with its trailing blank, and the five-line second loop) against 1 added, so -12 net, not -13. Line 378 was byte-compared against HEAD's 388 and is identical, and col 10 was READ at the new location rather than offset: `"attackRolled":`. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 378:10 to 402:10, +24 — see the RE-KEYED 2026-08-31 file-level note above this file's entries for why the shift is uniform across all eighteen fold.ts keys. Verified by reading the tree directly (not by offset): the quoted expression above was located at the new line and the column re-derived from it, not carried over. Re-pointed 2026-08-31 (retraction-leaves Task 9) from 402:10 to 448:10: Task 9 inserted the actorRemoved arm into fold.ts's switch, 46 lines at line 240, and a Remove-actor button into view/dm.ts's Add-actor group, 15 lines at line 337 — both ABOVE this entry. Located by DIFFING the line BYTE FOR BYTE against HEAD's 402 (identical) and re-reading column 10 at the new location, never by trusting an offset: the two files moved by different amounts, and dm.ts's own 158:7 entry did not move at all.

client/src/fold.ts 449:10 StringLiteral ""
    `case "abilityUsed":` -> `case "":`, same no-op-vs-no-op as above. Re-pointed 2026-08-23 (actor-kind Task 2) from 342:10 to 358:10, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 358 was byte-compared against HEAD's 342 and is identical, and col 10 was READ at the new location rather than offset: `"abilityUsed":`. Re-pointed 2026-08-24 (actor-kind Task 4) from 358:10 to 371:10, +13. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 371 was byte-compared against HEAD's 358 and is identical, and col 10 was READ at the new location rather than offset: `"abilityUsed":`. Re-pointed 2026-08-25 (prototype-safe maps) from 371:10 to 389:10, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 389:10 was byte-compared against HEAD's 371 and is identical, and col 10 was READ at the new location rather than offset: `"abilityUsed":`. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 389:10 to 379:10, -10. fold.ts's SHIFT IS UNIFORM: -10 for every key in it, all of which sit below `fold` itself, and 646 -> 636 lines is the whole of it. Three edits, each read off the diff rather than estimated: the header lost the internal/harness/fold.go citation, two lines becoming one (-1); the doc comment went from four lines to seven (+3); and the body collapsed to a single `for (const env of envelopes) apply(st, env);` — 13 lines deleted (the eight-line retracted-set pass with its trailing blank, and the five-line second loop) against 1 added, so -12 net, not -13. Line 379 was byte-compared against HEAD's 389 and is identical, and col 10 was READ at the new location rather than offset: `"abilityUsed":`. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 379:10 to 403:10, +24 — see the RE-KEYED 2026-08-31 file-level note above this file's entries for why the shift is uniform across all eighteen fold.ts keys. Verified by reading the tree directly (not by offset): the quoted expression above was located at the new line and the column re-derived from it, not carried over. Re-pointed 2026-08-31 (retraction-leaves Task 9) from 403:10 to 449:10: Task 9 inserted the actorRemoved arm into fold.ts's switch, 46 lines at line 240, and a Remove-actor button into view/dm.ts's Add-actor group, 15 lines at line 337 — both ABOVE this entry. Located by DIFFING the line BYTE FOR BYTE against HEAD's 403 (identical) and re-reading column 10 at the new location, never by trusting an offset: the two files moved by different amounts, and dm.ts's own 158:7 entry did not move at all.

client/src/fold.ts 450:5 ConditionalExpression case "adventureLoaded":
    Rewrites the `case "adventureLoaded":` clause itself. Every arm involved returns without touching state, so no reordering of them is observable. Re-pointed 2026-08-23 (actor-kind Task 2) from 343:5 to 359:5, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 359 was byte-compared against HEAD's 343 and is identical, and col 5 was READ at the new location rather than offset: `case "adventureLoaded"`. Re-pointed 2026-08-24 (actor-kind Task 4) from 359:5 to 372:5, +13. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 372 was byte-compared against HEAD's 359 and is identical, and col 5 was READ at the new location rather than offset: `case "adventure`. Re-pointed 2026-08-25 (prototype-safe maps) from 372:5 to 390:5, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 390:5 was byte-compared against HEAD's 372 and is identical, and col 5 was READ at the new location rather than offset: `case "adventureLoaded"`. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 390:5 to 380:5, -10. fold.ts's SHIFT IS UNIFORM: -10 for every key in it, all of which sit below `fold` itself, and 646 -> 636 lines is the whole of it. Three edits, each read off the diff rather than estimated: the header lost the internal/harness/fold.go citation, two lines becoming one (-1); the doc comment went from four lines to seven (+3); and the body collapsed to a single `for (const env of envelopes) apply(st, env);` — 13 lines deleted (the eight-line retracted-set pass with its trailing blank, and the five-line second loop) against 1 added, so -12 net, not -13. Line 380 was byte-compared against HEAD's 390 and is identical, and col 5 was READ at the new location rather than offset: `case "adventureLoaded`. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 380:5 to 404:5, +24 — see the RE-KEYED 2026-08-31 file-level note above this file's entries for why the shift is uniform across all eighteen fold.ts keys. Verified by reading the tree directly (not by offset): the quoted expression above was located at the new line and the column re-derived from it, not carried over. Re-pointed 2026-08-31 (retraction-leaves Task 9) from 404:5 to 450:5: Task 9 inserted the actorRemoved arm into fold.ts's switch, 46 lines at line 240, and a Remove-actor button into view/dm.ts's Add-actor group, 15 lines at line 337 — both ABOVE this entry. Located by DIFFING the line BYTE FOR BYTE against HEAD's 404 (identical) and re-reading column 5 at the new location, never by trusting an offset: the two files moved by different amounts, and dm.ts's own 158:7 entry did not move at all.

client/src/fold.ts 450:10 StringLiteral ""
    `case "adventureLoaded":` -> `case "":`, same no-op-vs-no-op as above. Re-pointed 2026-08-23 (actor-kind Task 2) from 343:10 to 359:10, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 359 was byte-compared against HEAD's 343 and is identical, and col 10 was READ at the new location rather than offset: `"adventureLoaded":`. Re-pointed 2026-08-24 (actor-kind Task 4) from 359:10 to 372:10, +13. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 372 was byte-compared against HEAD's 359 and is identical, and col 10 was READ at the new location rather than offset: `"adventureLoade`. Re-pointed 2026-08-25 (prototype-safe maps) from 372:10 to 390:10, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 390:10 was byte-compared against HEAD's 372 and is identical, and col 10 was READ at the new location rather than offset: `"adventureLoaded":`. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 390:10 to 380:10, -10. fold.ts's SHIFT IS UNIFORM: -10 for every key in it, all of which sit below `fold` itself, and 646 -> 636 lines is the whole of it. Three edits, each read off the diff rather than estimated: the header lost the internal/harness/fold.go citation, two lines becoming one (-1); the doc comment went from four lines to seven (+3); and the body collapsed to a single `for (const env of envelopes) apply(st, env);` — 13 lines deleted (the eight-line retracted-set pass with its trailing blank, and the five-line second loop) against 1 added, so -12 net, not -13. Line 380 was byte-compared against HEAD's 390 and is identical, and col 10 was READ at the new location rather than offset: `"adventureLoaded":`. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 380:10 to 404:10, +24 — see the RE-KEYED 2026-08-31 file-level note above this file's entries for why the shift is uniform across all eighteen fold.ts keys. Verified by reading the tree directly (not by offset): the quoted expression above was located at the new line and the column re-derived from it, not carried over. Re-pointed 2026-08-31 (retraction-leaves Task 9) from 404:10 to 450:10: Task 9 inserted the actorRemoved arm into fold.ts's switch, 46 lines at line 240, and a Remove-actor button into view/dm.ts's Add-actor group, 15 lines at line 337 — both ABOVE this entry. Located by DIFFING the line BYTE FOR BYTE against HEAD's 404 (identical) and re-reading column 10 at the new location, never by trusting an offset: the two files moved by different amounts, and dm.ts's own 158:7 entry did not move at all.

client/src/fold.ts 452:5 ConditionalExpression default:
    Rewrites the `default:` clause. Its body is a bare `return`, shared with the three no-effect event kinds above it; nothing distinguishes reaching it from reaching them. Re-pointed 2026-08-23 (actor-kind Task 2) from 345:5 to 361:5, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 361 was byte-compared against HEAD's 345 and is identical, and col 5 was READ at the new location rather than offset: `default:`. Re-pointed 2026-08-24 (actor-kind Task 4) from 361:5 to 374:5, +13. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 374 was byte-compared against HEAD's 361 and is identical, and col 5 was READ at the new location rather than offset: `default:`. Re-pointed 2026-08-25 (prototype-safe maps) from 374:5 to 392:5, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 392:5 was byte-compared against HEAD's 374 and is identical, and col 5 was READ at the new location rather than offset: `default:`. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 392:5 to 382:5, -10. fold.ts's SHIFT IS UNIFORM: -10 for every key in it, all of which sit below `fold` itself, and 646 -> 636 lines is the whole of it. Three edits, each read off the diff rather than estimated: the header lost the internal/harness/fold.go citation, two lines becoming one (-1); the doc comment went from four lines to seven (+3); and the body collapsed to a single `for (const env of envelopes) apply(st, env);` — 13 lines deleted (the eight-line retracted-set pass with its trailing blank, and the five-line second loop) against 1 added, so -12 net, not -13. Line 382 was byte-compared against HEAD's 392 and is identical, and col 5 was READ at the new location rather than offset: `default:`. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 382:5 to 406:5, +24 — see the RE-KEYED 2026-08-31 file-level note above this file's entries for why the shift is uniform across all eighteen fold.ts keys. Verified by reading the tree directly (not by offset): the quoted expression above was located at the new line and the column re-derived from it, not carried over. Re-pointed 2026-08-31 (retraction-leaves Task 9) from 406:5 to 452:5: Task 9 inserted the actorRemoved arm into fold.ts's switch, 46 lines at line 240, and a Remove-actor button into view/dm.ts's Add-actor group, 15 lines at line 337 — both ABOVE this entry. Located by DIFFING the line BYTE FOR BYTE against HEAD's 406 (identical) and re-reading column 5 at the new location, never by trusting an offset: the two files moved by different amounts, and dm.ts's own 158:7 entry did not move at all.

client/src/fold.ts 573:34 EqualityOperator Number(e.sequence) >= head
    `Number(e.sequence) > head` -> `>=` when computing headSequence. Differ only at equality, where the mutant assigns head its current value. (The `-> true` mutant at this location IS killable and is killed by "headSequence is the MAXIMUM sequence, not the last envelope's".) Re-pointed 2026-08-23 (actor-kind), +7: copyActor gained a kind field and its comment, both above this line. Line 473 was byte-compared against HEAD's 466 and is identical; col 34 read at the new location is `Number(e.sequence) >`. Re-pointed 2026-08-23 (actor-kind Task 2) from 473:34 to 489:34, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 489 was byte-compared against HEAD's 473 and is identical, and col 34 was READ at the new location rather than offset: `Number(e.sequence) > h`. Re-pointed 2026-08-24 (actor-kind Task 4) from 489:34 to 491:34, +2. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 491 was byte-compared against HEAD's 489 and is identical, and col 34 was READ at the new location rather than offset: `Number(e.sequen`. Re-pointed 2026-08-25 (prototype-safe maps) from 491:34 to 509:34, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 509:34 was byte-compared against HEAD's 491 and is identical, and col 34 was READ at the new location rather than offset: `Number(e.sequence) > h`. Re-pointed 2026-08-30 (Task 4 fix round 2, not this file's own doing) from 509:34 to 513:34, +4: Task 3 (maps-as-geometry) added the doorKey export and its doc comment above every entry in this file, landing without check:ts-mutation having run over it — the coordinator's own admission. Derived from reports/mutation/mutation.json (Task 4 fix round 2's container run) by matching (file, mutator, replacement), not by counting lines by hand — fix round 1's hand-derived re-keys on other files landed wrong by exactly this kind of drift, twice. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 513:34 to 503:34, -10. fold.ts's SHIFT IS UNIFORM: -10 for every key in it, all of which sit below `fold` itself, and 646 -> 636 lines is the whole of it. Three edits, each read off the diff rather than estimated: the header lost the internal/harness/fold.go citation, two lines becoming one (-1); the doc comment went from four lines to seven (+3); and the body collapsed to a single `for (const env of envelopes) apply(st, env);` — 13 lines deleted (the eight-line retracted-set pass with its trailing blank, and the five-line second loop) against 1 added, so -12 net, not -13. Line 503 was byte-compared against HEAD's 513 and is identical, and col 34 was READ at the new location rather than offset: `Number(e.sequence) > `. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 503:34 to 527:34, +24 — see the RE-KEYED 2026-08-31 file-level note above this file's entries for why the shift is uniform across all eighteen fold.ts keys. Verified by reading the tree directly (not by offset): the quoted expression above was located at the new line and the column re-derived from it, not carried over. Re-pointed 2026-08-31 (retraction-leaves Task 9) from 527:34 to 573:34: Task 9 inserted the actorRemoved arm into fold.ts's switch, 46 lines at line 240, and a Remove-actor button into view/dm.ts's Add-actor group, 15 lines at line 337 — both ABOVE this entry. Located by DIFFING the line BYTE FOR BYTE against HEAD's 527 (identical) and re-reading column 34 at the new location, never by trusting an offset: the two files moved by different amounts, and dm.ts's own 158:7 entry did not move at all.

client/src/fold.ts 689:7 ConditionalExpression true
    `if (a.actorId !== "")` -> `true` in actorJSON's omitempty emission. An Actor in state can never have an empty actorId: line 137 is the only writer, and line 135 rejects the event before it with "actor added with no actor or empty id". The condition is therefore already always true. Re-pointed 2026-08-23 (actor-kind) from 558:7 to 583:7, and THE OLD KEY WAS ALREADY WRONG ON MAIN — this is a correction, not just a shift. HEAD's line 558 is `ActorID: t.ActorID,` inside foldToDumpJSON's Tokens mapper: no conditional and no string literal, so it could not host either of these mutants and this pair has been excusing a location it does not describe. actorJSON's `if (a.actorId !== "")` was at 576 on main and is at 583 now (+7, copyActor's kind field and comment above it). Both columns were READ at the new location rather than offset: col 7 is `a.actorId !== ""`, col 21 is the `""`. That makes this the seventh stale TS key found by reading rather than by the gate, and the re-key note above is the reason to expect more: the 2026-08-21 sweep re-derived fold.ts's eleven lines and this pair still came out wrong. Re-pointed 2026-08-23 (actor-kind Task 2) from 583:7 to 599:7, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 599 was byte-compared against HEAD's 583 and is identical, and col 7 was READ at the new location rather than offset: `a.actorId !== "") out[`. Re-pointed 2026-08-24 (actor-kind Task 4) from 599:7 to 601:7, +2. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 601 was byte-compared against HEAD's 599 and is identical, and col 7 was READ at the new location rather than offset: `a.actorId !== "`. Re-pointed 2026-08-25 (prototype-safe maps) from 601:7 to 619:7, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 619:7 was byte-compared against HEAD's 601 and is identical, and col 7 was READ at the new location rather than offset: `a.actorId !== "") out[`. Re-pointed 2026-08-30 (Task 4 fix round 2, not this file's own doing) from 619:7 to 623:7, +4: Task 3 (maps-as-geometry) added the doorKey export and its doc comment above every entry in this file, landing without check:ts-mutation having run over it — the coordinator's own admission. Derived from reports/mutation/mutation.json (Task 4 fix round 2's container run) by matching (file, mutator, replacement), not by counting lines by hand — fix round 1's hand-derived re-keys on other files landed wrong by exactly this kind of drift, twice. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 623:7 to 613:7, -10. fold.ts's SHIFT IS UNIFORM: -10 for every key in it, all of which sit below `fold` itself, and 646 -> 636 lines is the whole of it. Three edits, each read off the diff rather than estimated: the header lost the internal/harness/fold.go citation, two lines becoming one (-1); the doc comment went from four lines to seven (+3); and the body collapsed to a single `for (const env of envelopes) apply(st, env);` — 13 lines deleted (the eight-line retracted-set pass with its trailing blank, and the five-line second loop) against 1 added, so -12 net, not -13. Line 613 was byte-compared against HEAD's 623 and is identical, and col 7 was READ at the new location rather than offset: `a.actorId !== "") out`. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 613:7 to 637:7, +24 — see the RE-KEYED 2026-08-31 file-level note above this file's entries for why the shift is uniform across all eighteen fold.ts keys. Verified by reading the tree directly (not by offset): the quoted expression above was located at the new line and the column re-derived from it, not carried over. Re-pointed 2026-08-31 (retraction-leaves Task 9) from 637:7 to 683:7: Task 9 inserted the actorRemoved arm into fold.ts's switch, 46 lines at line 240, and a Remove-actor button into view/dm.ts's Add-actor group, 15 lines at line 337 — both ABOVE this entry. Located by DIFFING the line BYTE FOR BYTE against HEAD's 637 (identical) and re-reading column 7 at the new location, never by trusting an offset: the two files moved by different amounts, and dm.ts's own 158:7 entry did not move at all. Re-pointed 2026-09-01 (retraction-leaves Task 10) from 683:7 to 687:7, +4: the only edit to fold.ts was a comment inside foldToDumpJSON's Explored-omission block, 7 lines replaced by 11, and it sits ABOVE this entry — the corpus stopped carrying a bare-canvas scene when create_scene began refusing an undeclared square, so the block's measured count went from ten cases to eight. THE SHIFT IS NOT UNIFORM: every other key in this file is above line 620 and does not move at all; only these two do. Line 687 was diffed BYTE FOR BYTE against HEAD's 683 (identical: `  if (a.actorId !== "") out["actor_id"] = a.actorId;`) and column 7 was READ at the new location rather than offset. Re-pointed again 2026-09-01 (Task 10, fix round 1) from 687:7 to 689:7, +2: the Explored-omission comment in foldToDumpJSON grew by two more lines when its measured count was corrected (9 injection failures, of which 8 are corpus cases). One hunk, at line 623, still ABOVE this entry and still BELOW every other key in this file, so THE SHIFT REMAINS NON-UNIFORM: these two move, the other sixteen do not. Line 689 was diffed BYTE FOR BYTE against HEAD's 683 (identical: `  if (a.actorId !== "") out["actor_id"] = a.actorId;`) and column 7 was READ at the new location rather than offset.

client/src/fold.ts 689:21 StringLiteral "Stryker was here!"
    Same line, comparing against "Stryker was here!" instead of "". Also always true, for the same reason: no stored actorId equals either string. Re-pointed 2026-08-23 (actor-kind) from 558:21 to 583:21, and THE OLD KEY WAS ALREADY WRONG ON MAIN — this is a correction, not just a shift. HEAD's line 558 is `ActorID: t.ActorID,` inside foldToDumpJSON's Tokens mapper: no conditional and no string literal, so it could not host either of these mutants and this pair has been excusing a location it does not describe. actorJSON's `if (a.actorId !== "")` was at 576 on main and is at 583 now (+7, copyActor's kind field and comment above it). Both columns were READ at the new location rather than offset: col 7 is `a.actorId !== ""`, col 21 is the `""`. That makes this the seventh stale TS key found by reading rather than by the gate, and the re-key note above is the reason to expect more: the 2026-08-21 sweep re-derived fold.ts's eleven lines and this pair still came out wrong. Re-pointed 2026-08-23 (actor-kind Task 2) from 583:21 to 599:21, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 599 was byte-compared against HEAD's 583 and is identical, and col 21 was READ at the new location rather than offset: `"") out["actor_id"] = `. Re-pointed 2026-08-24 (actor-kind Task 4) from 599:21 to 601:21, +2. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 601 was byte-compared against HEAD's 599 and is identical, and col 21 was READ at the new location rather than offset: `"") out["actor_`. Re-pointed 2026-08-25 (prototype-safe maps) from 601:21 to 619:21, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 619:21 was byte-compared against HEAD's 601 and is identical, and col 21 was READ at the new location rather than offset: `"") out["actor_id"] = `. Re-pointed 2026-08-30 (Task 4 fix round 2, not this file's own doing) from 619:21 to 623:21, +4: Task 3 (maps-as-geometry) added the doorKey export and its doc comment above every entry in this file, landing without check:ts-mutation having run over it — the coordinator's own admission. Derived from reports/mutation/mutation.json (Task 4 fix round 2's container run) by matching (file, mutator, replacement), not by counting lines by hand — fix round 1's hand-derived re-keys on other files landed wrong by exactly this kind of drift, twice. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 623:21 to 613:21, -10. fold.ts's SHIFT IS UNIFORM: -10 for every key in it, all of which sit below `fold` itself, and 646 -> 636 lines is the whole of it. Three edits, each read off the diff rather than estimated: the header lost the internal/harness/fold.go citation, two lines becoming one (-1); the doc comment went from four lines to seven (+3); and the body collapsed to a single `for (const env of envelopes) apply(st, env);` — 13 lines deleted (the eight-line retracted-set pass with its trailing blank, and the five-line second loop) against 1 added, so -12 net, not -13. Line 613 was byte-compared against HEAD's 623 and is identical, and col 21 was READ at the new location rather than offset: `"") out["actor_id"] =`. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 613:21 to 637:21, +24 — see the RE-KEYED 2026-08-31 file-level note above this file's entries for why the shift is uniform across all eighteen fold.ts keys. Verified by reading the tree directly (not by offset): the quoted expression above was located at the new line and the column re-derived from it, not carried over. Re-pointed 2026-08-31 (retraction-leaves Task 9) from 637:21 to 683:21: Task 9 inserted the actorRemoved arm into fold.ts's switch, 46 lines at line 240, and a Remove-actor button into view/dm.ts's Add-actor group, 15 lines at line 337 — both ABOVE this entry. Located by DIFFING the line BYTE FOR BYTE against HEAD's 637 (identical) and re-reading column 21 at the new location, never by trusting an offset: the two files moved by different amounts, and dm.ts's own 158:7 entry did not move at all. Re-pointed 2026-09-01 (retraction-leaves Task 10) from 683:21 to 687:21, +4: the only edit to fold.ts was a comment inside foldToDumpJSON's Explored-omission block, 7 lines replaced by 11, and it sits ABOVE this entry — the corpus stopped carrying a bare-canvas scene when create_scene began refusing an undeclared square, so the block's measured count went from ten cases to eight. THE SHIFT IS NOT UNIFORM: every other key in this file is above line 620 and does not move at all; only these two do. Line 687 was diffed BYTE FOR BYTE against HEAD's 683 (identical: `  if (a.actorId !== "") out["actor_id"] = a.actorId;`) and column 21 was READ at the new location rather than offset. Re-pointed again 2026-09-01 (Task 10, fix round 1) from 687:21 to 689:21, +2: the Explored-omission comment in foldToDumpJSON grew by two more lines when its measured count was corrected (9 injection failures, of which 8 are corpus cases). One hunk, at line 623, still ABOVE this entry and still BELOW every other key in this file, so THE SHIFT REMAINS NON-UNIFORM: these two move, the other sixteen do not. Line 689 was diffed BYTE FOR BYTE against HEAD's 683 (identical: `  if (a.actorId !== "") out["actor_id"] = a.actorId;`) and column 21 was READ at the new location rather than offset.

client/src/fold.ts 591:32 ArrayDeclaration ["Stryker was here"]
    `Objects: (s.Objects ?? []).map(...)` -> `?? ["Stryker was here"]` in foldToDumpJSON. The `??` right-hand side is evaluated only when `s.Objects` is nullish, and foldToDumpJSON folds its own state on the line above (`const st = fold(envelopes)`), so every Scene it renders came from fold.ts's sceneCreated arm with a real array. RE-EXAMINED 2026-08-25 against the prototype defect that made this entry's four neighbours wrong, and this one is genuinely untouched by it — which is the distinction worth recording, since the neighbours' reasons read almost identically. sortedMap ITERATES st.Scenes (`Object.keys(m)`) instead of looking a key up in it, and Object.keys returns own properties only, so no prototype member was ever handed to this mapper. Measured both ways by hand-applying the mutant: it survives the whole suite on HEAD 9d9d9df AND after the fix (684 pass, 0 fail), unlike 259:11/260:11/261:11/261:37/419:7, every one of which was killable on HEAD. fold.ts's own comment on these defaults already says they are "belt-and-braces rather than a path any golden actually exercises" — this entry is that sentence, verified. Re-pointed 2026-08-25 from 509:32 to 527:32, +18. Line 527:32 was byte-compared against HEAD's 509 and is identical, and col 32 was READ at the new location rather than offset: `[]).map((o) => ({`. Re-pointed 2026-08-30 (Task 4 fix round 2, not this file's own doing) from 527:32 to 531:32, +4: Task 3 (maps-as-geometry) added the doorKey export and its doc comment above every entry in this file, landing without check:ts-mutation having run over it — the coordinator's own admission. Derived from reports/mutation/mutation.json (Task 4 fix round 2's container run) by matching (file, mutator, replacement), not by counting lines by hand — fix round 1's hand-derived re-keys on other files landed wrong by exactly this kind of drift, twice. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 531:32 to 521:32, -10. fold.ts's SHIFT IS UNIFORM: -10 for every key in it, all of which sit below `fold` itself, and 646 -> 636 lines is the whole of it. Three edits, each read off the diff rather than estimated: the header lost the internal/harness/fold.go citation, two lines becoming one (-1); the doc comment went from four lines to seven (+3); and the body collapsed to a single `for (const env of envelopes) apply(st, env);` — 13 lines deleted (the eight-line retracted-set pass with its trailing blank, and the five-line second loop) against 1 added, so -12 net, not -13. Line 521 was byte-compared against HEAD's 531 and is identical, and col 32 was READ at the new location rather than offset: `[]).map((o) => ({`. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 521:32 to 545:32, +24 — see the RE-KEYED 2026-08-31 file-level note above this file's entries for why the shift is uniform across all eighteen fold.ts keys. Verified by reading the tree directly (not by offset): the quoted expression above was located at the new line and the column re-derived from it, not carried over. Re-pointed 2026-08-31 (retraction-leaves Task 9) from 545:32 to 591:32: Task 9 inserted the actorRemoved arm into fold.ts's switch, 46 lines at line 240, and a Remove-actor button into view/dm.ts's Add-actor group, 15 lines at line 337 — both ABOVE this entry. Located by DIFFING the line BYTE FOR BYTE against HEAD's 545 (identical) and re-reading column 32 at the new location, never by trusting an offset: the two files moved by different amounts, and dm.ts's own 158:7 entry did not move at all. PRE-EXISTING STALENESS REPAIRED 2026-08-31 (retraction-leaves Task 9), and NOT caused by this task's +46: this body pointed at `fold.ts:111`, which was already a COMMENT line at HEAD (the Scene literal opens at :101 and its fields sit at :106/:107/:114). Repaired by NAMING the arm instead of a line, per this branch's own citation rule and the precedent already set on the Go side, so it cannot rot again.

client/src/fold.ts 483:7 ConditionalExpression false
    `if (!sc.OpenDoors) sc.OpenDoors = emptyMap<boolean>();` -> `if (false)` in ensureOpenDoors. Same argument as the sceneSeen triple, one function over: fold.ts's sceneCreated arm sets `OpenDoors: emptyMap<boolean>()`, and door events only ever `delete` a KEY from it, never the property, so no folded Scene reaches ensureOpenDoors without it. The function still returns `sc.OpenDoors`, the same object it would have returned. SAME CORRECTION (2026-08-25): the old reason rested on "111 is the only writer" and a lookup needed no writer. Measured against HEAD 9d9d9df: `doorOpened` naming scene "hasOwnProperty" folded cleanly unmutated and threw `TypeError: undefined is not an object (evaluating 'ensureOpenDoors(sc)[doorKey(...)] = !0')` with the mutant applied. The door arms' own scene lookup now answers honestly, so that log is refused before ensureOpenDoors is called ('door opened in unknown scene "hasOwnProperty"'); the accept side of the same id is pinned by "a scene whose id names a prototype member is created, not refused as a duplicate" (client/test/fold-unit.test.ts), which opens a door on it and reads OpenDoors back. Re-verified after the fix: mutant applied, 684 pass, 0 fail. The `-> true` and `BooleanLiteral` mutants at this location are killable and were killed on 2026-08-24 by "opening a second door leaves the first one open, and shutting one leaves the other". Re-pointed 2026-08-25 from 401:7 to 419:7, +18. The line changed (`{}` -> `emptyMap<boolean>()`), so no byte comparison; col 7 was READ at the new location: `!sc.OpenDoors) sc.Open`. Re-pointed 2026-08-30 (Task 4 fix round 2, not this file's own doing) from 419:7 to 423:7, +4: Task 3 (maps-as-geometry) added the doorKey export and its doc comment above every entry in this file, landing without check:ts-mutation having run over it — the coordinator's own admission. Derived from reports/mutation/mutation.json (Task 4 fix round 2's container run) by matching (file, mutator, replacement), not by counting lines by hand — fix round 1's hand-derived re-keys on other files landed wrong by exactly this kind of drift, twice. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 423:7 to 413:7, -10. fold.ts's SHIFT IS UNIFORM: -10 for every key in it, all of which sit below `fold` itself, and 646 -> 636 lines is the whole of it. Three edits, each read off the diff rather than estimated: the header lost the internal/harness/fold.go citation, two lines becoming one (-1); the doc comment went from four lines to seven (+3); and the body collapsed to a single `for (const env of envelopes) apply(st, env);` — 13 lines deleted (the eight-line retracted-set pass with its trailing blank, and the five-line second loop) against 1 added, so -12 net, not -13. Line 413 was byte-compared against HEAD's 423 and is identical, and col 7 was READ at the new location rather than offset: `!sc.OpenDoors) sc.Ope`. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 413:7 to 437:7, +24 — see the RE-KEYED 2026-08-31 file-level note above this file's entries for why the shift is uniform across all eighteen fold.ts keys. Verified by reading the tree directly (not by offset): the quoted expression above was located at the new line and the column re-derived from it, not carried over. Re-pointed 2026-08-31 (retraction-leaves Task 9) from 437:7 to 483:7: Task 9 inserted the actorRemoved arm into fold.ts's switch, 46 lines at line 240, and a Remove-actor button into view/dm.ts's Add-actor group, 15 lines at line 337 — both ABOVE this entry. Located by DIFFING the line BYTE FOR BYTE against HEAD's 437 (identical) and re-reading column 7 at the new location, never by trusting an offset: the two files moved by different amounts, and dm.ts's own 158:7 entry did not move at all. PRE-EXISTING STALENESS REPAIRED 2026-08-31 (retraction-leaves Task 9), and NOT caused by this task's +46: this body pointed at `fold.ts:111`, which was already a COMMENT line at HEAD (the Scene literal opens at :101 and its fields sit at :106/:107/:114). Repaired by NAMING the arm instead of a line, per this branch's own citation rule and the precedent already set on the Go side, so it cannot rot again.

client/src/fold.ts 321:37 ArrayDeclaration ["Stryker was here"]
    `sc.Objects = []` -> `sc.Objects = ["Stryker was here"]` inside the guard body above. The sentinel can only reach anything if the branch is taken, and after 2026-08-25 it is not. ITS OLD EVIDENCE WAS THE WRONG MEASUREMENT. The entry said "measured: zero 'GUARD REACHED 243' across the whole suite", which was a fact about the SUITE and was being read as a fact about the CODE — no test folded a scene whose id named a prototype member, and one that did reached this guard immediately. Measured against HEAD 9d9d9df: a sceneSeen naming scene "hasOwnProperty" took the branch, and with this mutant applied the sentinel landed in `Object.prototype.hasOwnProperty.Objects[0]` — not in st, but in the prototype every object in the process inherits from, which a test can read and assert on. Killable, therefore, and adjudicated otherwise. st.Scenes is a prototype-less dictionary now (state.ts's emptyMap), so the lookup that reached the guard returns undefined and the arm rejects instead; "sceneSeen for a scene that never arrived is refused, even when its id names a prototype member" (client/test/fold-rejections.test.ts) holds it there. Re-verified after the fix: mutant applied, 684 pass, 0 fail. Re-pointed 2026-08-25 from 243:37 to 261:37, +18. Line 261:37 was byte-compared against HEAD's 243 and is identical, and col 37 was READ at the new location rather than offset: `[];`. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 261:37 to 251:37, -10. fold.ts's SHIFT IS UNIFORM: -10 for every key in it, all of which sit below `fold` itself, and 646 -> 636 lines is the whole of it. Three edits, each read off the diff rather than estimated: the header lost the internal/harness/fold.go citation, two lines becoming one (-1); the doc comment went from four lines to seven (+3); and the body collapsed to a single `for (const env of envelopes) apply(st, env);` — 13 lines deleted (the eight-line retracted-set pass with its trailing blank, and the five-line second loop) against 1 added, so -12 net, not -13. Line 251 was byte-compared against HEAD's 261 and is identical, and col 37 was READ at the new location rather than offset: `[];`. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 251:37 to 275:37, +24 — see the RE-KEYED 2026-08-31 file-level note above this file's entries for why the shift is uniform across all eighteen fold.ts keys. Verified by reading the tree directly (not by offset): the quoted expression above was located at the new line and the column re-derived from it, not carried over. Re-pointed 2026-08-31 (retraction-leaves Task 9) from 275:37 to 321:37: Task 9 inserted the actorRemoved arm into fold.ts's switch, 46 lines at line 240, and a Remove-actor button into view/dm.ts's Add-actor group, 15 lines at line 337 — both ABOVE this entry. Located by DIFFING the line BYTE FOR BYTE against HEAD's 275 (identical) and re-reading column 37 at the new location, never by trusting an offset: the two files moved by different amounts, and dm.ts's own 158:7 entry did not move at all.

client/src/fold.ts 321:11 ConditionalExpression false
    `if (!sc.Objects) sc.Objects = [];` -> `if (false)`. Third of the same triple; fold.ts's sceneCreated arm sets `Objects: objects`, an array built by `.map()` and never undefined. SAME CORRECTION AS ITS TWO NEIGHBOURS (2026-08-25) and the loudest of the three: measured against HEAD 9d9d9df, this mutant turned a cleanly-folding sceneSeen on scene "hasOwnProperty" into `TypeError: undefined is not an object (evaluating 'sc.Objects.findIndex')`. The old premise — fold.ts's only writer to st.Scenes — was true and did not entail what it was used for, because `st.Scenes[id]` on an object literal answers for ids nobody wrote. st.Scenes is prototype-less now (state.ts's emptyMap), and the one route that reached this guard is pinned shut by "sceneSeen for a scene that never arrived is refused, even when its id names a prototype member" (client/test/fold-rejections.test.ts). Re-verified after the fix: mutant applied, 684 pass, 0 fail. The other two mutants at this exact location — `-> true` and `BooleanLiteral -> sc.Objects` — are NOT equivalent and were killed on 2026-08-24 by "sceneSeen's merge keeps SLOTS, keeps objects it does not mention, and takes a positionless one at the origin", which is why the replacement text is part of the key. Re-pointed 2026-08-25 from 243:11 to 261:11, +18. Line 261:11 was byte-compared against HEAD's 243 and is identical, and col 11 was READ at the new location rather than offset: `!sc.Objects) sc.Object`. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 261:11 to 251:11, -10. fold.ts's SHIFT IS UNIFORM: -10 for every key in it, all of which sit below `fold` itself, and 646 -> 636 lines is the whole of it. Three edits, each read off the diff rather than estimated: the header lost the internal/harness/fold.go citation, two lines becoming one (-1); the doc comment went from four lines to seven (+3); and the body collapsed to a single `for (const env of envelopes) apply(st, env);` — 13 lines deleted (the eight-line retracted-set pass with its trailing blank, and the five-line second loop) against 1 added, so -12 net, not -13. Line 251 was byte-compared against HEAD's 261 and is identical, and col 11 was READ at the new location rather than offset: `!sc.Objects) sc.Objec`. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 251:11 to 275:11, +24 — see the RE-KEYED 2026-08-31 file-level note above this file's entries for why the shift is uniform across all eighteen fold.ts keys. Verified by reading the tree directly (not by offset): the quoted expression above was located at the new line and the column re-derived from it, not carried over. Re-pointed 2026-08-31 (retraction-leaves Task 9) from 275:11 to 321:11: Task 9 inserted the actorRemoved arm into fold.ts's switch, 46 lines at line 240, and a Remove-actor button into view/dm.ts's Add-actor group, 15 lines at line 337 — both ABOVE this entry. Located by DIFFING the line BYTE FOR BYTE against HEAD's 275 (identical) and re-reading column 11 at the new location, never by trusting an offset: the two files moved by different amounts, and dm.ts's own 158:7 entry did not move at all. PRE-EXISTING STALENESS REPAIRED 2026-08-31 (retraction-leaves Task 9), and NOT caused by this task's +46: this body pointed at `fold.ts:111`, which was already a COMMENT line at HEAD (the Scene literal opens at :101 and its fields sit at :106/:107/:114). Repaired by NAMING the arm instead of a line, per this branch's own citation rule and the precedent already set on the Go side, so it cannot rot again.

client/src/fold.ts 320:11 ConditionalExpression false
    `if (!sc.Explored) sc.Explored = emptyMap<boolean>();` -> `if (false)`, the same guard one line down. Equivalent for the same reason and worth keeping separate for the same one: the Go arm it mirrors leaves Explored NIL at SceneCreated (apply.go's SceneCreated arm has no Explored field), so this TS guard mirrors a Go guard that IS live, and only the TS side's stricter initialisation in fold.ts's sceneCreated arm makes it dead. SAME CORRECTION AS ITS NEIGHBOUR (2026-08-25): the old premise stopped at "111 is the only writer", which said nothing about a lookup that finds a prototype member instead. Measured against HEAD 9d9d9df: with the mutant applied, a sceneSeen naming scene "hasOwnProperty" throws `TypeError: undefined is not an object (evaluating 'sc.Explored[key] = !0')` where the unmutated fold folds it cleanly — killable, not equivalent. st.Scenes is prototype-less now (state.ts's emptyMap) and that log is rejected before this line by "sceneSeen for a scene that never arrived is refused, even when its id names a prototype member" (client/test/fold-rejections.test.ts). Re-verified after the fix: mutant applied, 684 pass, 0 fail. Re-pointed 2026-08-25 from 242:11 to 260:11, +18. The line changed (`{}` -> `emptyMap<boolean>()`), so no byte comparison; col 11 was READ at the new location: `!sc.Explored) sc.Explo`. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 260:11 to 250:11, -10. fold.ts's SHIFT IS UNIFORM: -10 for every key in it, all of which sit below `fold` itself, and 646 -> 636 lines is the whole of it. Three edits, each read off the diff rather than estimated: the header lost the internal/harness/fold.go citation, two lines becoming one (-1); the doc comment went from four lines to seven (+3); and the body collapsed to a single `for (const env of envelopes) apply(st, env);` — 13 lines deleted (the eight-line retracted-set pass with its trailing blank, and the five-line second loop) against 1 added, so -12 net, not -13. Line 250 was byte-compared against HEAD's 260 and is identical, and col 11 was READ at the new location rather than offset: `!sc.Explored) sc.Expl`. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 250:11 to 274:11, +24 — see the RE-KEYED 2026-08-31 file-level note above this file's entries for why the shift is uniform across all eighteen fold.ts keys. Verified by reading the tree directly (not by offset): the quoted expression above was located at the new line and the column re-derived from it, not carried over. Re-pointed 2026-08-31 (retraction-leaves Task 9) from 274:11 to 320:11: Task 9 inserted the actorRemoved arm into fold.ts's switch, 46 lines at line 240, and a Remove-actor button into view/dm.ts's Add-actor group, 15 lines at line 337 — both ABOVE this entry. Located by DIFFING the line BYTE FOR BYTE against HEAD's 274 (identical) and re-reading column 11 at the new location, never by trusting an offset: the two files moved by different amounts, and dm.ts's own 158:7 entry did not move at all. PRE-EXISTING STALENESS REPAIRED 2026-08-31 (retraction-leaves Task 9), and NOT caused by this task's +46: this body pointed at `fold.ts:111`, which was already a COMMENT line at HEAD (the Scene literal opens at :101 and its fields sit at :106/:107/:114). Repaired by NAMING the arm instead of a line, per this branch's own citation rule and the precedent already set on the Go side, so it cannot rot again.

client/src/fold.ts 319:11 ConditionalExpression false
    `if (!sc.Tiles) sc.Tiles = emptyMap<Tile>();` -> `if (false)` in the sceneSeen arm. `sc` can only be a Scene fold.ts's sceneCreated arm built, and that arm sets `Tiles: tiles`; with the condition pinned false the assignment that never happened still never happens, and the tile loop below writes into the same object either way. SAME VERDICT, DIFFERENT PREMISE, AND THE OLD PREMISE WAS FALSE (2026-08-25). This entry used to argue from "111 is the only writer to st.Scenes" alone, and that inference does not hold: a LOOKUP needs no writer. st.Scenes was an object literal, so `st.Scenes["hasOwnProperty"]` returned Object.prototype.hasOwnProperty — truthy, and a Scene in nothing but the type system — and this guard WAS reached, with `sc.Tiles` undefined. Measured against HEAD 9d9d9df by hand-applying the mutant: a sceneSeen naming that scene folds cleanly unmutated and throws `TypeError: undefined is not an object (evaluating 'sc.Tiles[key] = ...')` with the mutant applied. So this entry was excusing a KILLABLE mutant, which this file's header calls worse than an unadjudicated survivor. st.Scenes is a prototype-less dictionary now (state.ts's emptyMap), which is the half of the premise that was missing: a lookup can return only what a writer put there. The route is closed by a test rather than by argument — "sceneSeen for a scene that never arrived is refused, even when its id names a prototype member" (client/test/fold-rejections.test.ts) folds exactly that log and requires the FoldError. Re-verified after the fix by hand-applying the mutant again: 684 pass, 0 fail. Re-pointed 2026-08-25 from 241:11 to 259:11, +18 (see the uniform-shift note on any other fold.ts key). The LINE ITSELF changed — `{}` became `emptyMap<Tile>()` — so it was not byte-compared; the condition is untouched and col 11 was READ at the new location: `!sc.Tiles) sc.Tiles = `. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 259:11 to 249:11, -10. fold.ts's SHIFT IS UNIFORM: -10 for every key in it, all of which sit below `fold` itself, and 646 -> 636 lines is the whole of it. Three edits, each read off the diff rather than estimated: the header lost the internal/harness/fold.go citation, two lines becoming one (-1); the doc comment went from four lines to seven (+3); and the body collapsed to a single `for (const env of envelopes) apply(st, env);` — 13 lines deleted (the eight-line retracted-set pass with its trailing blank, and the five-line second loop) against 1 added, so -12 net, not -13. Line 249 was byte-compared against HEAD's 259 and is identical, and col 11 was READ at the new location rather than offset: `!sc.Tiles) sc.Tiles =`. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 249:11 to 273:11, +24 — see the RE-KEYED 2026-08-31 file-level note above this file's entries for why the shift is uniform across all eighteen fold.ts keys. Verified by reading the tree directly (not by offset): the quoted expression above was located at the new line and the column re-derived from it, not carried over. Re-pointed 2026-08-31 (retraction-leaves Task 9) from 273:11 to 319:11: Task 9 inserted the actorRemoved arm into fold.ts's switch, 46 lines at line 240, and a Remove-actor button into view/dm.ts's Add-actor group, 15 lines at line 337 — both ABOVE this entry. Located by DIFFING the line BYTE FOR BYTE against HEAD's 273 (identical) and re-reading column 11 at the new location, never by trusting an offset: the two files moved by different amounts, and dm.ts's own 158:7 entry did not move at all. PRE-EXISTING STALENESS REPAIRED 2026-08-31 (retraction-leaves Task 9), and NOT caused by this task's +46: this body pointed at `fold.ts:111`, which was already a COMMENT line at HEAD (the Scene literal opens at :101 and its fields sit at :106/:107/:114). Repaired by NAMING the arm instead of a line, per this branch's own citation rule and the precedent already set on the Go side, so it cannot rot again.

# --- client/src/view/dm.ts --------------------------------------------------
#
# 166 survivors -> 1 by writing the messages, the payloads and the field
# contract (client/test/dm-view.test.ts). One further mutant is handled in the
# SOURCE with a `// Stryker disable` and a stated reason rather than here,
# because it is NOT equivalent: dropping el()'s `text !== undefined` guard puts
# the word "undefined" above every group in a real browser, while happy-dom
# coerces it to "" and no test can see the difference. Claiming equivalence for
# it would be the wrong-adjudication this file warns about.

client/src/view/dm.ts 158:7 ConditionalExpression true
    `if (cls) i.className = cls` -> always-assign, in the input() helper. Unlike el()'s version of the same line, this parameter has a default of "" rather than being optional, so the mutated branch assigns the empty string — which is what className already holds. No element can tell the two apart, and there is no call site that passes undefined. Re-pointed 2026-08-23 (actor-kind Task 2) from 71:7 to 93:7, +22. dm.ts gained actorKindFromWireName and its doc comment above every one of these lines. Line 93 was byte-compared against HEAD's 71 and is identical, and col 7 was READ at the new location rather than offset: `cls) i.className = cls`. Re-pointed 2026-08-24 (actor-kind Task 7) from 93:7 to 131:7, +38. dm.ts gained kindSelect() and its doc comment (33 lines), placed between actorKindFromWireName and input() and so entirely ABOVE this helper, plus 5 lines of correction to actorKindFromWireName's own doc after review. Line 131 was byte-compared against HEAD's 93 and is identical, and col 7 was READ at the new location rather than offset: `cls) i.className = cls`. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 159:7 to 158:7, -1. dm.ts's SHIFT IS NOT UNIFORM: the `../undo` import went (-1), the now-unread `log: Envelope[]` prop went (-1), and the whole Undo group went (-32). So a key above DMDeps moves 1 and a key below the Undo group moves 34. Line 158 was byte-compared against HEAD's 159 and is identical, and col 7 was READ at the new location rather than offset: `cls) i.className = cl`.

# --- client/src/player.ts ----------------------------------------------------
#
# 22 survivors -> the comparators below, by pinning ORDER (client/test/
# player.test.ts). The game rules here — Chebyshev range, affordability — were
# already covered; what was unpinned was that every returned list is SORTED,
# and a reshuffling panel moves the actor out from under a player's cursor.
#
# What remains is one shape, in three copies of the same comparator:
#
#     (a, b) => (a.id < b.id ? -1 : a.id > b.id ? 1 : 0)
#
# Every id sorted here is UNIQUE, and that one fact is what makes these
# unobservable. Stated precisely, because the loose version ("the ids are the
# map keys") is not quite true and hid where the risk lives: a.actorId is a
# FIELD, not the key. It is unique only because the fold stores each actor and
# token under a key equal to its own id — fold.ts's actorAdded and tokenPlaced arms — an invariant
# maintained in a DIFFERENT FILE from the one these adjudications describe.
#
# That premise is now machine-checked, not merely asserted: see "every actor
# and token is stored under a key equal to its own id" in
# client/test/fold-dump.test.ts. If the fold ever stores an actor under some
# other key, that test fails and these fifteen claims must be re-derived rather
# than carried forward. The same applies if ids ever stop coming from a map at
# all — a list of actors arriving as an array, say.

client/src/player.ts 26:22 EqualityOperator a.actorId <= b.actorId
    Comparator boundary that differs only when the two ids are equal. They are object keys of the same map, so no two elements can share one.

client/src/player.ts 26:51 ConditionalExpression true
    Forces the inner `a > b ? 1 : 0` to 1. That branch is reached only when `a < b` is false, which for unique keys means a > b — where 1 is already the answer.

client/src/player.ts 26:51 ConditionalExpression false
    Forces the inner `a > b ? 1 : 0` to 0, leaving `a < b ? -1 : 0`. For unique keys that is a complete strict-less oracle and the sort reaches the same order from the negative signal alone; verified identical to the full comparator at n = 3, 5, 10 and 20 on deliberately unsorted ids, rather than assumed.

client/src/player.ts 26:51 EqualityOperator a.actorId >= b.actorId
    Comparator boundary that differs only when the two ids are equal. They are object keys of the same map, so no two elements can share one.

client/src/player.ts 26:51 EqualityOperator a.actorId <= b.actorId
    Comparator boundary that differs only when the two ids are equal. They are object keys of the same map, so no two elements can share one.

client/src/player.ts 58:22 EqualityOperator a.ID <= b.ID
    Comparator boundary that differs only when the two ids are equal. They are object keys of the same map, so no two elements can share one.

client/src/player.ts 58:41 ConditionalExpression true
    Forces the inner `a > b ? 1 : 0` to 1. That branch is reached only when `a < b` is false, which for unique keys means a > b — where 1 is already the answer.

client/src/player.ts 58:41 ConditionalExpression false
    Forces the inner `a > b ? 1 : 0` to 0, leaving `a < b ? -1 : 0`. For unique keys that is a complete strict-less oracle and the sort reaches the same order from the negative signal alone; verified identical to the full comparator at n = 3, 5, 10 and 20 on deliberately unsorted ids, rather than assumed.

client/src/player.ts 58:41 EqualityOperator a.ID <= b.ID
    Comparator boundary that differs only when the two ids are equal. They are object keys of the same map, so no two elements can share one.

client/src/player.ts 58:41 EqualityOperator a.ID >= b.ID
    Comparator boundary that differs only when the two ids are equal. They are object keys of the same map, so no two elements can share one.

client/src/player.ts 75:5 EqualityOperator a.actorId <= b.actorId
    Comparator boundary that differs only when the two ids are equal. They are object keys of the same map, so no two elements can share one.

client/src/player.ts 75:34 ConditionalExpression true
    Forces the inner `a > b ? 1 : 0` to 1. That branch is reached only when `a < b` is false, which for unique keys means a > b — where 1 is already the answer.

client/src/player.ts 75:34 EqualityOperator a.actorId >= b.actorId
    Comparator boundary that differs only when the two ids are equal. They are object keys of the same map, so no two elements can share one.

client/src/player.ts 75:34 ConditionalExpression false
    Forces the inner `a > b ? 1 : 0` to 0, leaving `a < b ? -1 : 0`. For unique keys that is a complete strict-less oracle and the sort reaches the same order from the negative signal alone; verified identical to the full comparator at n = 3, 5, 10 and 20 on deliberately unsorted ids, rather than assumed.

client/src/player.ts 75:34 EqualityOperator a.actorId <= b.actorId
    Comparator boundary that differs only when the two ids are equal. They are object keys of the same map, so no two elements can share one.

# --- client/src/view/player.ts -----------------------------------------------
#
# NOT client/src/player.ts above -- a different file, the view module for the
# player panel. NO ENTRY HERE, deliberately, after one stood briefly and was
# WITHDRAWN.
#
# WITHDRAWN 2026-08-30 (Task 4 fix round 2). Fix round 1 filed
# `client/src/view/player.ts 95:59 StringLiteral "Stryker was here!"` against
# mayWorkAnyDoor's `Object.keys(st.Scenes).sort().at(-1) ?? ""`, arguing the
# fallback fed only a SCENE LOOKUP on an object with zero own keys, so no
# string could make the lookup succeed -- verified, at the time, by
# hand-mutating and running the whole suite green. The coordinator asked for
# the claim to be FALSIFIED rather than trusted, naming Task 3's own
# near-miss on doors.ts's identically-shaped copy as the reason to be
# careful. It falsified: the argument missed the PROTOTYPE CHAIN.
# `st.Scenes[key]` for a key outside `Object.keys(st.Scenes)` still resolves
# through whatever `st.Scenes` inherits from, and "zero own keys" says
# nothing about what is inherited. A state with Scenes carrying no own keys
# but a PROTOTYPE that answers "" with a real scene (Object.setPrototypeOf,
# never reachable through fold.ts's own Object.create(null) Scenes, so a
# hand-built-past-the-fold state in the same category doors.test.ts already
# uses for this module family) makes the real fallback ("") find that scene
# while the mutant ("Stryker was here!") finds nothing -- a genuine,
# reproduced divergence. The mutant is now KILLED, not excused: see
# player.test.ts's "a prototype-injected empty-string scene defeats the `??
# ""` equivalence claim". player.ts's own doc comment on mayWorkAnyDoor
# carries the corrected reasoning inline.
#
# WHAT THIS MEANS FOR read_equivalents/this table's own promise: an
# adjudication that stood for one fix round, was independently challenged,
# and was found wrong -- exactly the risk this file's header names two of
# four Go-side entries for. The difference this time is it was caught before
# landing on branch HEAD as a false all-clear rather than after.

# --- client/src/view/spectator.ts --------------------------------------------
#
# 63 survivors -> 0 by pinning the board's GEOMETRY (the click-to-cell mapping
# and the board's size), describe()'s crash-safety on malformed events, and the
# ordering of notes and scenes. One mutant is handled in the SOURCE with a
# `// Stryker disable` and a stated reason — el()'s `text !== undefined` guard,
# the same harness-blind case as view/dm.ts.
#
# NO ENTRY FOR renderSpectator's `?? ""` SCENE PICK, after one was
# WITHDRAWN 2026-08-30 (Task 4 fix round 3). It argued the same shape fix
# round 2 already disproved for view/player.ts's mayWorkAnyDoor: "whatever
# string the fallback yields is looked up in st.Scenes and missed either
# way" — true only of st.Scenes's OWN keys, and silent about its PROTOTYPE.
# Fix round 2's own falsification of the player.ts twin should have been
# reason enough to re-examine this one at the time; it was instead re-keyed
# and re-confirmed twice in that same round without re-reading what it
# claimed. The reviewer built the distinguishing state and ran it: a
# prototype answering "" with a real scene renders that scene's heading
# under the real fallback and "No scene yet." under the mutant's — a real,
# reproduced divergence, not an equivalence. Killed instead:
# spectator-view.test.ts's "a prototype-injected empty-string scene defeats
# the renderSpectator `?? ""` equivalence claim".

client/src/view/spectator.ts 389:10 EqualityOperator label(a, a.actorId) <= label(b, b.actorId)
    The name-comparison arm, and it is the ELSE of `label(a, a.actorId) === label(b, b.actorId)` on the line above — so it is evaluated only when the two labels are NOT equal, which is precisely the case where string `<` and `<=` agree. label() is a pure function of its two arguments (`id === "" ? "nobody" : a?.name || id`) and both calls here pass the same arguments the guard compared, so no re-evaluation can smuggle the equal case back into the branch that excludes it. There is therefore no input that reaches this operand with equal operands, and nothing downstream to distinguish: order, class, text and the id each button sends are identical. Same exhaustive check as the entry above — 150 permutations, zero differing renders. Re-pointed 2026-08-30 (Task 4 fix round 1) from 365:10 to 389:10, +24 (see the 388:10 entry directly below for the shift's cause — every added line sits inside renderGrid, entirely above renderPerch). Line 389:10 was byte-compared against HEAD's 365 and is identical, and col 10 was READ at the new location rather than offset: `label(a, a.actorId) < `. CONFIRMED 2026-08-30 (Task 4 fix round 2): the coordinator's rekey map for this round named a DIFFERENT coordinate for this entry, derived from reports/mutation/mutation.json — but that report was generated from a snapshot taken the moment fix round 1's container run started, BEFORE the rest of fix round 1's edits to this file (which is what put this expression at its current line in the first place). Proven, not assumed: none of fix round 1's new test names appear anywhere in the 282MB report, byte-searched whole. The coordinate above was independently re-verified by reading the current source directly and is unchanged from fix round 1's own re-key.

client/src/view/spectator.ts 388:10 EqualityOperator a.actorId <= b.actorId
    The tie-break arm of renderPerch's comparator, reached only when two party members' LABELS are equal, where `<` and `<=` then differ only if their actorIds are equal too. No two entries of `Object.values(st.Actors)` can share an actorId: fold.ts keys that map BY actorId and throws `FoldError("duplicate actor \"...\"")` on a second one, so the input the mutation needs cannot be folded from any log — the same shape already adjudicated on client/src/player.ts 75:5 and client/src/view/grid.ts 139:32. It stays equivalent even for a state hand-built past the fold, which is the stronger claim and the one that matters: equal actorId AND equal label makes the two buttons identical in every rendered fact — same textContent (the shared label), same className (`a.actorId === perch.current` reads the shared id), same id handed to onPerch — so no ordering of that pair is observable, and the comparator's return value is read nowhere else (it sorts a local array from Object.values, never st.Actors itself). Checked rather than reasoned: all 150 permutations of three fixtures built around duplicate-id/duplicate-label pairs render a byte-identical button sequence under both operators. Re-pointed 2026-08-30 (Task 4 fix round 1) from 364:10 to 388:10, +24: this task's onCell/board-armed work added a required `doorsArmed` parameter and its comment to renderGrid (+12), the armed-label block (+7) and the canvas-container class line (+5) — all THREE inside renderGrid, which sits entirely above renderPerch in this file, so the whole +24 lands above this line and renderPerch's own body is untouched. `function renderPerch(...)` itself moved from HEAD's line 352 to 376, the same +24. Line 388:10 was byte-compared against HEAD's 364 and is identical, and col 10 was READ at the new location rather than offset: `a.actorId < b.actorId `. CONFIRMED 2026-08-30 (Task 4 fix round 2): the coordinator's rekey map for this round named a DIFFERENT coordinate for this entry, derived from reports/mutation/mutation.json — but that report was generated from a snapshot taken the moment fix round 1's container run started, BEFORE the rest of fix round 1's edits to this file (which is what put this expression at its current line in the first place). Proven, not assumed: none of fix round 1's new test names appear anywhere in the 282MB report, byte-searched whole. The coordinate above was independently re-verified by reading the current source directly and is unchanged from fix round 1's own re-key.

# --- client/src/auth.ts -----------------------------------------------------

client/src/auth.ts 37:12 ConditionalExpression false
    `return t === null || t === "" ? null : t;` — the LEFT OPERAND only (cols 12-22 is `t === null`, not the whole condition; the whole-condition mutants at 12-34 are all killed by the existing tests). Forcing it false leaves `false || t === ""`, and every input lands on the same value: t===null returns t, which IS null, the same thing the then-branch yields; t==="" still matches the right operand and returns null; anything else returns t under both. get()'s only observable is its return value — no side effects, no store writes, nothing reaches the DOM — so there is no reading under which the two differ. Hand-applied the mutation and ran the full client suite to check the claim rather than only reasoning it: 333 pass, as predicted.

# --- client/src/view/grid.ts ------------------------------------------------
#
# Two mutually-redundant fallbacks and five sort-comparator mutants. The
# comparator group is the same shape already adjudicated on client/src/player.ts:
# a three-way comparator over values that are MAP KEYS, so the equal case it
# guards cannot occur.

client/src/view/grid.ts 129:45 StringLiteral ""
    `[...(name || tok.ActorID || "?")][0] ?? "?"` — the FIRST "?" only. It and the `??` fallback beside it are redundant with each other, so neither is observable alone. Blanking this one makes the spread source "" exactly when name and ActorID are both empty; the spread of "" is [], [0] is undefined, and the `?? "?"` then yields the same "?" the original produced directly. Every other input never reaches this operand at all. Identical string out, and initial is the only thing this expression feeds.

client/src/view/grid.ts 129:57 StringLiteral ""
    The `?? "?"` half of the same pair, and unreachable while the operand above is intact: `name || tok.ActorID || "?"` cannot evaluate to an empty string, because "?" is truthy and terminates the chain. So `[...s][0]` is always defined and the `??` branch never runs. It cannot be deleted despite that — `[...s][0]` is typed `string | undefined`, so the fallback is load-bearing for the TYPE even where it is unreachable at run time.

client/src/view/grid.ts 139:32 EqualityOperator a.tokenId <= b.tokenId
    `a.tokenId < b.tokenId ? -1 : ...` -> `<=`. The two differ only when the ids are EQUAL, and they cannot be: disc.tokenId comes from tok.ID for tokens read out of st.Tokens, which is keyed by that id, so two discs in one call always carry distinct ids.

client/src/view/grid.ts 139:61 ConditionalExpression true
    The second arm, `a.tokenId > b.tokenId ? 1 : 0`, forced to 1. It is only reached when the first arm was false, i.e. a >= b, and with equal ids impossible that means a > b — where the original also yields 1. No input reaches it with a different answer.

client/src/view/grid.ts 139:61 ConditionalExpression false
    The same arm forced to 0, so a > b returns 0 ("equal") instead of 1 ("after"). Not observable through Array.prototype.sort: both engines' sorts branch on `comparator(...) < 0`, never on the difference between 0 and a positive result, so "not less" is the only fact either value conveys. Checked rather than assumed — a 20-element reversed board, enough to leave insertion sort and enter the merge paths, still sorts correctly under this mutant. Note this holds because of how sort CONSULTS the comparator; a caller that used the comparator directly for a three-way answer would distinguish them, and none exists.

client/src/view/grid.ts 139:61 EqualityOperator a.tokenId >= b.tokenId
    Same arm, `>` -> `>=`: differs only on equal ids, which map keys make impossible.

client/src/view/grid.ts 139:61 EqualityOperator a.tokenId <= b.tokenId
    Same arm, `>` -> `<=`. Reached only when a >= b; with equality impossible, `a <= b` is false there and the arm yields 0 where the original yields 1 — the 0-versus-positive case shown above to be invisible to sort.

# --- client/src/wire.ts -----------------------------------------------------

client/src/wire.ts 476:13 EqualityOperator env.sequence >= this.lastSeq
    `if (env.sequence > this.lastSeq) this.lastSeq = env.sequence` -> `>=`. The two differ on exactly one input — an envelope whose sequence EQUALS the mark — and on that input the mutant performs the assignment the original skips, writing the value the field already holds. bigint is a primitive compared by value, so there is no identity to see the write: `Object.is(4n, 4n)` is true. CORRECTED 2026-08-25: this used to say "lastSeq's only consumer is reconnect's resume cursor", and BOTH halves were false. reconnect() never reads lastSeq — it WRITES it (wire.ts 355-356), deriving resume from seenSeq, as this file's own header says in capitals. And lastSeq is not private to the wire: `get head()` (wire.ts:130) exposes it, Session.head forwards it (session.ts:73-74), and a dozen tests assert on it directly. The conclusion survives the correction — head returns the same bigint either way — but the old reason sent an auditor looking for a consumer that does not exist, past the public getter that does. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 475:13 to 476:13, +1. wire.ts's SHIFT IS UNIFORM: +1 for every key in it. The perch-sequence bullet in Wire.reconnect's note lost its "so that no undo can ever name one" rationale and gained the real one — a perch has no causing event — over one more line. Line 476 was byte-compared against HEAD's 475 and is identical, and col 13 was READ at the new location rather than offset: `env.sequence > this.l`.

client/src/wire.ts 500:7 ConditionalExpression default:
    Empties the `default:` arm of handleFrame's switch, whose body is a bare `return;`. The switch is the last statement in the function and nothing follows it, so falling out of an empty arm reaches the same end of the same void function. Deliberately NOT deleted despite that: the arm is where the ignore-unknown-frames decision is written down, and it is pinned by test ("an unknown frame kind is ignored and the stream keeps working") through the behaviour rather than through this mutant. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 499:7 to 500:7, +1. wire.ts's SHIFT IS UNIFORM: +1 for every key in it. The perch-sequence bullet in Wire.reconnect's note lost its "so that no undo can ever name one" rationale and gained the real one — a perch has no causing event — over one more line. Line 500 was byte-compared against HEAD's 499 and is identical, and col 7 was READ at the new location rather than offset: `default:`.

client/src/wire.ts 475:13 EqualityOperator env.sequence >= this.seenSeq
    `if (env.sequence > this.seenSeq) this.seenSeq = env.sequence;` -> `>=` in handleFrame's event arm. The two branches differ on exactly one input — an envelope whose sequence EQUALS the high-water mark, which the projection makes ordinary rather than exotic (one log event reaches a seat as several envelopes sharing one sequence). On that input the mutant performs the assignment the original skips, and the assignment writes the value the field already holds. Both are bigint, a primitive compared by value, so there is no identity to distinguish either: `Object.is(4n, 4n)` is true, and the field is a plain private with no getter, setter or Proxy on it. seenSeq is read in exactly two places — `reconnect()`'s `this.seenSeq > 0n ? this.seenSeq - 1n : 0n`, and this comparison itself — and neither can tell a re-written 4n from an untouched one. MEASURED 2026-08-24: mutant hand-applied, the WHOLE client suite run (639 tests, 26 files), 0 failures. THE SHAPE, stated here rather than by pointing at a neighbour: an equality mutant whose only extra effect is an assignment writing the value the field already holds, indistinguishable because bigint is compared by value. This sentence used to cite the two undo.ts entries as the exemplar of that shape; they were deleted on 2026-08-31 with undo.ts itself, and a cross-reference to a deleted entry is the failure no gate in this repo watches for — check-ts-mutation.py validates adjudication KEYS, never the prose between them. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 474:13 to 475:13, +1. wire.ts's SHIFT IS UNIFORM: +1 for every key in it. The perch-sequence bullet in Wire.reconnect's note lost its "so that no undo can ever name one" rationale and gained the real one — a perch has no causing event — over one more line. Line 475 was byte-compared against HEAD's 474 and is identical, and col 13 was READ at the new location rather than offset: `env.sequence > this.s`.

# --- client/src/view/feed.ts ------------------------------------------------
#
# Three groups. (1) guards whose bypassed branch is neutralised downstream by
# an undefined comparison or an empty result; (2) a filter that cannot observe
# an empty entry because no code path creates one; (3) the same sort-comparator
# shape adjudicated on player.ts and grid.ts.

client/src/view/feed.ts 43:28 ConditionalExpression true
    `for (const e of log) if (isMechanical(e)) groupOf.set(...)` forced true, adding narration sequences to groupOf as self-mappings. Nothing ever reads them: the two lookups are `groupOf.get(e.sequence)` guarded by isMechanical, and `groupOf.get(target.sequence)` where target comes from a find already filtered to mechanical events. Extra unread map entries, no rendered difference. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 54:29 to 43:28, -11. feed.ts's SHIFT IS UNIFORM: -11 for every key in it, and 107 -> 96 lines is the whole of it. The whole -11 comes from one hunk, and the header contributes NOTHING: its two-rule list became one rule plus a note, nine lines for nine (0). Inside buildFeed, the nine-line retracted-set pass with its trailing blank and the one-line `const live = log.filter(...)` went with nothing replacing them (-10), and isMechanical went from five lines to four, losing its eventsRetracted arm and gaining a shorter comment (-1). Every other changed line in this file is a `live` -> `log` rename, one line for one. Line 43 is NOT byte-identical to HEAD's 54 — `live` became `log` when the retracted-set filter went, one character shorter — so the COLUMN moved too and was READ at the new location rather than offset, from 29 to 28: `isMechanical(e)) grou`.

client/src/view/feed.ts 48:34 ConditionalExpression false
    The `n.anchorToSeq <= 0n` half of the half-specified-anchor guard, forced false. It changes only the from>0/to<=0 case, and that case falls through to a range filter of `c.sequence >= from && c.sequence <= 0n`, which no live sequence satisfies. inRange is empty and the next guard skips the narration anyway — the same outcome the guard produced directly. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 59:34 to 48:34, -11. feed.ts's SHIFT IS UNIFORM: -11 for every key in it, and 107 -> 96 lines is the whole of it. The whole -11 comes from one hunk, and the header contributes NOTHING: its two-rule list became one rule plus a note, nine lines for nine (0). Inside buildFeed, the nine-line retracted-set pass with its trailing blank and the one-line `const live = log.filter(...)` went with nothing replacing them (-10), and isMechanical went from five lines to four, losing its eventsRetracted arm and gaining a shorter comment (-1). Every other changed line in this file is a `live` -> `log` rename, one line for one. Line 48 was byte-compared against HEAD's 59 and is identical, and col 34 was READ at the new location rather than offset: `n.anchorToSeq <= 0n) `.

client/src/view/feed.ts 48:34 EqualityOperator n.anchorToSeq < 0n
    Same operand, `<=` -> `<`: differs only at anchorToSeq === 0, which is exactly the case shown above to reach an empty range and be skipped one line later. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 59:34 to 48:34, -11. feed.ts's SHIFT IS UNIFORM: -11 for every key in it, and 107 -> 96 lines is the whole of it. The whole -11 comes from one hunk, and the header contributes NOTHING: its two-rule list became one rule plus a note, nine lines for nine (0). Inside buildFeed, the nine-line retracted-set pass with its trailing blank and the one-line `const live = log.filter(...)` went with nothing replacing them (-10), and isMechanical went from five lines to four, losing its eventsRetracted arm and gaining a shorter comment (-1). Every other changed line in this file is a `live` -> `log` rename, one line for one. Line 48 was byte-compared against HEAD's 59 and is identical, and col 34 was READ at the new location rather than offset: `n.anchorToSeq <= 0n) `.

client/src/view/feed.ts 52:9 ConditionalExpression false
    `if (inRange.length === 0) continue` forced never to skip. The body it then runs is `const head = inRange[0]!` followed by `for (const seq of inRange) ...` — over an empty array, so head is undefined and the loop body never executes. Nothing is written to groupOf. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 63:9 to 52:9, -11. feed.ts's SHIFT IS UNIFORM: -11 for every key in it, and 107 -> 96 lines is the whole of it. The whole -11 comes from one hunk, and the header contributes NOTHING: its two-rule list became one rule plus a note, nine lines for nine (0). Inside buildFeed, the nine-line retracted-set pass with its trailing blank and the one-line `const live = log.filter(...)` went with nothing replacing them (-10), and isMechanical went from five lines to four, losing its eventsRetracted arm and gaining a shorter comment (-1). Every other changed line in this file is a `live` -> `log` rename, one line for one. Line 52 was byte-compared against HEAD's 63 and is identical, and col 9 was READ at the new location rather than offset: `inRange.length === 0)`.

client/src/view/feed.ts 80:46 ConditionalExpression true
    The `n.anchorToSeq > 0n` half of `anchored`, forced true. For a half-specified anchor this makes anchored true, and the find it enables searches `c.sequence >= from && c.sequence <= 0n`, which matches nothing; target is undefined and the narration lands at its own sequence — where the unanchored path would have put it. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 91:46 to 80:46, -11. feed.ts's SHIFT IS UNIFORM: -11 for every key in it, and 107 -> 96 lines is the whole of it. The whole -11 comes from one hunk, and the header contributes NOTHING: its two-rule list became one rule plus a note, nine lines for nine (0). Inside buildFeed, the nine-line retracted-set pass with its trailing blank and the one-line `const live = log.filter(...)` went with nothing replacing them (-10), and isMechanical went from five lines to four, losing its eventsRetracted arm and gaining a shorter comment (-1). Every other changed line in this file is a `live` -> `log` rename, one line for one. Line 80 was byte-compared against HEAD's 91 and is identical, and col 46 was READ at the new location rather than offset: `n.anchorToSeq > 0n;`.

client/src/view/feed.ts 80:46 EqualityOperator n.anchorToSeq >= 0n
    Same operand, `>` -> `>=`: differs only at anchorToSeq === 0, the case above. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 91:46 to 80:46, -11. feed.ts's SHIFT IS UNIFORM: -11 for every key in it, and 107 -> 96 lines is the whole of it. The whole -11 comes from one hunk, and the header contributes NOTHING: its two-rule list became one rule plus a note, nine lines for nine (0). Inside buildFeed, the nine-line retracted-set pass with its trailing blank and the one-line `const live = log.filter(...)` went with nothing replacing them (-10), and isMechanical went from five lines to four, losing its eventsRetracted arm and gaining a shorter comment (-1). Every other changed line in this file is a `live` -> `log` rename, one line for one. Line 80 was byte-compared against HEAD's 91 and is identical, and col 46 was READ at the new location rather than offset: `n.anchorToSeq > 0n;`.

client/src/view/feed.ts 93:10 MethodExpression [...bySeq.values()]
    Removes `.filter(e => e.events.length > 0 || e.narrations.length > 0)`. No entry can fail that predicate: entryAt is called in exactly two places and each pushes onto the entry it just obtained, so an entry exists only because something was about to be added to it. Kept rather than deleted because it states the invariant at the point a future caller would break it, but it has nothing to filter today. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 104:10 to 93:10, -11. feed.ts's SHIFT IS UNIFORM: -11 for every key in it, and 107 -> 96 lines is the whole of it. The whole -11 comes from one hunk, and the header contributes NOTHING: its two-rule list became one rule plus a note, nine lines for nine (0). Inside buildFeed, the nine-line retracted-set pass with its trailing blank and the one-line `const live = log.filter(...)` went with nothing replacing them (-10), and isMechanical went from five lines to four, losing its eventsRetracted arm and gaining a shorter comment (-1). Every other changed line in this file is a `live` -> `log` rename, one line for one. Line 93 was byte-compared against HEAD's 104 and is identical, and col 10 was READ at the new location rather than offset: `[...bySeq.values()]`.

client/src/view/feed.ts 94:20 ConditionalExpression true
    The same filter's predicate forced true. Every entry already satisfies it, per the entry above. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 105:20 to 94:20, -11. feed.ts's SHIFT IS UNIFORM: -11 for every key in it, and 107 -> 96 lines is the whole of it. The whole -11 comes from one hunk, and the header contributes NOTHING: its two-rule list became one rule plus a note, nine lines for nine (0). Inside buildFeed, the nine-line retracted-set pass with its trailing blank and the one-line `const live = log.filter(...)` went with nothing replacing them (-10), and isMechanical went from five lines to four, losing its eventsRetracted arm and gaining a shorter comment (-1). Every other changed line in this file is a `live` -> `log` rename, one line for one. Line 94 was byte-compared against HEAD's 105 and is identical, and col 20 was READ at the new location rather than offset: `e.events.length > 0 |`.

client/src/view/feed.ts 94:20 EqualityOperator e.events.length >= 0
    `> 0` -> `>= 0` in that predicate: true for every non-negative length, and the predicate is already true for every entry that exists. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 105:20 to 94:20, -11. feed.ts's SHIFT IS UNIFORM: -11 for every key in it, and 107 -> 96 lines is the whole of it. The whole -11 comes from one hunk, and the header contributes NOTHING: its two-rule list became one rule plus a note, nine lines for nine (0). Inside buildFeed, the nine-line retracted-set pass with its trailing blank and the one-line `const live = log.filter(...)` went with nothing replacing them (-10), and isMechanical went from five lines to four, losing its eventsRetracted arm and gaining a shorter comment (-1). Every other changed line in this file is a `live` -> `log` rename, one line for one. Line 94 was byte-compared against HEAD's 105 and is identical, and col 20 was READ at the new location rather than offset: `e.events.length > 0 |`.

client/src/view/feed.ts 94:43 EqualityOperator e.narrations.length >= 0
    The narrations half of the same predicate, same reasoning. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 105:43 to 94:43, -11. feed.ts's SHIFT IS UNIFORM: -11 for every key in it, and 107 -> 96 lines is the whole of it. The whole -11 comes from one hunk, and the header contributes NOTHING: its two-rule list became one rule plus a note, nine lines for nine (0). Inside buildFeed, the nine-line retracted-set pass with its trailing blank and the one-line `const live = log.filter(...)` went with nothing replacing them (-10), and isMechanical went from five lines to four, losing its eventsRetracted arm and gaining a shorter comment (-1). Every other changed line in this file is a `live` -> `log` rename, one line for one. Line 94 was byte-compared against HEAD's 105 and is identical, and col 43 was READ at the new location rather than offset: `e.narrations.length >`.

client/src/view/feed.ts 95:22 EqualityOperator a.seq <= b.seq
    `a.seq < b.seq ? -1 : ...` -> `<=`. Differs only on equal seqs, and seq is a Map key: bySeq is keyed by it, so one call never yields two entries sharing one. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 106:22 to 95:22, -11. feed.ts's SHIFT IS UNIFORM: -11 for every key in it, and 107 -> 96 lines is the whole of it. The whole -11 comes from one hunk, and the header contributes NOTHING: its two-rule list became one rule plus a note, nine lines for nine (0). Inside buildFeed, the nine-line retracted-set pass with its trailing blank and the one-line `const live = log.filter(...)` went with nothing replacing them (-10), and isMechanical went from five lines to four, losing its eventsRetracted arm and gaining a shorter comment (-1). Every other changed line in this file is a `live` -> `log` rename, one line for one. Line 95 was byte-compared against HEAD's 106 and is identical, and col 22 was READ at the new location rather than offset: `a.seq < b.seq ? -1 : `.

client/src/view/feed.ts 95:43 ConditionalExpression true
    Second arm `a.seq > b.seq ? 1 : 0` forced to 1; reached only when a >= b, and equality being impossible leaves a > b, where the original also yields 1. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 106:43 to 95:43, -11. feed.ts's SHIFT IS UNIFORM: -11 for every key in it, and 107 -> 96 lines is the whole of it. The whole -11 comes from one hunk, and the header contributes NOTHING: its two-rule list became one rule plus a note, nine lines for nine (0). Inside buildFeed, the nine-line retracted-set pass with its trailing blank and the one-line `const live = log.filter(...)` went with nothing replacing them (-10), and isMechanical went from five lines to four, losing its eventsRetracted arm and gaining a shorter comment (-1). Every other changed line in this file is a `live` -> `log` rename, one line for one. Line 95 was byte-compared against HEAD's 106 and is identical, and col 43 was READ at the new location rather than offset: `a.seq > b.seq ? 1 : 0`.

client/src/view/feed.ts 95:43 ConditionalExpression false
    Same arm forced to 0, so a > b reports "equal" rather than "after". Array.prototype.sort branches on `comparator(...) < 0` and never distinguishes 0 from a positive value, so the ordering is unchanged — the same reasoning, and the same 20-element check, recorded for view/grid.ts:139:61. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 106:43 to 95:43, -11. feed.ts's SHIFT IS UNIFORM: -11 for every key in it, and 107 -> 96 lines is the whole of it. The whole -11 comes from one hunk, and the header contributes NOTHING: its two-rule list became one rule plus a note, nine lines for nine (0). Inside buildFeed, the nine-line retracted-set pass with its trailing blank and the one-line `const live = log.filter(...)` went with nothing replacing them (-10), and isMechanical went from five lines to four, losing its eventsRetracted arm and gaining a shorter comment (-1). Every other changed line in this file is a `live` -> `log` rename, one line for one. Line 95 was byte-compared against HEAD's 106 and is identical, and col 43 was READ at the new location rather than offset: `a.seq > b.seq ? 1 : 0`.

client/src/view/feed.ts 95:43 EqualityOperator a.seq >= b.seq
    Same arm, `>` -> `>=`: differs only on equal seqs, impossible for map keys. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 106:43 to 95:43, -11. feed.ts's SHIFT IS UNIFORM: -11 for every key in it, and 107 -> 96 lines is the whole of it. The whole -11 comes from one hunk, and the header contributes NOTHING: its two-rule list became one rule plus a note, nine lines for nine (0). Inside buildFeed, the nine-line retracted-set pass with its trailing blank and the one-line `const live = log.filter(...)` went with nothing replacing them (-10), and isMechanical went from five lines to four, losing its eventsRetracted arm and gaining a shorter comment (-1). Every other changed line in this file is a `live` -> `log` rename, one line for one. Line 95 was byte-compared against HEAD's 106 and is identical, and col 43 was READ at the new location rather than offset: `a.seq > b.seq ? 1 : 0`.

client/src/view/feed.ts 95:43 EqualityOperator a.seq <= b.seq
    Same arm, `>` -> `<=`: false wherever this arm is reached, yielding 0 where the original yields 1 — the 0-versus-positive case sort cannot see. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 106:43 to 95:43, -11. feed.ts's SHIFT IS UNIFORM: -11 for every key in it, and 107 -> 96 lines is the whole of it. The whole -11 comes from one hunk, and the header contributes NOTHING: its two-rule list became one rule plus a note, nine lines for nine (0). Inside buildFeed, the nine-line retracted-set pass with its trailing blank and the one-line `const live = log.filter(...)` went with nothing replacing them (-10), and isMechanical went from five lines to four, losing its eventsRetracted arm and gaining a shorter comment (-1). Every other changed line in this file is a `live` -> `log` rename, one line for one. Line 95 was byte-compared against HEAD's 106 and is identical, and col 43 was READ at the new location rather than offset: `a.seq > b.seq ? 1 : 0`.

# --- client/src/app.ts ------------------------------------------------------
#
# TWO entries, both history.replaceState's title argument, which the runtime this
# gate measures discards outright (happy-dom's signature is literally
# replaceState(state, _unused, url)). There are TWO such calls in this file —
# the invite path and the join path — and each carries its own entry rather
# than one stretched to cover both.
#
# This preamble said "ONE" and "only one of them surfaces a surviving mutant"
# until 2026-08-10, while the section held two and the second entry said so in
# words. The same miscount this file already apologises for higher up, in the
# opposite direction. A preamble that disagrees with its own contents is the
# failure this file exists to prevent, in miniature.
#
# This preamble said "two initial values" until 2026-08-03 and delivered one.
# The second was app.ts 52:16, withdrawn by the review that found it wrong —
# it claimed no test could observe the DOM between boot's two paints, and the
# fetch stub boot() calls between them can. The entry went; the count did not.
# Corrected here because a preamble promising an adjudication that does not
# exist is the same failure this file's header warns about, in miniature.
#
# The module-level DOM bootstrap that used to sit at the foot of app.ts is NOT
# here: it was untestable because of the HARNESS rather than because its
# mutants were unobservable, so claiming equivalence would have been a lie.
# It moved to client/src/main.ts, which is excluded from `mutate`.

# KILLED 2026-08-25, entries removed rather than re-keyed. app.ts 67:32 and
# 130:32 (the ignored TITLE argument of history.replaceState, on the invite and
# join paths) were adjudicated equivalent because the value reaches nothing a
# user or a test can read -- browsers ignore it, and happy-dom does not move
# document.title. That was true of the DOM and false of the SPY: app.test.ts
# already replaces history.replaceState to assert the token is stripped, so
# recording its middle argument kills both. A test beats an excuse, and an
# adjudication that a test can retire should be retired.
client/src/session.ts 76:12 EqualityOperator a.participantId <= b.participantId
    Tie-break on participant id, reached only when two participants share a display name. `<` -> `<=` differs ONLY when the two ids are equal, and they cannot be: participantId is the KEY of the `present` map (session.ts, `this.present.set(p.participantId, ...)`), so two distinct entries never carry the same one, and Array.prototype.sort never compares an element with itself. The premise is stronger than the equivalent adjudications on player.ts, which depend on uniqueness established in another file: here the key and the field are assigned together, four lines apart, in this file. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 68:12 to 76:12, +8. session.ts's SHIFT IS UNIFORM: +8 for both keys in it, and ONE edit accounts for all of it. The header's "why the whole stream is re-folded" paragraph was rewritten off retraction and onto the mutate-then-throw reason, 15 lines becoming 23 (+8), and it sits above the participants getter these two keys are in. The file grew by 9, not 8: rollback's doc comment also gained a line, but it is near line 194 — BELOW both keys — so it moves nothing here and is named only so the missing +1 is not mistaken for an error. Line 76 was byte-compared against HEAD's 68 and is identical, and col 12 was READ at the new location rather than offset: `a.participantId < b.p`.
client/src/session.ts 77:12 EqualityOperator a.displayName <= b.displayName
    Display-name comparison, reached ONLY on the else branch of `a.displayName === b.displayName` — so by construction the two names differ, and `<` and `<=` agree on every input that reaches it. Not a claim about names being unique (they are not, which is why the tie-break above exists); a claim about which branch this expression lives on. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 69:12 to 77:12, +8. session.ts's SHIFT IS UNIFORM: +8 for both keys in it, and ONE edit accounts for all of it. The header's "why the whole stream is re-folded" paragraph was rewritten off retraction and onto the mutate-then-throw reason, 15 lines becoming 23 (+8), and it sits above the participants getter these two keys are in. The file grew by 9, not 8: rollback's doc comment also gained a line, but it is near line 194 — BELOW both keys — so it moves nothing here and is named only so the missing +1 is not mistaken for an error. Line 77 was byte-compared against HEAD's 69 and is identical, and col 12 was READ at the new location rather than offset: `a.displayName < b.dis`.

client/src/view/dm.ts 349:46 StringLiteral ""
    The DRAFT KEY the scene-fill <select> remembers its answer under, at fillSelect's one call site. The argument's only two uses are inside fillSelect and they are a MATCHED PAIR: `o.selected = value === draft[field]` reads it and the change listener `draft[field] = sel.value` writes it, so renaming the key renames both halves at once and every observable this console has is unchanged — which option ends up selected, what the select reports, and the tiles that reach the wire. `draft` is a module-private const in dm.ts, which exports only DMDeps and renderDMConsole, so no test can read the key itself. FALSIFICATION ATTEMPTED, NOT ASSUMED. The one thing that would distinguish the two is a COLLISION — some other control reading or writing `draft[""]` — so every draft key in the file was enumerated on 2026-09-01, and the enumeration is corrected here after review caught it short: FIFTEEN `input(...)` calls (323, 342-345, 401-402, 460-464, 539-541), not nine; TWO `kindSelect(...)` calls; this `fillSelect(...)` call; the grant row's target <select>, which reads and writes `draft[field]` inline at 621/623; and the paste textarea, which writes `draft["actor-json"]` DIRECTLY at 440/442 and goes through no helper at all — the one the first enumeration missed entirely. Seven `clearDraft(...)` calls only delete. Every one of those keys is either a non-empty string literal or a template with a fixed prefix (`grant-${a.actorId}`, `grant-kind-${a.actorId}`), so none can ever be "". Confirmed empirically too: the mutant was hand-applied and the whole 708-test client suite stayed green. THIS ENTRY IS RETIRED THE DAY EITHER OF TWO THINGS LANDS, and both are plausible. A `clearDraft("scene-fill")` in the Create handler would name the key out loud and make it observable, and THAT omission is the whole reason this key is invisible. Stated exactly, because a first draft of this entry said "create_scene alone does not" and that was false: of the ten form-backed commands in this console, SEVEN clear their own group (Start session, Add actor, Remove actor, Add from JSON, Place token, Remove token, Grant) and THREE do not — Create scene, and the Notes group's Save and Delete. So the question is a real inconsistency across three controls rather than a lone omission. Or a `dataset["field"]` on the select, the way `input()` sets one, which would put the key in the DOM where this file's own field contract ("every input the console owns is reachable by its stable data-field") could read it — the two <select>s are that contract's only exceptions. Neither was done here: this is Task 11 of plan 2026-08-31-retraction-leaves, whose scope is the citation convention and the gate run, and changing console behaviour to kill a mutant is a product decision that belongs to whoever owns it. First adjudicated 2026-09-01; the line arrived with Task 10's fill selector, which is why no earlier run saw it. Column 46 was READ at the line rather than derived from an offset: the SECOND `"scene-fill"`, the field argument.

client/src/view/dm.ts 576:62 EqualityOperator x.actorId <= y.actorId
    Ordering the control panel by actorId. `<` -> `<=` differs ONLY when the two ids are equal, and they cannot be: actorId is the key of State.Actors (a Record), so two entries never carry the same one, and Array.prototype.sort never compares an element with itself. Same premise as the session.ts participant comparator, and for the same structural reason — the key and the field are the same value. Re-pointed 2026-08-23 (actor-kind Task 2) from 342:62 to 364:62, +22. dm.ts gained actorKindFromWireName and its doc comment above every one of these lines. Line 364 was byte-compared against HEAD's 342 and is identical, and col 62 was READ at the new location rather than offset: `x.actorId < y.actorId `. Re-pointed 2026-08-24 (actor-kind Task 4) from 364:62 to 373:62, +9. The Add actor form lost its "controller participant id (optional)" input and gained the comment saying why, all ABOVE this line and with no executable line of the control panel changed. Line 373 was byte-compared against HEAD's 364 and is identical, and col 62 was READ at the new location rather than offset: `x.actorId < y.a`. Re-pointed 2026-08-24 (actor-kind Task 7) from 373:62 to 425:62, +52. Three edits, all above this line and summing to the offset: kindSelect() with its doc comment (+33), the Add actor form's kind selector plus its guard (+14), and a review correction to actorKindFromWireName's doc (+5). The grant row's own selector shrank to a kindSelect call, but that is BELOW this line and so does not move it. Line 425 was byte-compared against HEAD's 373 and is identical, and col 62 was READ at the new location rather than offset: `x.actorId < y.a`. Re-pointed 2026-08-30 (Task 4 fix round 1) from 453:62 to 506:62, +53 — and split into two causes, because the ENTRY was already wrong at Task 4's own HEAD, not only moved by Task 4 itself. THE KEY WAS ALREADY STALE ON HEAD: `git show HEAD:client/src/view/dm.ts` puts this comparator at line 469, sixteen lines below what this entry said (453) — some prior change (maps-as-geometry Task 2, per the reviewer who caught this) shifted it and was committed without running check:ts-mutation. That -16 drift is not this task's doing and is recorded rather than silently folded into one number. THEN Task 4 itself (original submission plus this fix round) added 37 lines above this line: DMDeps gained doorsArmed and toggleDoors with their doc comments (+15), and the "Doors" group with its rejected-alternative comment landed directly above "--- adventures ---", both inside renderDMConsole and above the control-panel loop (+22) — 15+22=37. 16+37=53, matching 453->506 exactly. Line 506:62 was byte-compared against HEAD's 469 (not against the stale 453) and is identical, and col 62 was READ at the new location rather than offset: `x.actorId < y.actorId `. CONFIRMED 2026-08-30 (Task 4 fix round 2): the coordinator's rekey map for this round named a DIFFERENT coordinate for this entry, derived from reports/mutation/mutation.json — but that report was generated from a snapshot taken the moment fix round 1's container run started, BEFORE the rest of fix round 1's edits to this file (which is what put this expression at its current line in the first place). Proven, not assumed: none of fix round 1's new test names appear anywhere in the 282MB report, byte-searched whole. The coordinate above was independently re-verified by reading the current source directly and is unchanged from fix round 1's own re-key. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 506:62 to 472:62, -34. dm.ts's SHIFT IS NOT UNIFORM: the `../undo` import went (-1), the now-unread `log: Envelope[]` prop went (-1), and the whole Undo group went (-32). So a key above DMDeps moves 1 and a key below the Undo group moves 34. Line 472 was byte-compared against HEAD's 506 and is identical, and col 62 was READ at the new location rather than offset: `x.actorId < y.actorId`. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 472:62 to 481:62, +9 — the Remove button added beside Place token in the SAME group (view/dm.ts's Place token group, ~9 lines) sits ABOVE both of these entries with no executable line of either the actor-panel comparator or the grant row's select changed. Verified by reading the tree directly: the quoted expression above was located at the new line and the column re-derived from it, not carried over as an offset. Re-pointed 2026-08-31 (retraction-leaves Task 9) from 481:62 to 496:62: Task 9 inserted the actorRemoved arm into fold.ts's switch, 46 lines at line 240, and a Remove-actor button into view/dm.ts's Add-actor group, 15 lines at line 337 — both ABOVE this entry. Located by DIFFING the line BYTE FOR BYTE against HEAD's 481 (identical) and re-reading column 62 at the new location, never by trusting an offset: the two files moved by different amounts, and dm.ts's own 158:7 entry did not move at all. Re-pointed 2026-09-01 (retraction-leaves Task 10, fix round 1) from 496:62 to 576:62, +80. dm.ts gained tileKindFromWireName and fillSelect above renderDMConsole (+61), a two-line fill <select> in the Create-scene group (+2), and the Create button's size and fill guards (+17) — every one of them ABOVE this entry. THE SHIFT IS NOT UNIFORM: dm.ts's own 158:7 sits above all three insertions and does not move at all. Line 576 was diffed BYTE FOR BYTE against HEAD's 496 (identical) and column 62 was READ at the new location rather than offset.
client/src/view/dm.ts 621:36 StringLiteral "Stryker was here!"
    The `?? ""` fallback when the DM has no remembered choice for this actor. Assigning ANY string with no matching <option> is normalised by the DOM to "" with selectedIndex -1 (HTML spec: setting select.value selects the matching option, or none). So the mutant and the original leave the element in the identical observable state, and the test that asserts a fresh dropdown reads "" passes under both. Not a claim that the fallback is pointless — it is what restores a remembered choice; a claim that its SENTINEL value is unobservable. Re-pointed 2026-08-23 (actor-kind Task 2) from 386:36 to 408:36, +22. dm.ts gained actorKindFromWireName and its doc comment above every one of these lines. Line 408 was byte-compared against HEAD's 386 and is identical, and col 36 was READ at the new location rather than offset: `"";`. Re-pointed 2026-08-24 (actor-kind Task 4) from 408:36 to 417:36, +9. The Add actor form lost its "controller participant id (optional)" input and gained the comment saying why, all ABOVE this line and with no executable line of the control panel changed. Line 417 was byte-compared against HEAD's 408 and is identical, and col 36 was READ at the new location rather than offset: `"";`. Re-pointed 2026-08-24 (actor-kind Task 7) from 417:36 to 469:36, +52, by the same two edits as the comparator entry directly above — and note this line is INSIDE the grant row, below the call that replaced its inline selector, so the shrink there nets out against nothing and the offset is the comparator's exactly. Line 469 was byte-compared against HEAD's 417 and is identical, and col 36 was READ at the new location rather than offset: `"";`. Re-pointed 2026-08-30 (Task 4 fix round 1) from 497:36 to 551:36, +54, by the same two causes as the comparator entry directly above, with one more added: THE KEY WAS ALREADY STALE ON HEAD (this line sits at 513 there, not 497 — the same uncredited -16 drift from before Task 4 touched the file). Task 4 then added 37 lines above it for the same reason as the comparator entry (DMDeps +15, the Doors group +22), plus ONE further line strictly between this line and the comparator above it: the grant row's Revoke button gained `off.dataset["action"] = "revoke-actor-control";` (+1), which sits below the comparator but above this fallback, so it widens THIS pair's gap by one where the comparator's own offset stops at 37. 16+37+1=54, matching 497->551 exactly — and explaining why the two entries' offsets are no longer identical, unlike every prior re-key of this pair. Line 551:36 was byte-compared against HEAD's 513 (not against the stale 497) and is identical, and col 36 was READ at the new location rather than offset: `"";`. CONFIRMED 2026-08-30 (Task 4 fix round 2): the coordinator's rekey map for this round named a DIFFERENT coordinate for this entry, derived from reports/mutation/mutation.json — but that report was generated from a snapshot taken the moment fix round 1's container run started, BEFORE the rest of fix round 1's edits to this file (which is what put this expression at its current line in the first place). Proven, not assumed: none of fix round 1's new test names appear anywhere in the 282MB report, byte-searched whole. The coordinate above was independently re-verified by reading the current source directly and is unchanged from fix round 1's own re-key. Re-pointed 2026-08-31 (retraction leaves, Task 3) from 551:36 to 517:36, -34. dm.ts's SHIFT IS NOT UNIFORM: the `../undo` import went (-1), the now-unread `log: Envelope[]` prop went (-1), and the whole Undo group went (-32). So a key above DMDeps moves 1 and a key below the Undo group moves 34. Line 517 was byte-compared against HEAD's 551 and is identical, and col 36 was READ at the new location rather than offset: `"";`. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 517:36 to 526:36, +9 — the Remove button added beside Place token in the SAME group (view/dm.ts's Place token group, ~9 lines) sits ABOVE both of these entries with no executable line of either the actor-panel comparator or the grant row's select changed. Verified by reading the tree directly: the quoted expression above was located at the new line and the column re-derived from it, not carried over as an offset. Re-pointed 2026-08-31 (retraction-leaves Task 9) from 526:36 to 541:36: Task 9 inserted the actorRemoved arm into fold.ts's switch, 46 lines at line 240, and a Remove-actor button into view/dm.ts's Add-actor group, 15 lines at line 337 — both ABOVE this entry. Located by DIFFING the line BYTE FOR BYTE against HEAD's 526 (identical) and re-reading column 36 at the new location, never by trusting an offset: the two files moved by different amounts, and dm.ts's own 158:7 entry did not move at all. Re-pointed 2026-09-01 (retraction-leaves Task 10, fix round 1) from 541:36 to 621:36, +80. dm.ts gained tileKindFromWireName and fillSelect above renderDMConsole (+61), a two-line fill <select> in the Create-scene group (+2), and the Create button's size and fill guards (+17) — every one of them ABOVE this entry. THE SHIFT IS NOT UNIFORM: dm.ts's own 158:7 sits above all three insertions and does not move at all. Line 621 was diffed BYTE FOR BYTE against HEAD's 541 (identical) and column 36 was READ at the new location rather than offset.

# --- client/src/view/canvas.ts -------------------------------------------

client/src/view/canvas.ts 65:24 ArithmeticOperator gx - gy
    drawMissingTile's checkerboard parity. `(gx + gy) % 2 === 0` and `(gx - gy) % 2 === 0` are the same predicate on every integer pair, not merely on the four this loop produces: (gx+gy) and (gx-gy) differ by 2*gy, so they always share a parity, and JS's sign-preserving `%` cannot separate them either, because the one case where it differs at all is an even negative, whose remainder is `-0`, and `-0 === 0` is true. The only observable this function has is the ordered sequence of fillStyle assignments and fillRect arguments reaching the context; a recording double (client/test/canvas.test.ts) captures both, and the two versions produce a byte-identical log — #ff00ff, #1a1a1a, #1a1a1a, #ff00ff over the same four rects. No input can widen the gx/gy range either: the bounds are the literal 2s in drawMissingTile's own two `for` headers, drawMissingTile is module-private, and its one caller passes only sw and sh. Verified by hand-applying the mutant (suite stayed green, 11/11) and by exhaustively comparing both predicates over 10,201 integer pairs in [-50, 50]^2: zero divergences.

# --- client/src/view/scene-plan.ts ---------------------------------------

client/src/view/scene-plan.ts 103:38 ArrayDeclaration ["Stryker was here"]
    planObjects's `scene.Objects ?? []` fallback fires only when a Scene omits Objects entirely (state.ts marks it optional so bare four-field literals keep compiling), and the mutant's fallback element is the STRING "Stryker was here". Reading `.X`, `.Y`, `.Width` and `.Height` off a string yields undefined, so sx/sy/sw/sh are all NaN; every comparison against NaN is false, so intersectsViewport's first clause (`NaN + NaN > 0`) short-circuits the whole conjunction and the extra element is culled before anything is pushed. The observable is the returned DrawOp[] and it is unchanged in every respect the equivalents file asks about: same array object (planScene allocates `ops` and hands it to planObjects), same length, same element order, same key order per element, no throw. Verified by hand-applying the mutant (suite stayed green, 673/673) and by running both versions over 439,040 (scene, camera, cell, viewport) combinations — scales 0/negative/1e9/±Infinity/NaN, offsets ±1e9/±Infinity/NaN, cells 0/negative/fractional/Infinity/NaN, viewports 0/-1/Infinity/NaN, with and without terrain — for zero divergences. ONE distinguisher exists and is recorded rather than hidden: defining numeric X/Y/Width/Height on String.prototype makes the fallback element draw (measured, not supposed). That is not an input to planScene — it is a redefinition of the language underneath it — and a test doing it would pin the literal contents of a defaulting expression rather than any behaviour of the planner, while leaking a polluted builtin into every other test sharing its process.
