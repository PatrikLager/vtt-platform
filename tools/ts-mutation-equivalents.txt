# Adjudicated EQUIVALENT mutants in the TypeScript client: surviving
# mutations that no test can kill, because the mutated code is observationally
# identical to the original.
#
# Format:  <file> <line>:<col> <MUTATOR> <replacement>
# followed by an indented reason line. The replacement runs to end of line
# because it can contain spaces.
#
# The REPLACEMENT is part of the key for a reason found the hard way: Stryker
# emits both `-> true` and `-> false` for one ConditionalExpression at one
# location, and without it those two collapse into a single entry — so
# adjudicating the harmless one silently excuses the other. The gate's own
# duplicate check caught that on client/src/player.ts:20:51. Both are required — a bare entry is a
# parse error, and so is a reason with no entry above it.
#
# ------------------------------------------------------------------------
# READ THIS BEFORE ADDING AN ENTRY.
#
# An "equivalent" verdict asserts that NO test can ever kill this mutant, so
# it forecloses writing one. A wrong entry is therefore WORSE than an
# unadjudicated survivor: it converts a real gap into a permanently excused
# one, in writing, with a confident-sounding justification.
#
# That is not hypothetical — see tools/mutation-equivalents.txt, where four
# mutants were adjudicated on the Go side and TWO were wrong. The reasoning
# was about JSON marshalling when the actual observable was reflect.DeepEqual
# over decoded structs, where nil map != empty map. A two-line fixture killed
# both.
#
# The test to apply, learned from that mistake: do not ask "is the output the
# same?" Ask "is there ANY observable — under deep equality, under identity,
# under undefined-ness, under iteration order, under what reaches the DOM —
# that distinguishes them?"
#
# An empty file is the GOAL STATE, not a problem. Prefer killing a mutant to
# excusing it; reach for an entry only when the mutation genuinely cannot be
# observed.
#
# Entries are keyed by file, line:col AND mutator, so two different mutations
# at one location are two separate claims. A stale entry — one whose mutant no
# longer survives, because it was killed or the line moved — is a hard error,
# since it would otherwise pre-approve whatever mutant lands there next.
#
# RE-KEYED 2026-08-21 (visibility Task 7), in two passes, and it was a
# re-derivation rather than an offset. 28 of the 64 keys moved; 36 were already
# right. The drift has two separate causes and the earlier drafts of this note
# ran them together: SOME of these keys were already wrong on main (fold.ts's
# eleven lines, view/spectator.ts's one, app.ts's two), while OTHERS were
# correct on main and were moved by this branch (view/grid.ts, wire.ts,
# session.ts). Neither group is "all of them".
#
# WHAT THE FIRST PASS COVERED, stated because the first draft of this note said
# "wholesale" and that is a completeness claim it did not have. It swept only
# the files Task 7 edited: all fourteen fold.ts entries (eleven distinct lines),
# the seven in view/grid.ts (two lines), and the one in view/spectator.ts. It
# left six stale
# keys standing in files it never opened, which review found:
#
#   entry said           actually at   note
#   wire.ts 175:13       292:13        moved by this branch; a COMMENT line
#   wire.ts 199:7        316:7         moved by this branch
#   app.ts 64:32         66:32         stale on MAIN; a COMMENT line
#   app.ts 127:32        129:32        stale on MAIN; a COMMENT line
#   session.ts 66:12     67:12         moved by this branch; a COMMENT line
#   session.ts 67:12     68:12         moved by this branch; landed on the
#                                      SIBLING comparison, right shape wrong
#                                      operand — the worst of the six, because
#                                      it looks plausible at the new location
#
# Four of the six named a line Stryker generates no mutant on at all. A key that
# points at a comment is not a near miss; it is an adjudication for a mutant
# that does not exist, beside a real survivor nobody has excused.
#
# THE "actually at" COLUMN ABOVE IS SUPERSEDED (2026-08-24). It records where
# those keys sat on 2026-08-21 and is kept for the reasoning, not as a current
# address. The perch UI moved six of them again and they are now:
#
#   wire.ts       292:13 -> 460:13   landed on a COMMENT line a SECOND time
#   wire.ts       316:7  -> 484:7
#   app.ts        66:32  -> 67:32    re-keyed, then KILLED 2026-08-25 and
#   app.ts        129:32 -> 130:32   removed outright — see the app.ts note below
#   session.ts    67:12  -> 68:12
#   session.ts    68:12  -> 69:12
#   spectator.ts  399:59 -> 501:59
#
# Read the entries themselves, never this table. That the SAME two wire.ts keys
# landed on comment lines twice in three days is the argument for keying on
# something other than a line number, and it is why the gate's own MOVED
# detection could not rescue the app.ts pair: their replacement text is
# `"Stryker was here!"`, which is identical for EVERY string literal in a file,
# so the pairing is ambiguous by construction and the gate correctly refused to
# guess. It told the reader to DELETE two sound adjudications instead.
#
# THE SECOND PASS IS MECHANICAL AND COVERS EVERY ENTRY. All 64 were parsed and
# each one's file:line:col was read out of the working tree and compared against
# the expression its own replacement text names. It re-pointed the six above,
# plus the two fold.ts keys its OWN edit to that file had just moved.
#
# WRITE THAT CHECK CAREFULLY IF YOU RE-RUN IT. The first version here had two
# holes and reported "0 suspect" over a key that had drifted three lines onto a
# `/**` line: it tested `startswith("//")`, which a `/**` doc comment does not
# match, and it had no branch for the mutator on that entry so it silently
# skipped it. A checker that cannot fail is worse than no checker, and this one
# said everything was fine twice. Test it against a key you have deliberately
# broken before believing it.
#
# THE DELTAS ARE NOT UNIFORM, which is exactly why this file's own procedure
# says to READ the new location rather than assume the shift. Measured by
# comparing this file against main entry by entry, in order: the 28 moved keys
# span +1 (session.ts 66 -> 67) to +208 (fold.ts 350 -> 558), and every one of
# the 64 kept its COLUMN — only lines moved. Per file the shifts are
# app.ts +2, session.ts +1, wire.ts +117, view/grid.ts +31,
# view/spectator.ts +144, and fold.ts a spread of +3/+133/+157/+208. Anyone repeating that comparison
# should pair the entries POSITIONALLY, not by (file, mutator, replacement): the
# file contains repeated triples on purpose, twelve of them — for instance
# app.ts's two `StringLiteral
# "Stryker was here!"`, view/grid.ts's two `StringLiteral ""` (129:45 and
# 129:57) and its two `EqualityOperator a.tokenId <= b.tokenId` (139:32 and
# 139:61) — and a dict keyed that way silently drops them and reports 22 instead
# of 28. That happened here. NOTE the four entries at 139:61 are NOT an example:
# they are four DISTINCT triples at one location, which is exactly what keying
# on the replacement text is for.
#
# Task 7's own contribution, across its landing commit and three fix rounds,
# measured as two hunks against 1f34407: +20 in fold.ts's sceneSeen arm
# (@@ -212,6 +212,26 @@) and +16 in its dump (@@ -489,6 +509,22 @@), which sum
# to fold.ts's total of +36; +31 in view/grid.ts; +10 in view/spectator.ts; and
# nothing at all in wire.ts, session.ts or app.ts — those three it never
# touched.
#
# "THE SMALLER HALF" IS TRUE OF fold.ts AND FALSE OF view/grid.ts, and an
# earlier draft said it of everything. grid.ts is byte-identical between main
# and 1f34407, so Task 7's +31 is the WHOLE of that file's shift; fold.ts's +36
# sits under a further +157 that Tasks 1-6 put in. The dump figure was +11 in
# that draft, carried forward unexamined from before the round that was supposed
# to re-measure all of it — it had already been +16 at 15cd2c7.
#
# THIS HEADER'S OWN CITATIONS ARE PART OF THE RE-KEY, and round 2 found them
# stale after round 2's edits: it re-pointed the entries and left the numbers
# quoted up here pointing at the old lines. A preamble that disagrees with its
# own contents is the failure this file exists to prevent, so when you move an
# entry, grep this header for its old line before you stop.
#
# UNVERIFIED BY THE GATE, and that is stated rather than glossed: check:
# ts-mutation skips on macOS (kernel bug FB21686886, see Taskfile.yml), so these
# keys were derived by reading the source, not by watching Stryker agree. The
# first Linux run is what confirms or corrects them.

client/src/undo.ts 39:26 EqualityOperator e.sequence >= best
    `e.sequence > best` -> `>=` in lastUndoable. The two differ only when e.sequence equals best, and in that case the mutant's extra assignment writes the value best already holds. The guard `best === null ||` short-circuits before this operand on the first iteration, so the null case never reaches it. No observable: not the return value, not the number of iterations, not identity (BigInt is a value type).

client/src/undo.ts 63:28 EqualityOperator e.sequence >= head
    `e.sequence > head` -> `>=` when computing the head. Same shape as above: equality makes the assignment a no-op writing head's current value. head starts at 0n, so a sequence of 0 assigns 0n over 0n. Nothing downstream can distinguish them.

# --- client/src/fold.ts -----------------------------------------------------
#
# fold.ts went 93 survivors -> 14 by writing the rejection and dump suites
# (client/test/fold-rejections.test.ts, fold-dump.test.ts). The 14 below are
# the residue, and they fall into four shapes, none of which any test can
# observe. Each was checked against the "is there ANY observable?" rule rather
# than "is the output the same?".

client/src/fold.ts 61:9 ConditionalExpression false
    Deleting `if (env.payload.case === "eventsRetracted") continue;` in pass 2 lets a retraction MARKER reach apply(). apply()'s switch has no case for eventsRetracted, so it falls to `default: return`, and nothing before the switch has a side effect (it computes seq and reads payload). The marker is skipped either way — one by the guard, one by the default arm. Re-pointed 2026-08-23 (actor-kind Task 2) from 40:9 to 43:9, +3. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 43 was byte-compared against HEAD's 40 and is identical, and col 9 was READ at the new location rather than offset: `env.payload.case === "`. Re-pointed 2026-08-25 (prototype-safe maps) from 43:9 to 61:9, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 61:9 was byte-compared against HEAD's 43 and is identical, and col 9 was READ at the new location rather than offset: `env.payload.case === "`.

client/src/fold.ts 61:30 StringLiteral ""
    Same line, same effect by another route: the case name becomes "", the comparison is false for every envelope, and markers reach apply()'s no-op default arm. A payload-less envelope has case undefined, not "", so it cannot collide with the emptied literal. Re-pointed 2026-08-23 (actor-kind Task 2) from 40:30 to 43:30, +3. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 43 was byte-compared against HEAD's 40 and is identical, and col 30 was READ at the new location rather than offset: `"eventsRetracted") con`. Re-pointed 2026-08-25 (prototype-safe maps) from 43:30 to 61:30, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 61:30 was byte-compared against HEAD's 43 and is identical, and col 30 was READ at the new location rather than offset: `"eventsRetracted") con`.

client/src/fold.ts 318:48 ArrayDeclaration ["Stryker was here"]
    `st.Conditions[v.actorId] ?? []` -> `?? ["Stryker was here"]` in conditionRemoved. The fallback fires only when the actor has NEVER had a condition (an emptied list is retained as [], which is not nullish). findIndex then compares `c.ID` on a string element — undefined — against the condition id, yielding -1, so the fold throws the same "not present" error it would have thrown. The assignment that would leak the sentinel is after that throw. Re-pointed 2026-08-23 (actor-kind Task 2) from 271:48 to 287:48, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 287 was byte-compared against HEAD's 271 and is identical, and col 48 was READ at the new location rather than offset: `[];`. Re-pointed 2026-08-24 (actor-kind Task 4) from 287:48 to 300:48, +13. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 300 was byte-compared against HEAD's 287 and is identical, and col 48 was READ at the new location rather than offset: `[];`. Re-pointed 2026-08-25 (prototype-safe maps) from 300:48 to 318:48, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 318:48 was byte-compared against HEAD's 300 and is identical, and col 48 was READ at the new location rather than offset: `[];`.

client/src/fold.ts 347:19 OptionalChaining actor.resources[v.resource]
    `actor.resources?.[v.resource]` -> `actor.resources[v.resource]`. Removing the guard only matters if a stored Actor can have undefined resources. It cannot: line 137 is the only writer to st.Actors and it always goes through copyActor, which builds `resources` as an object literal before returning. Re-pointed 2026-08-23 (actor-kind Task 2) from 300:19 to 316:19, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 316 was byte-compared against HEAD's 300 and is identical, and col 19 was READ at the new location rather than offset: `actor.resources?.[v.re`. Re-pointed 2026-08-24 (actor-kind Task 4) from 316:19 to 329:19, +13. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 329 was byte-compared against HEAD's 316 and is identical, and col 19 was READ at the new location rather than offset: `actor.resources`. Re-pointed 2026-08-25 (prototype-safe maps) from 329:19 to 347:19, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 347:19 was byte-compared against HEAD's 329 and is identical, and col 19 was READ at the new location rather than offset: `actor.resources?.[v.re`.

client/src/fold.ts 356:11 EqualityOperator computed <= 0
    `computed < 0` -> `<= 0` in the resource clamp. The arms differ only at computed === 0, where the mutant assigns 0 over 0. Re-pointed 2026-08-23 (actor-kind Task 2) from 309:11 to 325:11, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 325 was byte-compared against HEAD's 309 and is identical, and col 11 was READ at the new location rather than offset: `computed < 0) computed`. Re-pointed 2026-08-24 (actor-kind Task 4) from 325:11 to 338:11, +13. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 338 was byte-compared against HEAD's 325 and is identical, and col 11 was READ at the new location rather than offset: `computed < 0) c`. Re-pointed 2026-08-25 (prototype-safe maps) from 338:11 to 356:11, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 356:11 was byte-compared against HEAD's 338 and is identical, and col 11 was READ at the new location rather than offset: `computed < 0) computed`.

client/src/fold.ts 357:26 EqualityOperator computed >= res.max
    `computed > res.max` -> `>=`. Differ only at computed === res.max, where the mutant assigns max over max. Re-pointed 2026-08-23 (actor-kind Task 2) from 310:26 to 326:26, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 326 was byte-compared against HEAD's 310 and is identical, and col 26 was READ at the new location rather than offset: `computed > res.max) co`. Re-pointed 2026-08-24 (actor-kind Task 4) from 326:26 to 339:26, +13. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 339 was byte-compared against HEAD's 326 and is identical, and col 26 was READ at the new location rather than offset: `computed > res.`. Re-pointed 2026-08-25 (prototype-safe maps) from 339:26 to 357:26, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 357:26 was byte-compared against HEAD's 339 and is identical, and col 26 was READ at the new location rather than offset: `computed > res.max) co`.

client/src/fold.ts 388:10 StringLiteral ""
    `case "attackRolled":` -> `case "":`. The case body is a bare `return`, and an attackRolled event whose label no longer matches falls through to `default:`, which is also a bare `return`. A no-op reached two ways. Re-pointed 2026-08-23 (actor-kind Task 2) from 341:10 to 357:10, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 357 was byte-compared against HEAD's 341 and is identical, and col 10 was READ at the new location rather than offset: `"attackRolled":`. Re-pointed 2026-08-24 (actor-kind Task 4) from 357:10 to 370:10, +13. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 370 was byte-compared against HEAD's 357 and is identical, and col 10 was READ at the new location rather than offset: `"attackRolled":`. Re-pointed 2026-08-25 (prototype-safe maps) from 370:10 to 388:10, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 388:10 was byte-compared against HEAD's 370 and is identical, and col 10 was READ at the new location rather than offset: `"attackRolled":`.

client/src/fold.ts 389:10 StringLiteral ""
    `case "abilityUsed":` -> `case "":`, same no-op-vs-no-op as above. Re-pointed 2026-08-23 (actor-kind Task 2) from 342:10 to 358:10, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 358 was byte-compared against HEAD's 342 and is identical, and col 10 was READ at the new location rather than offset: `"abilityUsed":`. Re-pointed 2026-08-24 (actor-kind Task 4) from 358:10 to 371:10, +13. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 371 was byte-compared against HEAD's 358 and is identical, and col 10 was READ at the new location rather than offset: `"abilityUsed":`. Re-pointed 2026-08-25 (prototype-safe maps) from 371:10 to 389:10, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 389:10 was byte-compared against HEAD's 371 and is identical, and col 10 was READ at the new location rather than offset: `"abilityUsed":`.

client/src/fold.ts 390:5 ConditionalExpression case "adventureLoaded":
    Rewrites the `case "adventureLoaded":` clause itself. Every arm involved returns without touching state, so no reordering of them is observable. Re-pointed 2026-08-23 (actor-kind Task 2) from 343:5 to 359:5, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 359 was byte-compared against HEAD's 343 and is identical, and col 5 was READ at the new location rather than offset: `case "adventureLoaded"`. Re-pointed 2026-08-24 (actor-kind Task 4) from 359:5 to 372:5, +13. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 372 was byte-compared against HEAD's 359 and is identical, and col 5 was READ at the new location rather than offset: `case "adventure`. Re-pointed 2026-08-25 (prototype-safe maps) from 372:5 to 390:5, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 390:5 was byte-compared against HEAD's 372 and is identical, and col 5 was READ at the new location rather than offset: `case "adventureLoaded"`.

client/src/fold.ts 390:10 StringLiteral ""
    `case "adventureLoaded":` -> `case "":`, same no-op-vs-no-op as above. Re-pointed 2026-08-23 (actor-kind Task 2) from 343:10 to 359:10, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 359 was byte-compared against HEAD's 343 and is identical, and col 10 was READ at the new location rather than offset: `"adventureLoaded":`. Re-pointed 2026-08-24 (actor-kind Task 4) from 359:10 to 372:10, +13. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 372 was byte-compared against HEAD's 359 and is identical, and col 10 was READ at the new location rather than offset: `"adventureLoade`. Re-pointed 2026-08-25 (prototype-safe maps) from 372:10 to 390:10, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 390:10 was byte-compared against HEAD's 372 and is identical, and col 10 was READ at the new location rather than offset: `"adventureLoaded":`.

client/src/fold.ts 392:5 ConditionalExpression default:
    Rewrites the `default:` clause. Its body is a bare `return`, shared with the three no-effect event kinds above it; nothing distinguishes reaching it from reaching them. Re-pointed 2026-08-23 (actor-kind Task 2) from 345:5 to 361:5, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 361 was byte-compared against HEAD's 345 and is identical, and col 5 was READ at the new location rather than offset: `default:`. Re-pointed 2026-08-24 (actor-kind Task 4) from 361:5 to 374:5, +13. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 374 was byte-compared against HEAD's 361 and is identical, and col 5 was READ at the new location rather than offset: `default:`. Re-pointed 2026-08-25 (prototype-safe maps) from 374:5 to 392:5, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 392:5 was byte-compared against HEAD's 374 and is identical, and col 5 was READ at the new location rather than offset: `default:`.

client/src/fold.ts 509:34 EqualityOperator Number(e.sequence) >= head
    `Number(e.sequence) > head` -> `>=` when computing headSequence. Differ only at equality, where the mutant assigns head its current value. (The `-> true` mutant at this location IS killable and is killed by "headSequence is the MAXIMUM sequence, not the last envelope's".) Re-pointed 2026-08-23 (actor-kind), +7: copyActor gained a kind field and its comment, both above this line. Line 473 was byte-compared against HEAD's 466 and is identical; col 34 read at the new location is `Number(e.sequence) >`. Re-pointed 2026-08-23 (actor-kind Task 2) from 473:34 to 489:34, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 489 was byte-compared against HEAD's 473 and is identical, and col 34 was READ at the new location rather than offset: `Number(e.sequence) > h`. Re-pointed 2026-08-24 (actor-kind Task 4) from 489:34 to 491:34, +2. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 491 was byte-compared against HEAD's 489 and is identical, and col 34 was READ at the new location rather than offset: `Number(e.sequen`. Re-pointed 2026-08-25 (prototype-safe maps) from 491:34 to 509:34, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 509:34 was byte-compared against HEAD's 491 and is identical, and col 34 was READ at the new location rather than offset: `Number(e.sequence) > h`.

client/src/fold.ts 619:7 ConditionalExpression true
    `if (a.actorId !== "")` -> `true` in actorJSON's omitempty emission. An Actor in state can never have an empty actorId: line 137 is the only writer, and line 135 rejects the event before it with "actor added with no actor or empty id". The condition is therefore already always true. Re-pointed 2026-08-23 (actor-kind) from 558:7 to 583:7, and THE OLD KEY WAS ALREADY WRONG ON MAIN — this is a correction, not just a shift. HEAD's line 558 is `ActorID: t.ActorID,` inside foldToDumpJSON's Tokens mapper: no conditional and no string literal, so it could not host either of these mutants and this pair has been excusing a location it does not describe. actorJSON's `if (a.actorId !== "")` was at 576 on main and is at 583 now (+7, copyActor's kind field and comment above it). Both columns were READ at the new location rather than offset: col 7 is `a.actorId !== ""`, col 21 is the `""`. That makes this the seventh stale TS key found by reading rather than by the gate, and the re-key note above is the reason to expect more: the 2026-08-21 sweep re-derived fold.ts's eleven lines and this pair still came out wrong. Re-pointed 2026-08-23 (actor-kind Task 2) from 583:7 to 599:7, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 599 was byte-compared against HEAD's 583 and is identical, and col 7 was READ at the new location rather than offset: `a.actorId !== "") out[`. Re-pointed 2026-08-24 (actor-kind Task 4) from 599:7 to 601:7, +2. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 601 was byte-compared against HEAD's 599 and is identical, and col 7 was READ at the new location rather than offset: `a.actorId !== "`. Re-pointed 2026-08-25 (prototype-safe maps) from 601:7 to 619:7, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 619:7 was byte-compared against HEAD's 601 and is identical, and col 7 was READ at the new location rather than offset: `a.actorId !== "") out[`.

client/src/fold.ts 619:21 StringLiteral "Stryker was here!"
    Same line, comparing against "Stryker was here!" instead of "". Also always true, for the same reason: no stored actorId equals either string. Re-pointed 2026-08-23 (actor-kind) from 558:21 to 583:21, and THE OLD KEY WAS ALREADY WRONG ON MAIN — this is a correction, not just a shift. HEAD's line 558 is `ActorID: t.ActorID,` inside foldToDumpJSON's Tokens mapper: no conditional and no string literal, so it could not host either of these mutants and this pair has been excusing a location it does not describe. actorJSON's `if (a.actorId !== "")` was at 576 on main and is at 583 now (+7, copyActor's kind field and comment above it). Both columns were READ at the new location rather than offset: col 7 is `a.actorId !== ""`, col 21 is the `""`. That makes this the seventh stale TS key found by reading rather than by the gate, and the re-key note above is the reason to expect more: the 2026-08-21 sweep re-derived fold.ts's eleven lines and this pair still came out wrong. Re-pointed 2026-08-23 (actor-kind Task 2) from 583:21 to 599:21, +16. fold.ts's ActorKind import became a VALUE import with a 3-line comment (+3), and the grant/revoke arms gained a conditional kind write and their comments (+13) — which is why THIS FILE'S SHIFT IS NOT UNIFORM: keys above the grant arm move 3 and keys below it move 16. Line 599 was byte-compared against HEAD's 583 and is identical, and col 21 was READ at the new location rather than offset: `"") out["actor_id"] = `. Re-pointed 2026-08-24 (actor-kind Task 4) from 599:21 to 601:21, +2. fold.ts's actorAdded arm gained the refusal of an actor that names a controller (+13) and copyActor's controller ternary collapsed to `mirrorControl([])` (-2), so THIS FILE'S SHIFT IS NOT UNIFORM: keys above the actorAdded arm move 0 and keys below it move 13 down to line 460 and 2 after it. Line 601 was byte-compared against HEAD's 599 and is identical, and col 21 was READ at the new location rather than offset: `"") out["actor_`. Re-pointed 2026-08-25 (prototype-safe maps) from 601:21 to 619:21, +18, and THIS FILE'S SHIFT IS UNIFORM for once: both additions went above the first key in it — a 17-line paragraph in fold.ts's header on why its maps carry no prototype, and `emptyMap,` in the import from ./state — and every edit below them was written to be line-count-neutral so that the twenty keys here would all move by one number. Line 619:21 was byte-compared against HEAD's 601 and is identical, and col 21 was READ at the new location rather than offset: `"") out["actor_id"] = `.

client/src/fold.ts 527:32 ArrayDeclaration ["Stryker was here"]
    `Objects: (s.Objects ?? []).map(...)` -> `?? ["Stryker was here"]` in foldToDumpJSON. The `??` right-hand side is evaluated only when `s.Objects` is nullish, and foldToDumpJSON folds its own state on the line above (`const st = fold(envelopes)`), so every Scene it renders came from fold.ts:111 with a real array. RE-EXAMINED 2026-08-25 against the prototype defect that made this entry's four neighbours wrong, and this one is genuinely untouched by it — which is the distinction worth recording, since the neighbours' reasons read almost identically. sortedMap ITERATES st.Scenes (`Object.keys(m)`) instead of looking a key up in it, and Object.keys returns own properties only, so no prototype member was ever handed to this mapper. Measured both ways by hand-applying the mutant: it survives the whole suite on HEAD 9d9d9df AND after the fix (684 pass, 0 fail), unlike 259:11/260:11/261:11/261:37/419:7, every one of which was killable on HEAD. fold.ts's own comment on these defaults already says they are "belt-and-braces rather than a path any golden actually exercises" — this entry is that sentence, verified. Re-pointed 2026-08-25 from 509:32 to 527:32, +18. Line 527:32 was byte-compared against HEAD's 509 and is identical, and col 32 was READ at the new location rather than offset: `[]).map((o) => ({`.

client/src/fold.ts 419:7 ConditionalExpression false
    `if (!sc.OpenDoors) sc.OpenDoors = emptyMap<boolean>();` -> `if (false)` in ensureOpenDoors. Same argument as the sceneSeen triple, one function over: fold.ts:111 sets `OpenDoors: emptyMap<boolean>()`, and door events only ever `delete` a KEY from it, never the property, so no folded Scene reaches ensureOpenDoors without it. The function still returns `sc.OpenDoors`, the same object it would have returned. SAME CORRECTION (2026-08-25): the old reason rested on "111 is the only writer" and a lookup needed no writer. Measured against HEAD 9d9d9df: `doorOpened` naming scene "hasOwnProperty" folded cleanly unmutated and threw `TypeError: undefined is not an object (evaluating 'ensureOpenDoors(sc)[doorKey(...)] = !0')` with the mutant applied. The door arms' own scene lookup now answers honestly, so that log is refused before ensureOpenDoors is called ('door opened in unknown scene "hasOwnProperty"'); the accept side of the same id is pinned by "a scene whose id names a prototype member is created, not refused as a duplicate" (client/test/fold-unit.test.ts), which opens a door on it and reads OpenDoors back. Re-verified after the fix: mutant applied, 684 pass, 0 fail. The `-> true` and `BooleanLiteral` mutants at this location are killable and were killed on 2026-08-24 by "opening a second door leaves the first one open, and shutting one leaves the other". Re-pointed 2026-08-25 from 401:7 to 419:7, +18. The line changed (`{}` -> `emptyMap<boolean>()`), so no byte comparison; col 7 was READ at the new location: `!sc.OpenDoors) sc.Open`.

client/src/fold.ts 261:37 ArrayDeclaration ["Stryker was here"]
    `sc.Objects = []` -> `sc.Objects = ["Stryker was here"]` inside the guard body above. The sentinel can only reach anything if the branch is taken, and after 2026-08-25 it is not. ITS OLD EVIDENCE WAS THE WRONG MEASUREMENT. The entry said "measured: zero 'GUARD REACHED 243' across the whole suite", which was a fact about the SUITE and was being read as a fact about the CODE — no test folded a scene whose id named a prototype member, and one that did reached this guard immediately. Measured against HEAD 9d9d9df: a sceneSeen naming scene "hasOwnProperty" took the branch, and with this mutant applied the sentinel landed in `Object.prototype.hasOwnProperty.Objects[0]` — not in st, but in the prototype every object in the process inherits from, which a test can read and assert on. Killable, therefore, and adjudicated otherwise. st.Scenes is a prototype-less dictionary now (state.ts's emptyMap), so the lookup that reached the guard returns undefined and the arm rejects instead; "sceneSeen for a scene that never arrived is refused, even when its id names a prototype member" (client/test/fold-rejections.test.ts) holds it there. Re-verified after the fix: mutant applied, 684 pass, 0 fail. Re-pointed 2026-08-25 from 243:37 to 261:37, +18. Line 261:37 was byte-compared against HEAD's 243 and is identical, and col 37 was READ at the new location rather than offset: `[];`.

client/src/fold.ts 261:11 ConditionalExpression false
    `if (!sc.Objects) sc.Objects = [];` -> `if (false)`. Third of the same triple; fold.ts:111 sets `Objects: objects`, an array built by `.map()` and never undefined. SAME CORRECTION AS ITS TWO NEIGHBOURS (2026-08-25) and the loudest of the three: measured against HEAD 9d9d9df, this mutant turned a cleanly-folding sceneSeen on scene "hasOwnProperty" into `TypeError: undefined is not an object (evaluating 'sc.Objects.findIndex')`. The old premise — fold.ts's only writer to st.Scenes — was true and did not entail what it was used for, because `st.Scenes[id]` on an object literal answers for ids nobody wrote. st.Scenes is prototype-less now (state.ts's emptyMap), and the one route that reached this guard is pinned shut by "sceneSeen for a scene that never arrived is refused, even when its id names a prototype member" (client/test/fold-rejections.test.ts). Re-verified after the fix: mutant applied, 684 pass, 0 fail. The other two mutants at this exact location — `-> true` and `BooleanLiteral -> sc.Objects` — are NOT equivalent and were killed on 2026-08-24 by "sceneSeen's merge keeps SLOTS, keeps objects it does not mention, and takes a positionless one at the origin", which is why the replacement text is part of the key. Re-pointed 2026-08-25 from 243:11 to 261:11, +18. Line 261:11 was byte-compared against HEAD's 243 and is identical, and col 11 was READ at the new location rather than offset: `!sc.Objects) sc.Object`.

client/src/fold.ts 260:11 ConditionalExpression false
    `if (!sc.Explored) sc.Explored = emptyMap<boolean>();` -> `if (false)`, the same guard one line down. Equivalent for the same reason and worth keeping separate for the same one: the Go arm it mirrors leaves Explored NIL at SceneCreated (apply.go's SceneCreated arm has no Explored field), so this TS guard mirrors a Go guard that IS live, and only the TS side's stricter initialisation at fold.ts:111 makes it dead. SAME CORRECTION AS ITS NEIGHBOUR (2026-08-25): the old premise stopped at "111 is the only writer", which said nothing about a lookup that finds a prototype member instead. Measured against HEAD 9d9d9df: with the mutant applied, a sceneSeen naming scene "hasOwnProperty" throws `TypeError: undefined is not an object (evaluating 'sc.Explored[key] = !0')` where the unmutated fold folds it cleanly — killable, not equivalent. st.Scenes is prototype-less now (state.ts's emptyMap) and that log is rejected before this line by "sceneSeen for a scene that never arrived is refused, even when its id names a prototype member" (client/test/fold-rejections.test.ts). Re-verified after the fix: mutant applied, 684 pass, 0 fail. Re-pointed 2026-08-25 from 242:11 to 260:11, +18. The line changed (`{}` -> `emptyMap<boolean>()`), so no byte comparison; col 11 was READ at the new location: `!sc.Explored) sc.Explo`.

client/src/fold.ts 259:11 ConditionalExpression false
    `if (!sc.Tiles) sc.Tiles = emptyMap<Tile>();` -> `if (false)` in the sceneSeen arm. `sc` can only be a Scene fold.ts:111 built, and 111 sets `Tiles: tiles`; with the condition pinned false the assignment that never happened still never happens, and the tile loop below writes into the same object either way. SAME VERDICT, DIFFERENT PREMISE, AND THE OLD PREMISE WAS FALSE (2026-08-25). This entry used to argue from "111 is the only writer to st.Scenes" alone, and that inference does not hold: a LOOKUP needs no writer. st.Scenes was an object literal, so `st.Scenes["hasOwnProperty"]` returned Object.prototype.hasOwnProperty — truthy, and a Scene in nothing but the type system — and this guard WAS reached, with `sc.Tiles` undefined. Measured against HEAD 9d9d9df by hand-applying the mutant: a sceneSeen naming that scene folds cleanly unmutated and throws `TypeError: undefined is not an object (evaluating 'sc.Tiles[key] = ...')` with the mutant applied. So this entry was excusing a KILLABLE mutant, which this file's header calls worse than an unadjudicated survivor. st.Scenes is a prototype-less dictionary now (state.ts's emptyMap), which is the half of the premise that was missing: a lookup can return only what a writer put there. The route is closed by a test rather than by argument — "sceneSeen for a scene that never arrived is refused, even when its id names a prototype member" (client/test/fold-rejections.test.ts) folds exactly that log and requires the FoldError. Re-verified after the fix by hand-applying the mutant again: 684 pass, 0 fail. Re-pointed 2026-08-25 from 241:11 to 259:11, +18 (see the uniform-shift note on any other fold.ts key). The LINE ITSELF changed — `{}` became `emptyMap<Tile>()` — so it was not byte-compared; the condition is untouched and col 11 was READ at the new location: `!sc.Tiles) sc.Tiles = `.

# --- client/src/view/dm.ts --------------------------------------------------
#
# 166 survivors -> 1 by writing the messages, the payloads and the field
# contract (client/test/dm-view.test.ts). One further mutant is handled in the
# SOURCE with a `// Stryker disable` and a stated reason rather than here,
# because it is NOT equivalent: dropping el()'s `text !== undefined` guard puts
# the word "undefined" above every group in a real browser, while happy-dom
# coerces it to "" and no test can see the difference. Claiming equivalence for
# it would be the wrong-adjudication this file warns about.

client/src/view/dm.ts 159:7 ConditionalExpression true
    `if (cls) i.className = cls` -> always-assign, in the input() helper. Unlike el()'s version of the same line, this parameter has a default of "" rather than being optional, so the mutated branch assigns the empty string — which is what className already holds. No element can tell the two apart, and there is no call site that passes undefined. Re-pointed 2026-08-23 (actor-kind Task 2) from 71:7 to 93:7, +22. dm.ts gained actorKindFromWireName and its doc comment above every one of these lines. Line 93 was byte-compared against HEAD's 71 and is identical, and col 7 was READ at the new location rather than offset: `cls) i.className = cls`. Re-pointed 2026-08-24 (actor-kind Task 7) from 93:7 to 131:7, +38. dm.ts gained kindSelect() and its doc comment (33 lines), placed between actorKindFromWireName and input() and so entirely ABOVE this helper, plus 5 lines of correction to actorKindFromWireName's own doc after review. Line 131 was byte-compared against HEAD's 93 and is identical, and col 7 was READ at the new location rather than offset: `cls) i.className = cls`.

# --- client/src/player.ts ----------------------------------------------------
#
# 22 survivors -> the comparators below, by pinning ORDER (client/test/
# player.test.ts). The game rules here — Chebyshev range, affordability — were
# already covered; what was unpinned was that every returned list is SORTED,
# and a reshuffling panel moves the actor out from under a player's cursor.
#
# What remains is one shape, in three copies of the same comparator:
#
#     (a, b) => (a.id < b.id ? -1 : a.id > b.id ? 1 : 0)
#
# Every id sorted here is UNIQUE, and that one fact is what makes these
# unobservable. Stated precisely, because the loose version ("the ids are the
# map keys") is not quite true and hid where the risk lives: a.actorId is a
# FIELD, not the key. It is unique only because the fold stores each actor and
# token under a key equal to its own id — fold.ts:137 and :160 — an invariant
# maintained in a DIFFERENT FILE from the one these adjudications describe.
#
# That premise is now machine-checked, not merely asserted: see "every actor
# and token is stored under a key equal to its own id" in
# client/test/fold-dump.test.ts. If the fold ever stores an actor under some
# other key, that test fails and these fifteen claims must be re-derived rather
# than carried forward. The same applies if ids ever stop coming from a map at
# all — a list of actors arriving as an array, say.

client/src/player.ts 26:22 EqualityOperator a.actorId <= b.actorId
    Comparator boundary that differs only when the two ids are equal. They are object keys of the same map, so no two elements can share one.

client/src/player.ts 26:51 ConditionalExpression true
    Forces the inner `a > b ? 1 : 0` to 1. That branch is reached only when `a < b` is false, which for unique keys means a > b — where 1 is already the answer.

client/src/player.ts 26:51 ConditionalExpression false
    Forces the inner `a > b ? 1 : 0` to 0, leaving `a < b ? -1 : 0`. For unique keys that is a complete strict-less oracle and the sort reaches the same order from the negative signal alone; verified identical to the full comparator at n = 3, 5, 10 and 20 on deliberately unsorted ids, rather than assumed.

client/src/player.ts 26:51 EqualityOperator a.actorId >= b.actorId
    Comparator boundary that differs only when the two ids are equal. They are object keys of the same map, so no two elements can share one.

client/src/player.ts 26:51 EqualityOperator a.actorId <= b.actorId
    Comparator boundary that differs only when the two ids are equal. They are object keys of the same map, so no two elements can share one.

client/src/player.ts 58:22 EqualityOperator a.ID <= b.ID
    Comparator boundary that differs only when the two ids are equal. They are object keys of the same map, so no two elements can share one.

client/src/player.ts 58:41 ConditionalExpression true
    Forces the inner `a > b ? 1 : 0` to 1. That branch is reached only when `a < b` is false, which for unique keys means a > b — where 1 is already the answer.

client/src/player.ts 58:41 ConditionalExpression false
    Forces the inner `a > b ? 1 : 0` to 0, leaving `a < b ? -1 : 0`. For unique keys that is a complete strict-less oracle and the sort reaches the same order from the negative signal alone; verified identical to the full comparator at n = 3, 5, 10 and 20 on deliberately unsorted ids, rather than assumed.

client/src/player.ts 58:41 EqualityOperator a.ID <= b.ID
    Comparator boundary that differs only when the two ids are equal. They are object keys of the same map, so no two elements can share one.

client/src/player.ts 58:41 EqualityOperator a.ID >= b.ID
    Comparator boundary that differs only when the two ids are equal. They are object keys of the same map, so no two elements can share one.

client/src/player.ts 75:5 EqualityOperator a.actorId <= b.actorId
    Comparator boundary that differs only when the two ids are equal. They are object keys of the same map, so no two elements can share one.

client/src/player.ts 75:34 ConditionalExpression true
    Forces the inner `a > b ? 1 : 0` to 1. That branch is reached only when `a < b` is false, which for unique keys means a > b — where 1 is already the answer.

client/src/player.ts 75:34 EqualityOperator a.actorId >= b.actorId
    Comparator boundary that differs only when the two ids are equal. They are object keys of the same map, so no two elements can share one.

client/src/player.ts 75:34 ConditionalExpression false
    Forces the inner `a > b ? 1 : 0` to 0, leaving `a < b ? -1 : 0`. For unique keys that is a complete strict-less oracle and the sort reaches the same order from the negative signal alone; verified identical to the full comparator at n = 3, 5, 10 and 20 on deliberately unsorted ids, rather than assumed.

client/src/player.ts 75:34 EqualityOperator a.actorId <= b.actorId
    Comparator boundary that differs only when the two ids are equal. They are object keys of the same map, so no two elements can share one.

# --- client/src/view/spectator.ts --------------------------------------------
#
# 63 survivors -> 0 by pinning the board's GEOMETRY (the click-to-cell mapping
# and the board's size), describe()'s crash-safety on malformed events, and the
# ordering of notes and scenes. One mutant is handled in the SOURCE with a
# `// Stryker disable` and a stated reason — el()'s `text !== undefined` guard,
# the same harness-blind case as view/dm.ts.

client/src/view/spectator.ts 501:59 StringLiteral "Stryker was here!"
    `Object.keys(st.Scenes).sort().at(-1) ?? ""` — the fallback value. It is reached ONLY when there are no scenes, because at(-1) returns undefined only for an empty key list. Whatever string it yields is then looked up in st.Scenes and missed, rendering "No scene yet." either way. A scene keyed by the sentinel string would distinguish them, which requires a scene id of "Stryker was here!" to exist. (A scene keyed "" is a different case and IS pinned by test — that key is returned by at(-1) directly, never via the fallback.)

client/src/view/spectator.ts 365:10 EqualityOperator label(a, a.actorId) <= label(b, b.actorId)
    The name-comparison arm, and it is the ELSE of `label(a, a.actorId) === label(b, b.actorId)` on the line above — so it is evaluated only when the two labels are NOT equal, which is precisely the case where string `<` and `<=` agree. label() is a pure function of its two arguments (`id === "" ? "nobody" : a?.name || id`) and both calls here pass the same arguments the guard compared, so no re-evaluation can smuggle the equal case back into the branch that excludes it. There is therefore no input that reaches this operand with equal operands, and nothing downstream to distinguish: order, class, text and the id each button sends are identical. Same exhaustive check as the entry above — 150 permutations, zero differing renders.

client/src/view/spectator.ts 364:10 EqualityOperator a.actorId <= b.actorId
    The tie-break arm of renderPerch's comparator, reached only when two party members' LABELS are equal, where `<` and `<=` then differ only if their actorIds are equal too. No two entries of `Object.values(st.Actors)` can share an actorId: fold.ts keys that map BY actorId and throws `FoldError("duplicate actor \"...\"")` on a second one, so the input the mutation needs cannot be folded from any log — the same shape already adjudicated on client/src/player.ts 75:5 and client/src/view/grid.ts 139:32. It stays equivalent even for a state hand-built past the fold, which is the stronger claim and the one that matters: equal actorId AND equal label makes the two buttons identical in every rendered fact — same textContent (the shared label), same className (`a.actorId === perch.current` reads the shared id), same id handed to onPerch — so no ordering of that pair is observable, and the comparator's return value is read nowhere else (it sorts a local array from Object.values, never st.Actors itself). Checked rather than reasoned: all 150 permutations of three fixtures built around duplicate-id/duplicate-label pairs render a byte-identical button sequence under both operators.

# --- client/src/auth.ts -----------------------------------------------------

client/src/auth.ts 37:12 ConditionalExpression false
    `return t === null || t === "" ? null : t;` — the LEFT OPERAND only (cols 12-22 is `t === null`, not the whole condition; the whole-condition mutants at 12-34 are all killed by the existing tests). Forcing it false leaves `false || t === ""`, and every input lands on the same value: t===null returns t, which IS null, the same thing the then-branch yields; t==="" still matches the right operand and returns null; anything else returns t under both. get()'s only observable is its return value — no side effects, no store writes, nothing reaches the DOM — so there is no reading under which the two differ. Hand-applied the mutation and ran the full client suite to check the claim rather than only reasoning it: 333 pass, as predicted.

# --- client/src/view/grid.ts ------------------------------------------------
#
# Two mutually-redundant fallbacks and five sort-comparator mutants. The
# comparator group is the same shape already adjudicated on client/src/player.ts:
# a three-way comparator over values that are MAP KEYS, so the equal case it
# guards cannot occur.

client/src/view/grid.ts 129:45 StringLiteral ""
    `[...(name || tok.ActorID || "?")][0] ?? "?"` — the FIRST "?" only. It and the `??` fallback beside it are redundant with each other, so neither is observable alone. Blanking this one makes the spread source "" exactly when name and ActorID are both empty; the spread of "" is [], [0] is undefined, and the `?? "?"` then yields the same "?" the original produced directly. Every other input never reaches this operand at all. Identical string out, and initial is the only thing this expression feeds.

client/src/view/grid.ts 129:57 StringLiteral ""
    The `?? "?"` half of the same pair, and unreachable while the operand above is intact: `name || tok.ActorID || "?"` cannot evaluate to an empty string, because "?" is truthy and terminates the chain. So `[...s][0]` is always defined and the `??` branch never runs. It cannot be deleted despite that — `[...s][0]` is typed `string | undefined`, so the fallback is load-bearing for the TYPE even where it is unreachable at run time.

client/src/view/grid.ts 139:32 EqualityOperator a.tokenId <= b.tokenId
    `a.tokenId < b.tokenId ? -1 : ...` -> `<=`. The two differ only when the ids are EQUAL, and they cannot be: disc.tokenId comes from tok.ID for tokens read out of st.Tokens, which is keyed by that id, so two discs in one call always carry distinct ids.

client/src/view/grid.ts 139:61 ConditionalExpression true
    The second arm, `a.tokenId > b.tokenId ? 1 : 0`, forced to 1. It is only reached when the first arm was false, i.e. a >= b, and with equal ids impossible that means a > b — where the original also yields 1. No input reaches it with a different answer.

client/src/view/grid.ts 139:61 ConditionalExpression false
    The same arm forced to 0, so a > b returns 0 ("equal") instead of 1 ("after"). Not observable through Array.prototype.sort: both engines' sorts branch on `comparator(...) < 0`, never on the difference between 0 and a positive result, so "not less" is the only fact either value conveys. Checked rather than assumed — a 20-element reversed board, enough to leave insertion sort and enter the merge paths, still sorts correctly under this mutant. Note this holds because of how sort CONSULTS the comparator; a caller that used the comparator directly for a three-way answer would distinguish them, and none exists.

client/src/view/grid.ts 139:61 EqualityOperator a.tokenId >= b.tokenId
    Same arm, `>` -> `>=`: differs only on equal ids, which map keys make impossible.

client/src/view/grid.ts 139:61 EqualityOperator a.tokenId <= b.tokenId
    Same arm, `>` -> `<=`. Reached only when a >= b; with equality impossible, `a <= b` is false there and the arm yields 0 where the original yields 1 — the 0-versus-positive case shown above to be invisible to sort.

# --- client/src/wire.ts -----------------------------------------------------

client/src/wire.ts 460:13 EqualityOperator env.sequence >= this.lastSeq
    `if (env.sequence > this.lastSeq) this.lastSeq = env.sequence` -> `>=`. Equality makes the assignment write the value lastSeq already holds. Same shape as the two undo.ts entries above, and lastSeq's only consumer is reconnect's resume cursor, which sees an unchanged number either way.

client/src/wire.ts 484:7 ConditionalExpression default:
    Empties the `default:` arm of handleFrame's switch, whose body is a bare `return;`. The switch is the last statement in the function and nothing follows it, so falling out of an empty arm reaches the same end of the same void function. Deliberately NOT deleted despite that: the arm is where the ignore-unknown-frames decision is written down, and it is pinned by test ("an unknown frame kind is ignored and the stream keeps working") through the behaviour rather than through this mutant.

client/src/wire.ts 459:13 EqualityOperator env.sequence >= this.seenSeq
    `if (env.sequence > this.seenSeq) this.seenSeq = env.sequence;` -> `>=` in handleFrame's event arm. The two branches differ on exactly one input — an envelope whose sequence EQUALS the high-water mark, which the projection makes ordinary rather than exotic (one log event reaches a seat as several envelopes sharing one sequence). On that input the mutant performs the assignment the original skips, and the assignment writes the value the field already holds. Both are bigint, a primitive compared by value, so there is no identity to distinguish either: `Object.is(4n, 4n)` is true, and the field is a plain private with no getter, setter or Proxy on it. seenSeq is read in exactly two places — `reconnect()`'s `this.seenSeq > 0n ? this.seenSeq - 1n : 0n`, and this comparison itself — and neither can tell a re-written 4n from an untouched one. MEASURED 2026-08-24: mutant hand-applied, the WHOLE client suite run (639 tests, 26 files), 0 failures. Same shape as the two undo.ts entries already in this file.

# --- client/src/view/feed.ts ------------------------------------------------
#
# Three groups. (1) guards whose bypassed branch is neutralised downstream by
# an undefined comparison or an empty result; (2) a filter that cannot observe
# an empty entry because no code path creates one; (3) the same sort-comparator
# shape adjudicated on player.ts and grid.ts.

client/src/view/feed.ts 54:29 ConditionalExpression true
    `for (const e of live) if (isMechanical(e)) groupOf.set(...)` forced true, adding narration sequences to groupOf as self-mappings. Nothing ever reads them: the two lookups are `groupOf.get(e.sequence)` guarded by isMechanical, and `groupOf.get(target.sequence)` where target comes from a find already filtered to mechanical events. Extra unread map entries, no rendered difference.

client/src/view/feed.ts 59:34 ConditionalExpression false
    The `n.anchorToSeq <= 0n` half of the half-specified-anchor guard, forced false. It changes only the from>0/to<=0 case, and that case falls through to a range filter of `c.sequence >= from && c.sequence <= 0n`, which no live sequence satisfies. inRange is empty and the next guard skips the narration anyway — the same outcome the guard produced directly.

client/src/view/feed.ts 59:34 EqualityOperator n.anchorToSeq < 0n
    Same operand, `<=` -> `<`: differs only at anchorToSeq === 0, which is exactly the case shown above to reach an empty range and be skipped one line later.

client/src/view/feed.ts 63:9 ConditionalExpression false
    `if (inRange.length === 0) continue` forced never to skip. The body it then runs is `const head = inRange[0]!` followed by `for (const seq of inRange) ...` — over an empty array, so head is undefined and the loop body never executes. Nothing is written to groupOf.

client/src/view/feed.ts 91:46 ConditionalExpression true
    The `n.anchorToSeq > 0n` half of `anchored`, forced true. For a half-specified anchor this makes anchored true, and the find it enables searches `c.sequence >= from && c.sequence <= 0n`, which matches nothing; target is undefined and the narration lands at its own sequence — where the unanchored path would have put it.

client/src/view/feed.ts 91:46 EqualityOperator n.anchorToSeq >= 0n
    Same operand, `>` -> `>=`: differs only at anchorToSeq === 0, the case above.

client/src/view/feed.ts 104:10 MethodExpression [...bySeq.values()]
    Removes `.filter(e => e.events.length > 0 || e.narrations.length > 0)`. No entry can fail that predicate: entryAt is called in exactly two places and each pushes onto the entry it just obtained, so an entry exists only because something was about to be added to it. Kept rather than deleted because it states the invariant at the point a future caller would break it, but it has nothing to filter today.

client/src/view/feed.ts 105:20 ConditionalExpression true
    The same filter's predicate forced true. Every entry already satisfies it, per the entry above.

client/src/view/feed.ts 105:20 EqualityOperator e.events.length >= 0
    `> 0` -> `>= 0` in that predicate: true for every non-negative length, and the predicate is already true for every entry that exists.

client/src/view/feed.ts 105:43 EqualityOperator e.narrations.length >= 0
    The narrations half of the same predicate, same reasoning.

client/src/view/feed.ts 106:22 EqualityOperator a.seq <= b.seq
    `a.seq < b.seq ? -1 : ...` -> `<=`. Differs only on equal seqs, and seq is a Map key: bySeq is keyed by it, so one call never yields two entries sharing one.

client/src/view/feed.ts 106:43 ConditionalExpression true
    Second arm `a.seq > b.seq ? 1 : 0` forced to 1; reached only when a >= b, and equality being impossible leaves a > b, where the original also yields 1.

client/src/view/feed.ts 106:43 ConditionalExpression false
    Same arm forced to 0, so a > b reports "equal" rather than "after". Array.prototype.sort branches on `comparator(...) < 0` and never distinguishes 0 from a positive value, so the ordering is unchanged — the same reasoning, and the same 20-element check, recorded for view/grid.ts:139:61.

client/src/view/feed.ts 106:43 EqualityOperator a.seq >= b.seq
    Same arm, `>` -> `>=`: differs only on equal seqs, impossible for map keys.

client/src/view/feed.ts 106:43 EqualityOperator a.seq <= b.seq
    Same arm, `>` -> `<=`: false wherever this arm is reached, yielding 0 where the original yields 1 — the 0-versus-positive case sort cannot see.

# --- client/src/app.ts ------------------------------------------------------
#
# TWO entries, both history.replaceState's title argument, which the runtime this
# gate measures discards outright (happy-dom's signature is literally
# replaceState(state, _unused, url)). There are TWO such calls in this file —
# the invite path and the join path — and each carries its own entry rather
# than one stretched to cover both.
#
# This preamble said "ONE" and "only one of them surfaces a surviving mutant"
# until 2026-08-10, while the section held two and the second entry said so in
# words. The same miscount this file already apologises for higher up, in the
# opposite direction. A preamble that disagrees with its own contents is the
# failure this file exists to prevent, in miniature.
#
# This preamble said "two initial values" until 2026-08-03 and delivered one.
# The second was app.ts 52:16, withdrawn by the review that found it wrong —
# it claimed no test could observe the DOM between boot's two paints, and the
# fetch stub boot() calls between them can. The entry went; the count did not.
# Corrected here because a preamble promising an adjudication that does not
# exist is the same failure this file's header warns about, in miniature.
#
# The module-level DOM bootstrap that used to sit at the foot of app.ts is NOT
# here: it was untestable because of the HARNESS rather than because its
# mutants were unobservable, so claiming equivalence would have been a lie.
# It moved to client/src/main.ts, which is excluded from `mutate`.

# KILLED 2026-08-25, entries removed rather than re-keyed. app.ts 67:32 and
# 130:32 (the ignored TITLE argument of history.replaceState, on the invite and
# join paths) were adjudicated equivalent because the value reaches nothing a
# user or a test can read -- browsers ignore it, and happy-dom does not move
# document.title. That was true of the DOM and false of the SPY: app.test.ts
# already replaces history.replaceState to assert the token is stripped, so
# recording its middle argument kills both. A test beats an excuse, and an
# adjudication that a test can retire should be retired.
client/src/session.ts 68:12 EqualityOperator a.participantId <= b.participantId
    Tie-break on participant id, reached only when two participants share a display name. `<` -> `<=` differs ONLY when the two ids are equal, and they cannot be: participantId is the KEY of the `present` map (session.ts, `this.present.set(p.participantId, ...)`), so two distinct entries never carry the same one, and Array.prototype.sort never compares an element with itself. The premise is stronger than the equivalent adjudications on player.ts, which depend on uniqueness established in another file: here the key and the field are assigned together, four lines apart, in this file.
client/src/session.ts 69:12 EqualityOperator a.displayName <= b.displayName
    Display-name comparison, reached ONLY on the else branch of `a.displayName === b.displayName` — so by construction the two names differ, and `<` and `<=` agree on every input that reaches it. Not a claim about names being unique (they are not, which is why the tie-break above exists); a claim about which branch this expression lives on.

client/src/view/dm.ts 453:62 EqualityOperator x.actorId <= y.actorId
    Ordering the control panel by actorId. `<` -> `<=` differs ONLY when the two ids are equal, and they cannot be: actorId is the key of State.Actors (a Record), so two entries never carry the same one, and Array.prototype.sort never compares an element with itself. Same premise as the session.ts participant comparator, and for the same structural reason — the key and the field are the same value. Re-pointed 2026-08-23 (actor-kind Task 2) from 342:62 to 364:62, +22. dm.ts gained actorKindFromWireName and its doc comment above every one of these lines. Line 364 was byte-compared against HEAD's 342 and is identical, and col 62 was READ at the new location rather than offset: `x.actorId < y.actorId `. Re-pointed 2026-08-24 (actor-kind Task 4) from 364:62 to 373:62, +9. The Add actor form lost its "controller participant id (optional)" input and gained the comment saying why, all ABOVE this line and with no executable line of the control panel changed. Line 373 was byte-compared against HEAD's 364 and is identical, and col 62 was READ at the new location rather than offset: `x.actorId < y.a`. Re-pointed 2026-08-24 (actor-kind Task 7) from 373:62 to 425:62, +52. Three edits, all above this line and summing to the offset: kindSelect() with its doc comment (+33), the Add actor form's kind selector plus its guard (+14), and a review correction to actorKindFromWireName's doc (+5). The grant row's own selector shrank to a kindSelect call, but that is BELOW this line and so does not move it. Line 425 was byte-compared against HEAD's 373 and is identical, and col 62 was READ at the new location rather than offset: `x.actorId < y.a`.
client/src/view/dm.ts 497:36 StringLiteral "Stryker was here!"
    The `?? ""` fallback when the DM has no remembered choice for this actor. Assigning ANY string with no matching <option> is normalised by the DOM to "" with selectedIndex -1 (HTML spec: setting select.value selects the matching option, or none). So the mutant and the original leave the element in the identical observable state, and the test that asserts a fresh dropdown reads "" passes under both. Not a claim that the fallback is pointless — it is what restores a remembered choice; a claim that its SENTINEL value is unobservable. Re-pointed 2026-08-23 (actor-kind Task 2) from 386:36 to 408:36, +22. dm.ts gained actorKindFromWireName and its doc comment above every one of these lines. Line 408 was byte-compared against HEAD's 386 and is identical, and col 36 was READ at the new location rather than offset: `"";`. Re-pointed 2026-08-24 (actor-kind Task 4) from 408:36 to 417:36, +9. The Add actor form lost its "controller participant id (optional)" input and gained the comment saying why, all ABOVE this line and with no executable line of the control panel changed. Line 417 was byte-compared against HEAD's 408 and is identical, and col 36 was READ at the new location rather than offset: `"";`. Re-pointed 2026-08-24 (actor-kind Task 7) from 417:36 to 469:36, +52, by the same two edits as the comparator entry directly above — and note this line is INSIDE the grant row, below the call that replaced its inline selector, so the shrink there nets out against nothing and the offset is the comparator's exactly. Line 469 was byte-compared against HEAD's 417 and is identical, and col 36 was READ at the new location rather than offset: `"";`.

# --- client/src/view/canvas.ts -------------------------------------------

client/src/view/canvas.ts 65:24 ArithmeticOperator gx - gy
    drawMissingTile's checkerboard parity. `(gx + gy) % 2 === 0` and `(gx - gy) % 2 === 0` are the same predicate on every integer pair, not merely on the four this loop produces: (gx+gy) and (gx-gy) differ by 2*gy, so they always share a parity, and JS's sign-preserving `%` cannot separate them either, because the one case where it differs at all is an even negative, whose remainder is `-0`, and `-0 === 0` is true. The only observable this function has is the ordered sequence of fillStyle assignments and fillRect arguments reaching the context; a recording double (client/test/canvas.test.ts) captures both, and the two versions produce a byte-identical log — #ff00ff, #1a1a1a, #1a1a1a, #ff00ff over the same four rects. No input can widen the gx/gy range either: the bounds are the literal 2s on lines 46-47, drawMissingTile is module-private, and its one caller passes only sw and sh. Verified by hand-applying the mutant (suite stayed green, 11/11) and by exhaustively comparing both predicates over 10,201 integer pairs in [-50, 50]^2: zero divergences.

# --- client/src/view/scene-plan.ts ---------------------------------------

client/src/view/scene-plan.ts 103:38 ArrayDeclaration ["Stryker was here"]
    planObjects's `scene.Objects ?? []` fallback fires only when a Scene omits Objects entirely (state.ts marks it optional so bare four-field literals keep compiling), and the mutant's fallback element is the STRING "Stryker was here". Reading `.X`, `.Y`, `.Width` and `.Height` off a string yields undefined, so sx/sy/sw/sh are all NaN; every comparison against NaN is false, so intersectsViewport's first clause (`NaN + NaN > 0`) short-circuits the whole conjunction and the extra element is culled before anything is pushed. The observable is the returned DrawOp[] and it is unchanged in every respect the equivalents file asks about: same array object (planScene allocates `ops` and hands it to planObjects), same length, same element order, same key order per element, no throw. Verified by hand-applying the mutant (suite stayed green, 673/673) and by running both versions over 439,040 (scene, camera, cell, viewport) combinations — scales 0/negative/1e9/±Infinity/NaN, offsets ±1e9/±Infinity/NaN, cells 0/negative/fractional/Infinity/NaN, viewports 0/-1/Infinity/NaN, with and without terrain — for zero divergences. ONE distinguisher exists and is recorded rather than hidden: defining numeric X/Y/Width/Height on String.prototype makes the fallback element draw (measured, not supposed). That is not an input to planScene — it is a redefinition of the language underneath it — and a test doing it would pin the literal contents of a defaulting expression rather than any behaviour of the planner, while leaking a polluted builtin into every other test sharing its process.
