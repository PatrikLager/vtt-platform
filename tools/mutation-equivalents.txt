# Adjudicated EQUIVALENT mutants: surviving mutations that no test can kill,
# because the mutated code is observationally identical to the original.
#
# Format:  <package>  <file>:<line>:<col>  <MUTATOR>
# followed by an indented reason line. Both are required — a bare entry is a
# parse error.
#
# ------------------------------------------------------------------------
# READ THIS BEFORE ADDING AN ENTRY.
#
# An "equivalent" verdict asserts that NO test can ever kill this mutant, so
# it forecloses writing one. A wrong entry is therefore worse than an
# unadjudicated survivor: it converts a real gap into a permanently excused
# one, in writing, with a confident-sounding justification.
#
# That is not hypothetical. Four mutants were adjudicated equivalent here on
# 2026-07-27 and TWO were wrong (conformance.go:319 and :325). The reasoning
# was that `json:"...,omitempty"` makes a nil map and an empty map marshal
# identically — true, and irrelevant, because the observable is not JSON:
# checkCompiledBatchGolden compares DECODED STRUCTS with reflect.DeepEqual,
# where nil map != empty map. A two-line fixture killed both.
#
# The test to apply, learned from that mistake: do not ask "is the output the
# same?" Ask "is there ANY observable — under DeepEqual, under a pointer
# comparison, under nil-ness, under identity — that distinguishes them?"
# The two entries below survive that test because the mutated branch assigns
# a scalar EQUAL to the value the variable already holds. The conformance
# pair did not, because make(map...) assigns a NEW OBJECT whose nil-ness is
# observable.
#
# If you cannot state the reason in one sentence that names the observable,
# the mutant is not equivalent — it is untested. Write the test.
# ------------------------------------------------------------------------

# ------------------------------------------------------------------------
# THESE ENTRIES ARE KEYED BY LINE:COLUMN, so ANY edit above a mutant shifts
# it and the gate fails twice over: once for the "new" unadjudicated survivor
# and once for the now-stale old entry. That is the design working — a
# location-keyed pre-approval MUST NOT silently follow the code around, or it
# would start excusing whatever ends up on that line later. On such a failure,
# confirm the code at the NEW location is the same expression (read it; do not
# assume the shift is uniform), then re-point the entry.
# Seen 2026-07-29: a six-line //nolint:gocyclo comment above Apply moved both
# of its entries down by six.
# ------------------------------------------------------------------------

internal/engine  apply.go:357:15  CONDITIONALS_BOUNDARY
    `computed < 0` -> `<=`: when computed is 0 the branch assigns 0, the value it already holds. computed is a local int64 whose only uses are a value comparison, a %d format and an int32() conversion — no pointer, no map, no nil-ness, so nothing can observe the assignment. Re-pointed 2026-08-23 from 293:15 to 324:15, +31, by actor-kind Task 2: the grant arm gained a conditional kind write and a 18-line comment, and the revoke arm a 9-line one, all ABOVE this line with no executable line of the ResourceChanged arm touched. Line 324 was byte-compared against HEAD's 293 and is identical, and col 15 was READ at the new location rather than offset: `< 0 {`. All three apply.go entries move by the same +31 here — verified individually rather than assumed, since this file's own header says not to assume uniformity. Re-pointed 2026-08-24 from 324:15 to 332:15, +8, by actor-kind Task 4: the ActorAdded arm was rewritten to REFUSE an actor that names a controller — the seed, the empty-id strip and the mirrorControl call all went, and a long comment came in — which is a net +8 ABOVE this line with no executable line of the ResourceChanged arm touched. Line 332 was byte-compared against HEAD's 324 and is identical, and col 15 was READ at the new location rather than offset: `< 0 {`. All three apply.go entries move by the same +8 here — verified individually rather than assumed, since this file's own header says not to assume uniformity. Re-pointed 2026-08-31 from 332:15 to 329:15, -3, by 2026-08-30-retraction-leaves Task 7: engine.Apply's `case *vttv1.Envelope_EventsRetracted` arm and its trailing blank line went with the message the contract dropped, and the Apply doc comment lost EventsRetracted from its no-op list and was rewrapped at the same six lines — a net -3 ABOVE this line with no executable line of the arm this entry sits in touched. Line 329 was byte-compared against HEAD's 332 and is identical, and col 15 was READ at the new location rather than offset: `< 0 {`. All three apply.go entries move by the same -3 here — verified individually rather than assumed, since this file's own header says not to assume uniformity, and the internal/gateway entry in the same change moves by a DIFFERENT amount (-26), which is what that warning is about. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 329:15 to 357:15 — verified by reading the tree directly: the quoted expression above was located at the new line and the column re-derived from it, not carried over as an offset.

internal/engine  apply.go:360:30  CONDITIONALS_BOUNDARY
    `computed > int64(res.Max)` -> `>=`: when computed == Max the branch assigns Max, the value it already holds. Same reasoning as above. NOTE the sibling `res.Max > 0` on this line is NOT equivalent and IS killed — apply_test.go pins "max == 0 means unlimited". Re-pointed 2026-08-17 from 206:15/209:30: maps-as-geometry added the DoorOpened/DoorClosed arms ABOVE this one, moving both entries down 87 lines. Both new locations were read and confirmed to hold the same two expressions. Re-pointed 2026-08-23 from 296:30 to 327:30, +31, by actor-kind Task 2: the grant arm gained a conditional kind write and a 18-line comment, and the revoke arm a 9-line one, all ABOVE this line with no executable line of the ResourceChanged arm touched. Line 327 was byte-compared against HEAD's 296 and is identical, and col 30 was READ at the new location rather than offset: `> int64(res.Max`. All three apply.go entries move by the same +31 here — verified individually rather than assumed, since this file's own header says not to assume uniformity. Re-pointed 2026-08-24 from 327:30 to 335:30, +8, by actor-kind Task 4: the ActorAdded arm was rewritten to REFUSE an actor that names a controller — the seed, the empty-id strip and the mirrorControl call all went, and a long comment came in — which is a net +8 ABOVE this line with no executable line of the ResourceChanged arm touched. Line 335 was byte-compared against HEAD's 327 and is identical, and col 30 was READ at the new location rather than offset: `> int64(res.Max`. All three apply.go entries move by the same +8 here — verified individually rather than assumed, since this file's own header says not to assume uniformity. Re-pointed 2026-08-31 from 335:30 to 332:30, -3, by 2026-08-30-retraction-leaves Task 7: engine.Apply's `case *vttv1.Envelope_EventsRetracted` arm and its trailing blank line went with the message the contract dropped, and the Apply doc comment lost EventsRetracted from its no-op list and was rewrapped at the same six lines — a net -3 ABOVE this line with no executable line of the arm this entry sits in touched. Line 332 was byte-compared against HEAD's 335 and is identical, and col 30 was READ at the new location rather than offset: `> int64(res.Max`. All three apply.go entries move by the same -3 here — verified individually rather than assumed, since this file's own header says not to assume uniformity, and the internal/gateway entry in the same change moves by a DIFFERENT amount (-26), which is what that warning is about. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 332:30 to 360:30 — verified by reading the tree directly: the quoted expression above was located at the new line and the column re-derived from it, not carried over as an offset.

internal/engine  apply.go:504:41  CONDITIONALS_BOUNDARY
    `len(objs) > 0` -> `>= 0` in the SceneSeen arm, which makes the guard always true so that mergeObjects runs on an empty incoming slice. THE OBSERVABLE LOOKED FOR WAS WHETHER mergeObjects IS ACTUALLY INERT THERE, because a merge that copied, reallocated, reordered, or returned a non-nil empty where nil was would be observable through sc.Objects and this would be a missing test rather than an equivalent. It is inert, and that was measured rather than read: mergeObjects' only mutations (`have[i] = got` and `have = append(...)`) live inside `for _, o := range incoming`, which does not execute, so it returns its first argument. A probe compared the slice HEADER (data pointer, len, cap), nil-ness, and reflect.DeepEqual across all six combinations of have in {nil, empty non-nil, populated} and incoming in {nil, empty non-nil}: identical on every axis in every case. The map `byID` it builds is discarded unread. The write-back at :479 is unconditional and happens either way, so `sc.Objects = sc.Objects` is the whole effect. THE GUARD IS REDUNDANT, NOT UNTESTED, and those are different things: the CONDITIONALS_NEGATION mutant at this same site (`> 0` -> `<= 0`) dies immediately in TestSceneSeenObjectsMergeReplacingDuplicatesAndAppendingNew with "want 2 objects after merge, got 0". So the line is covered; only this one boundary is unobservable. THE STRONGEST ARGUMENT IS ONE THIS ENTRY ORIGINALLY MISSED: a fold-built Scene can never have nil Objects at all, because apply.go's sceneCreated arm builds them as `make([]SceneObject, 0, len(sc.Objects))`, so the nil-`have` cases are unreachable through the fold and the nil-promotion worry is moot before it starts. ON THE TS MIRROR, CORRECTED 2026-08-22: this entry used to claim fold.ts "has no such branch" and that the two arms were therefore the same thing said differently. They are not. fold.ts's sceneSeen arm does iterate `for (const o of v.objects)` with no length guard, but EARLIER IN THE SAME ARM it runs `if (!sc.Objects) sc.Objects = [];` unconditionally — an undefined-to-empty NORMALISATION that Go's arm deliberately does not perform for Objects, though it does perform the analogous one for Tiles and Explored. (No line distance is quoted here on purpose: the first attempt at this correction said "three lines above" when the real separation is 25 lines and six statements, which is the same class of defect this whole entry is being corrected for, and a distance in prose goes stale the moment anything is inserted between them.) That asymmetry is inert for the same reason as above — fold-built scenes are never nil there, and fold.ts's own comment says the guard exists only "to let bare test literals elsewhere keep compiling" because sceneCreated always sets Tiles, Objects and Explored on any Scene built through the fold — but the earlier symmetry claim was false, and it mattered because "normalising nil to empty" is the very operation the expiry clause below names. EXPIRES IF mergeObjects ever touches `have` outside the incoming loop — sorting it, deduplicating it, or normalising nil to empty would each make this killable, and the test to write then is a SceneSeen carrying zero objects against a scene that already has some. Re-pointed 2026-08-23 from 440:41 to 471:41, +31, by actor-kind Task 2: the grant arm gained a conditional kind write and a 18-line comment, and the revoke arm a 9-line one, all ABOVE this line with no executable line of the ResourceChanged arm touched. Line 471 was byte-compared against HEAD's 440 and is identical, and col 41 was READ at the new location rather than offset: `> 0 {`. All three apply.go entries move by the same +31 here — verified individually rather than assumed, since this file's own header says not to assume uniformity. Re-pointed 2026-08-24 from 471:41 to 479:41, +8, by actor-kind Task 4: the ActorAdded arm was rewritten to REFUSE an actor that names a controller — the seed, the empty-id strip and the mirrorControl call all went, and a long comment came in — which is a net +8 ABOVE this line with no executable line of the ResourceChanged arm touched. Line 479 was byte-compared against HEAD's 471 and is identical, and col 41 was READ at the new location rather than offset: `> 0 {`. All three apply.go entries move by the same +8 here — verified individually rather than assumed, since this file's own header says not to assume uniformity. Re-pointed 2026-08-31 from 479:41 to 476:41, -3, by 2026-08-30-retraction-leaves Task 7: engine.Apply's `case *vttv1.Envelope_EventsRetracted` arm and its trailing blank line went with the message the contract dropped, and the Apply doc comment lost EventsRetracted from its no-op list and was rewrapped at the same six lines — a net -3 ABOVE this line with no executable line of the arm this entry sits in touched. Line 476 was byte-compared against HEAD's 479 and is identical, and col 41 was READ at the new location rather than offset: `> 0 {`. All three apply.go entries move by the same -3 here — verified individually rather than assumed, since this file's own header says not to assume uniformity, and the internal/gateway entry in the same change moves by a DIFFERENT amount (-26), which is what that warning is about. PRE-EXISTING STALENESS REPAIRED 2026-08-31, fix round 1, and NOT caused by this task's -3: the body said "the write-back at :443", which was already wrong at HEAD (the write-back `st.Scenes[ss.GetSceneId()] = sc` sat at :482 there, and sits at :479 now). Repaired to :479 and rendered. Found only because the key re-point above put a reader in this entry with the tree open — which is the argument for re-rendering a whole entry rather than its key. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 476:41 to 504:41 — verified by reading the tree directly: the quoted expression above was located at the new line and the column re-derived from it, not carried over as an offset.

internal/gateway  authz.go:196:7  CONDITIONALS_BOUNDARY
    `if n < 0` -> `<=` inside abs(). At n == 0 the mutated branch returns -0, and for an int32 that IS 0 — the same value the unmutated path returns. No caller can distinguish them; there is no signed-zero to observe as there would be for a float. The SIBLING mutations in this function are NOT equivalent and ARE killed: `return -n` -> `return n` (190:10, both mutators) is killed by TestAuthorizePlayerMayNotWorkADoorItStandsWestOrNorthOf, which puts a door four squares east and four squares south of the token. Re-pointed 2026-08-20 from 163:7/164:10: the visibility arc's Task 2 added a set_viewpoint row and its comment to commandRoles, moving abs() down 11 lines. Both new locations were READ before re-keying (174 is `if n < 0`, 175 is `return -n`), per this file's own procedure — the shift is not assumed uniform. RE-POINTED AGAIN 2026-08-20 from 174:7/175:10, +15: Task 6 rewrote that same set_viewpoint comment and added MayPerch's call to Authorize, both ABOVE abs(), with no executable line of abs() touched. Read again at the new locations (189 is `if n < 0`, 190 is `return -n`); the killing test named above is unchanged and still kills. Re-pointed 2026-08-23 from 189:7, +1: the actor-kind branch corrected two comments in commandRoles above it — set_viewpoint's rule now says PARTY MEMBER rather than player-controlled — and one of them grew by a line. A COMMENT-ONLY move, which is the case this file's header warns about and the case no reader expects. Line 190 was byte-compared against HEAD's 189 (`if n < 0 {`, unique in the file) and col 7 was READ at the new location: `< 0 {`. Found by verifying ALL 39 keys against the tree rather than only the ones in the obviously edited file; code review had caught the project.go trio and missed this one. RE-POINTED 2026-08-31 from 190:7 to 189:7, -1, by the retraction-leaves branch's Task 6: the `"retract_events"` row left commandRoles and the `*vttv1.ClientCommand_RetractEvents` arm left commandName — both ABOVE abs(), with no executable line of abs() changed. Line 189 was diffed BYTE FOR BYTE against HEAD's 190 (`\tif n < 0 {`) and is identical, it is the only line in the file carrying that text, and column 7 was READ at the new location rather than offset (it is the `<` of `n < 0`). abs()'s enclosing function and its one caller mayWorkDoor are untouched. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 189:7 to 196:7 — verified by reading the tree directly: the quoted expression above was located at the new line and the column re-derived from it, not carried over as an offset.

internal/gateway  project.go:736:26  CONDITIONALS_BOUNDARY
    `y < sc.GridHeight` -> `<=` in objectInSight's footprint walk. THE OBSERVABLE IS A MAP LOOKUP THAT CANNOT HIT: the only thing the extra iteration does is read `squares[squareKey(x, y)]` at y == sc.GridHeight, one row past the bottom edge, and `squares` is only ever a set built by sight.VisibleFrom, whose walk is `for y := int32(0); y < sc.GridHeight` (sight.go:235-236). So the key is absent by construction, the read is false, and no object's verdict changes. The clamp is a PERFORMANCE guard against an enormous or int32-overflowing footprint, not a correctness one, and objectInSight's doc comment says so. SIBLINGS ARE NOT EQUIVALENT AND ARE KILLED: CONDITIONALS_NEGATION at this same site, ARITHMETIC_BASE at 736:66, and — importantly — the boundary mutant on the FOOTPRINT bound at 736:54, which is a real visibility leak (an object revealed by the lit square below it) and is killed by TestAnObjectIsRevealedOnlyByTheSquaresItStandsOn, written for it 2026-08-20. Re-pointed 2026-08-20 from 510:26/511:27 within the same day: fix round 1 added doorTransitions and rewrote the stamping comment ABOVE objectInSight, moving both entries down 34 lines with no executable line of objectInSight changed. Both new locations were read and confirmed to hold the same `<`. RE-POINTED AGAIN 2026-08-20 from 544:26/545:27, +42: Task 6 added reperch and its comment above transitions, again with no executable line of objectInSight changed. Read again at 586/587; TestAnObjectIsRevealedOnlyByTheSquaresItStandsOn, which kills the footprint siblings, is unchanged. RE-POINTED A THIRD TIME 2026-08-21 from 586:26/587:27, +25: task 6's fix round added perchSequence and rewrote reperch above objectInSight — again with no executable line of it changed. Every column was READ at the new location rather than offset (611 col 26 is `< sc.GridHeigh`, col 54 `< int64(o.Y)+i`, col 66 `+int64(o.Heigh`), and the body references above moved with the header this time, which is the half no gate watches. RE-POINTED A FOURTH TIME 2026-08-21 from 611:26/612:27, +12: task 6's fix round 2 qualified reperch's "every shoulder it has sat on" comment above objectInSight — a COMMENT-ONLY move, so nothing but the gate would have noticed, which is the case this file's header warns about. Lines 623 and 624 were compared BYTE FOR BYTE against the old 611 and 612 and are identical, and every column was then read at the new location rather than carried over (623 col 26 is `< sc.GridHeigh`, col 54 `< int64(o.Y)+i`, col 66 `+int64(o.Heigh`). The two sibling references above moved with the header. RE-POINTED A SIXTH TIME 2026-08-22 from 666:26/667:27 to 715:26/716:27, and the FIFTH re-point was WRONG — a defect this entry now records because the gate could not. Round 1 measured objectInSight at 666 partway through its own edits and then kept editing project.go above it (the forgetting loop's comment, the OpenDoors correction) without re-measuring, so the committed key was 30 lines short: at 15cd2c7 the loop is at 696/697, not 666/667. Caught in round 2 by byte-comparing rather than by trusting the last number. THE LESSON, since this is the seventh drift on this branch: measure AFTER the last edit to the file, not during. Round 2's own shift (+19 from SceneSeen's visible field and sceneSeenFor's comment) is folded in, 715 and 716 were diffed BYTE FOR BYTE against 15cd2c7's 696 and 697 and are identical, and every column was READ at the new location (715 col 26 is `< sc.GridHeigh`, col 54 `< int64(o.Y)+i`, col 66 `+int64(o.Heigh`; 716 col 27 `< sc.GridWidth`, col 54 `< int64(o.X)+i`). AND THE LESSON BIT AGAIN WHILE BEING WRITTEN: the first attempt at this re-key said 713/714, measured before round 2's review fixes shortened sceneSeenFor's comment by two lines. Re-measured as the last action before committing — and round 3 then moved it a FOURTH time (+5, protojson's map-ordering correction in this same function's comment), which is the point: this key moves whenever anyone edits the file above objectInSight, comment or code. Final 720:26/721:27, byte-compared against a304865's 715/716 and identical, every column read at the new location (720 col 26 `< sc.GridHeigh`, col 54 `< int64(o.Y)+i`, col 66 `+int64(o.Heigh`; 721 col 27 `< sc.GridWidth`, col 54 `< int64(o.X)+i`). PREVIOUSLY, fifth re-point 2026-08-21 from 623:26/624:27, +43: Task 7 widened transitions' SceneSeen loop to walk the UNION of pr.seen and now.squares (a scene that leaves sight entirely must be reported dark), guarded it against a scene retracted out of the world, and added the sortedSceneIDsUnion helper — all ABOVE objectInSight, with no executable line of it changed. Lines 666 and 667 were diffed BYTE FOR BYTE against HEAD's 623 and 624 and are identical, and every column was then READ at the new location rather than carried over (666 col 26 is `< sc.GridHeigh`, col 54 `< int64(o.Y)+i`, col 66 `+int64(o.Heigh`). The two sibling references above moved with the header. RE-POINTED AN EIGHTH TIME 2026-08-23 from 720:26 to 732:26, +12: the actor-kind branch replaced look()'s roster predicate with isPartyMember and rewrote its comment, did the same to eyes()'s spectator arm, and corrected the Viewer doc — all ABOVE objectInSight, with no executable line of it changed. Line 732 was diffed BYTE FOR BYTE against HEAD's 720 and is identical, and every column was READ at the new location rather than offset (732 col 26 is `< sc.GridHeigh`, col 54 `< int64(o.Y)+i`, col 66 `+int64(o.Heigh`). The two sibling references above moved with the header. AND THE STANDING LESSON BIT AGAIN, for the ninth time and in the same way: this re-point was first written as +10 (730/731/1162), measured partway through the branch and then invalidated by two further edits to project.go above these lines — the Viewer doc correction (+2) and classify's ActorAdded comment (+4, which moves the 1168 entry and not these two). Code review caught it by reading `git show :internal/gateway/project.go` at the claimed lines and finding `return false` and `}`, while the entry's own prose asserted a byte comparison that had never been run against that tree. MEASURE AFTER THE LAST EDIT means after the last one, including the ones review asks for. RE-POINTED A NINTH TIME 2026-08-24 from 732:26 to 765:26, +33, by actor-kind Task 4: the introduction loop in transitions now strips the controllers off the cloned Actor and emits a grant per controller behind it (an ActorAdded naming a controller no longer folds), which is +32 ABOVE objectInSight with no executable line of it changed. Line 765 was diffed BYTE FOR BYTE against HEAD's 732 and is identical, and every column was READ at the new location rather than offset (765 col 26 is `< sc.GridHeigh`). Measured as the LAST action before committing, which is this entry's own standing lesson. RE-POINTED A TENTH TIME 2026-08-31 from 765:26 to 735:26, -30, by the retraction-leaves branch's Task 6: redactedFor was deleted outright (its only subject was an EventsRetracted's free-text reason) and three comments above objectInSight were rewritten — perchSequence's, transitions', and the Projector doc's — all ABOVE objectInSight with no executable line of it changed. Line 735 was diffed BYTE FOR BYTE against HEAD's 765 and is identical, it is the only line in the file carrying that text, and column 26 was READ at the new location rather than offset (it is the `<` of `y < sc.GridHeight`). AND THE TWO LIVE SIBLING COORDINATES ABOVE — the ARITHMETIC_BASE and the FOOTPRINT bound — WERE RE-RENDERED WITH IT, which the tenth re-point's first attempt did NOT do: they still read 732:66/732:54, stale by 33 lines since before this branch, and the gate never says so because it validates KEYS and never the prose between them. Both were checked by printing the byte at the column, not by adding an offset. Measured as the LAST action before reporting, which is this entry's own standing lesson. RE-POINTED AN ELEVENTH TIME 2026-08-31 from 735:26 to 729:26, -6, by the same branch's Task 5: the scene-forgetting loop and its comment were DELETED from transitions (nothing removes a scene from engine.State once retraction is gone) and the shared-sequence paragraph above it was shortened — all ABOVE objectInSight with no executable line of it changed. Line 729 was diffed BYTE FOR BYTE against HEAD's 735 and is identical, it is the only line in the file carrying that text, and every column was READ at the new location rather than offset (729 col 26 is the `<` of `y < sc.GridHeight`, col 54 the `<` of `< int64(o.Y)+`, col 66 the `+` of `+int64(o.Height`). The two live sibling coordinates above were re-rendered with it. NOTE THE SHIFT IS NOT UNIFORM WITH THE sortedSceneIDsUnion ENTRY BELOW (-6 against its -4): classify's dead EventsRetracted arm sits between them and gained two lines, which is this file's own standing warning arriving again — each key was located by searching for its own text, not by adding this entry's offset. RE-POINTED A TWELFTH TIME 2026-08-31, in Task 5's own review round, from 729:26 to 736:26, +7: the reviewer asked the deleted loop's replacement paragraph to say WHY nothing stands in its place rather than leaving it to be inferred, and the answer (a loud guard would be another unkillable mutant in a gated package) is seven lines longer — A COMMENT-ONLY MOVE, which is the shape this entry has been caught by more than once. Line 736 was diffed BYTE FOR BYTE against HEAD's 735 and is identical, it is the only line in the file carrying that text, and every column was READ at the new location (736 col 26 the `<` of `y < sc.GridHeight`, col 54 the `<` of `< int64(o.Y)+`, col 66 the `+` of `+int64(o.Height`). The two live sibling coordinates above moved with it. THE FIX ROUND'S OWN INSTRUCTION SAID NO KEY WOULD MOVE, and it was wrong for the ordinary reason: a comment in a gated file moves every key beneath it, and the fingerprint changes with it, so internal/gateway's cached verdict is invalidated and re-measures on the next run whatever these numbers say.

internal/gateway  project.go:737:27  CONDITIONALS_BOUNDARY
    `x < sc.GridWidth` -> `<=`, the x twin of the entry above and equivalent for the same reason and by the same evidence: the admitted column is x == sc.GridWidth, and VisibleFrom's inner walk is `for x := int32(0); x < sc.GridWidth`, so `squares` can hold no such key. Its own footprint sibling at 736:54 is likewise a real leak rather than an equivalent, and is likewise killed by TestAnObjectIsRevealedOnlyByTheSquaresItStandsOn. Re-pointed 2026-08-20 from 545:27, +42, alongside its twin — and it was MISSED on the first pass of that re-key, which is how this entry came to be the fifth time this trap has bitten on this branch. The gate caught it: 587:27 reported unadjudicated while 545:27 reported stale, with the hint that a shift, not a new mutant, was the likely cause. Read at the new location before re-keying (587 is the `x` loop), per this file's own procedure. Re-pointed again 2026-08-21 to 612:27 alongside its twin, columns read rather than derived (612 col 27 is `< sc.GridWidth`, col 54 `< int64(o.X)+i`). Re-pointed a fourth time 2026-08-21 to 624:27, again alongside its twin and again for a comment-only shift, with 624 read at the new location (col 27 is `< sc.GridWidth`, col 54 `< int64(o.X)+i`) and the sibling reference above moved with the header. Re-pointed to 721:27 alongside its twin (sixth and seventh re-points, same day), after the fifth re-point was found to have landed 30 lines short (see the twin's entry); 721 read at the new location (col 27 is `< sc.GridWidth`, col 54 `< int64(o.X)+i`). PREVIOUSLY, fifth re-point 2026-08-21 to 667:27, again alongside its twin and for the same Task 7 shift, with 667 read at the new location (col 27 is `< sc.GridWidth`, col 54 `< int64(o.X)+i`) and the sibling reference above moved with the header. Re-pointed 2026-08-23 from 721:27 to 733:27 alongside its twin (eighth re-point), same cause and same evidence: line 733 byte-compared against HEAD's 721 and identical, columns read at the new location (733 col 27 `< sc.GridWidth`, col 54 `< int64(o.X)+i`), sibling reference above moved with the header. It carried the twin's +10 error and its correction too — see there for what went wrong and why the number is +12. RE-POINTED A NINTH TIME 2026-08-24 from 733:27 to 766:27, +33, by actor-kind Task 4: the introduction loop in transitions now strips the controllers off the cloned Actor and emits a grant per controller behind it (an ActorAdded naming a controller no longer folds), which is +32 ABOVE objectInSight with no executable line of it changed. Line 766 was diffed BYTE FOR BYTE against HEAD's 733 and is identical, and every column was READ at the new location rather than offset (766 col 27 is `< sc.GridWidth`). Measured as the LAST action before committing, which is this entry's own standing lesson. RE-POINTED 2026-08-31 from 766:27 to 736:27, -30, with its y twin directly above and for the same reason: Task 6 deleted redactedFor and rewrote three comments above objectInSight. Line 736 was diffed BYTE FOR BYTE against HEAD's 766 and is identical, it is the only line in the file carrying that text, and column 27 was READ at the new location (it is the `<` of `x < sc.GridWidth`). Its own live sibling reference moved too: the footprint bound then read 736:54, re-rendered from the source rather than carried over — it had said 733:54, which after the tenth re-point pointed at the Y bound instead of the X one. RE-POINTED AGAIN 2026-08-31, later the same day, from 736:27 to 730:27, -6, by Task 5's deletion of the scene-forgetting loop above objectInSight — with its y twin, and for the reason given there. Line 730 was diffed BYTE FOR BYTE against HEAD's 736 and is identical, and both columns were READ at the new location (730 col 27 is the `<` of `x < sc.GridWidth`, col 54 the `<` of `< int64(o.X)+`). SUPERSEDING THE SENTENCE BEFORE THIS ONE: the live footprint bound is 730:54, not 736:54. RE-POINTED AGAIN 2026-08-31 in the review round, from 730:27 to 737:27, +7, with its y twin and for the reason given there (a seven-line comment-only growth above objectInSight). Line 737 was diffed BYTE FOR BYTE against HEAD's 736 and is identical; both columns were READ at the new location (737 col 27 the `<` of `x < sc.GridWidth`, col 54 the `<` of `< int64(o.X)+`). SUPERSEDING AGAIN: the live footprint bound is 737:54.

internal/gateway  project.go:1159:38  ARITHMETIC_BASE
    Map CAPACITY HINT: `make(map[string]bool, len(a)+len(b))` in sortedSceneIDsUnion, col 38 being the `+`. Same shape and same reasoning as the internal/adventure and internal/rules capacity entries below (a fourth twin, internal/campaign campaign.go:448, was DELETED 2026-08-31 rather than re-keyed: it sat in Undo's would-be-retracted set, and retraction left the platform with the whole function): the hint affects only initial allocation, maps grow as needed, and no read distinguishes a different starting capacity — not len, not iteration, not lookup. THE NEGATIVE-HINT CAVEAT APPLIES AND IS REACHABLE HERE, not merely theoretical: `a` is pr.seen and `b` is now.squares, so the ordinary first look (pr.seen empty, one scene in sight) makes `len(a)-len(b)` equal -1. THE SPEC DOES NOT SAY A NEGATIVE MAP HINT PANICS, and this file claimed it did in four places until 2026-08-22. go1.26.4's own doc/go_spec.html, "Making slices, maps and channels", scopes it explicitly: "A constant size argument must be non-negative and representable", then "FOR SLICES AND CHANNELS, if n is negative or larger than m at run time, a run-time panic occurs." Maps are absent from the run-time clause, so gc is CONFORMING here rather than failing to implement something, and a runtime-negative map hint is simply unspecified. Verified on go1.26.4 with a RUNTIME value rather than a constant, because a constant IS rejected at compile time — that is the one non-negativity rule reaching maps — and so proves nothing: `n := -1000; make(map[string]bool, n)` is fine and the map works normally. The verdict still holds on gc and only on gc, since unspecified is equally unspecified for gccgo and TinyGo, but for THAT reason rather than the one this file used to give. THE SLICE ON THE NEXT LINE IS THE CONTRAST AND IS NOT ADJUDICATED BECAUSE IT IS KILLED: `out := make([]string, 0, len(a)+len(b))` at :1140 takes the same mutation to a negative CAP, where gc does panic — measured, `go test ./internal/gateway/` dies with "panic: runtime error: makeslice: cap out of range" in TestAPerchCarriesNoSequenceAtAll. Two adjacent lines, one equivalent and one killed, for no reason visible in either line: check which you are looking at before reusing this reason. Re-pointed 2026-08-23 from 1152:38 to 1168:38, +16, by the actor-kind branch's edits to look(), eyes(), the Viewer doc and classify's ActorAdded arm, all far above it. Line 1168 byte-compared against HEAD's 1152 and identical; col 38 read at the new location and is the `+` of `len(a)+len(b)`. The three body cross-references to this entry (internal/campaign, internal/adventure, internal/rules) were re-pointed with it — that is the half no gate watches. THIS ENTRY'S SHIFT IS NOT ITS TWINS' (+16 against their +12), because classify sits between them; a first attempt keyed all three at one offset and was wrong for both reasons at once — measured too early AND assumed uniform. See the 732:26 entry. Re-pointed 2026-08-24 from 1168:38 to 1201:38, +33, by actor-kind Task 4's introduction-loop change in transitions, far above it. Line 1201 byte-compared against HEAD's 1168 and identical; col 38 read at the new location and is the `+` of `len(a)+len(b)`. The contrast line named in this entry moved with it: the killed slice hint is now :1202, not :1153. THE THREE BODY CROSS-REFERENCES WERE NOT MOVED IN THAT 2026-08-24 RE-POINT — exactly the half-a-move this entry names as the half no gate watches, committed by this entry against itself one day after writing it down. They read :1168 until 2026-08-27. Corrected then, together with the internal/sight entry's reference to internal/gateway authz.go, stale at :189:7 since that key was re-pointed to :190:7 on 2026-08-23. RE-POINTED 2026-08-31 from 1201:38 to 1162:38, -39, by the retraction-leaves branch's Task 6, which deleted redactedFor and rewrote four comments — all ABOVE sortedSceneIDsUnion, with no executable line of it changed. Line 1162 was diffed BYTE FOR BYTE against HEAD's 1201 and is identical, it is the only line in the file carrying that text, and column 38 was READ at the new location rather than offset (it is the `+` of `len(a)+len(b)`). AND THE STANDING LESSON ON THIS FILE BIT TWICE MORE IN ONE TASK: this entry was written 1158, then 1157, then 1160, then 1162 — three prose passes over comments above it, each moving it after the previous measurement, the last being fix round 1's. Every one was caught by re-running the gate's own position check, not by anyone re-reading the number — which is the honest reason to run that check as the LAST action rather than as a step. RE-POINTED AGAIN 2026-08-31, later the same day, from 1162:38 to 1158:38, -4, by Task 5: transitions lost the scene-forgetting loop and its comment (-6) while classify's dead EventsRetracted arm gained two lines (+2), and both sit ABOVE sortedSceneIDsUnion with no executable line of it changed. Line 1158 was diffed BYTE FOR BYTE against HEAD's 1162 and is identical, it is the only line in the file carrying that text, and column 38 was READ at the new location (the `+` of `len(a)+len(b)`). THE CONTRAST COORDINATE IN THIS BODY WAS RE-RESOLVED AGAINST THE TREE RATHER THAN OFFSET, and it needed it twice over: the killed slice hint is :1159, and the live sentence naming it still said :1153 — stale since 2026-08-24, when only the historical sentence was updated. The internal/adventure and internal/rules cross-references now name this entry BY FUNCTION instead of by coordinate, so this is the last time they need moving. RE-POINTED ONCE MORE 2026-08-31 in the review round, from 1158:38 to 1165:38, +7, by the same comment-only growth in transitions; line 1165 diffed BYTE FOR BYTE against HEAD's 1162 and identical, column 38 READ at the new location (the `+` of `len(a)+len(b)`), and the contrast slice hint re-resolved against the tree to :1166. THE NAMED CROSS-REFERENCES DID NOT NEED TOUCHING, which is the first evidence that naming them by function was worth doing. Re-pointed 2026-08-31 from 1165:38 to 1139:38, -26, by 2026-08-30-retraction-leaves Task 7: Projector.classify's `case *vttv1.Envelope_EventsRetracted` arm — a 24-line withheld ruling plus its trailing blank — left with the Envelope payload arm it ruled on, all ABOVE this line and none of it inside sortedSceneIDsUnion. Line 1139 was byte-compared against HEAD's 1165 and is identical, and col 38 was READ at the new location rather than offset: `+len(b))`. The three internal/engine entries in the SAME change move by -3, not -26 — a reminder, not a coincidence, that a per-file delta is never a per-change one. CORRECTED same day, fix round 1: that re-point moved the KEY and left the body's contrast-slice pointer reading `:1166`, which the -26 had already carried to :1140 — current :1166 is a `return true` in a different function. THE THIRD TIME THIS ONE SENTENCE HAS GONE STALE (see the two earlier repairs above, one of them logged as "stale since 2026-08-24, when only the historical sentence was updated"), and the second time it went stale in a re-point whose own note claimed to have re-resolved it. Re-keying an entry is not done until every file:line INSIDE its body has been rendered against the tree, not just the key at the top. Repaired to :1140 and rendered: `out := make([]string, 0, len(a)+len(b))`. Re-pointed 2026-08-31 (retraction-leaves Task 8, fix round 1) from 1139:38 to 1159:38 — verified by reading the tree directly: the quoted expression above was located at the new line and the column re-derived from it, not carried over as an offset.

internal/mcp  read_tools.go:238:11  CONDITIONALS_BOUNDARY
    `limit > maxEventsSinceLimit` -> `>=`: at limit == max the branch assigns max, the value limit already holds. Clamping to a bound you are already at is a no-op. (Re-keyed 2026-08-27 from :236:11, which ddf2d96 shifted; the statement is byte-identical, so the adjudication is unchanged.)

internal/mcp  server.go:304:15  CONDITIONALS_BOUNDARY
    `backoff < redialMaxBackoff` -> `<=`: at backoff == max the body doubles then immediately re-clamps to max on the next line, so backoff is unchanged either way. COUPLED: this reason depends on the clamp at :306 being intact. Sound under single-mutant analysis (gremlins applies one at a time), but if :306 changes, re-derive this entry rather than trusting it. (Re-keyed 2026-08-27 from :307:15, which ddf2d96 shifted. The clamp moved with it, :309 -> :306, and BOTH were verified byte-identical against 21ef370 before either number was touched — so the coupling holds and this is a re-key, not a re-derivation. The body reference is moved with the header deliberately: leaving it behind is the exact defect this file records at the conformance.go entry below.)

internal/mcp  server.go:306:16  CONDITIONALS_BOUNDARY
    `backoff > redialMaxBackoff` -> `>=`: at backoff == max the branch assigns max, the value it already holds. (Re-keyed 2026-08-27 from :309:16, which ddf2d96 shifted; statement byte-identical.)

internal/rules/conformance  conformance.go:207:11  CONDITIONALS_BOUNDARY
    `v > math.MaxInt32` -> `>=`: at v == MaxInt32 the branch assigns MaxInt32, the value v already holds. v is a local int whose only subsequent use is `int32(v)`, so there is no pointer, no map, no nil-ness and no identity that could observe the redundant assignment. Same shape as internal/engine apply.go:360:30 above (re-pointed 2026-08-31, fix round 1 — see that entry). (That cross-reference read apply.go:150:30 until 2026-08-23 and was ALREADY WRONG at HEAD — HEAD's apply.go:150 is `case *vttv1.Envelope_ActorControlRevoked:`, which hosts no boundary mutant at all. 150:30 was the `computed > int64(res.Max)` entry's key before the 2026-08-17 re-point to 209:30, and the body references were never moved with the header. Found by resolving every key AND every cross-reference against the tree, which is the half no gate watches.) NOTE the sibling mutation on this line, `v <= math.MaxInt32`, is NOT equivalent and IS killed — it clamps every ordinary value to MaxInt32, which TestRunSmokeFailure catches via "have 1" (1 being what only default_max_expr can produce). RE-RESOLVED 2026-08-31, fix round 1: this cross-reference read apply.go:335:30 until the engine entry was re-keyed to 332:30 by 2026-08-30-retraction-leaves Task 7 (-3, engine.Apply's EventsRetracted arm). NOT FOUND BY REVIEW AND NOT IN ITS FINDINGS — found by sweeping every file:line inside every body after review raised the same defect against the internal/gateway project.go entry, which is what a caret is for. :335:30 now renders onto a COMMENT. Repaired to :332:30 and rendered: `if res.Max > 0 && computed > int64(res.Max) {`. That makes THREE separate entries whose bodies went stale while their keys stayed right, in one change: a re-point must re-render the whole file, not the entry it touched.

internal/rules/conformance  conformance.go:361:11  CONDITIONALS_NEGATION
    `scene == ""` -> `!=`, choosing between a golden's own scene name and the default "scene". EQUIVALENT ONLY BECAUSE OF THE GOLDEN FORMAT, and the reason matters more than the verdict here. An empty scene id is NOT generally interchangeable with a name: resolve.go:677 is `if sceneID != "" && tok.SceneID != sceneID`, so "" DISABLES the same-scene filter. Demonstrated at the Resolve boundary — caster in "" with target in "elsewhere" is ACCEPTED, while caster in "scene" with target in "elsewhere" is rejected with "has no token in the active scene". What makes the mutant unkillable is that goldenFile has ONE `scene` field (conformance.go:278) which runGolden stamps onto every token (:365, :373), so the caster/target scene MISMATCH that filter exists to catch cannot be expressed by any golden. An earlier version of this entry claimed the string was "used uniformly, so any two names are identical" — true of the golden path, false of the mechanism, and exactly the "true, and irrelevant" shape this file's header records for conformance.go:319/:325. THIS ENTRY EXPIRES IF goldenActor GAINS A PER-ACTOR SCENE. goldenActor already carries per-actor x/y, so that is a natural extension; the moment it lands, a golden can place caster and target in different scenes and this mutant becomes killable. Delete the entry then, do not re-point it.

internal/adventure  load.go:86:58  ARITHMETIC_BASE
    Map CAPACITY HINT: `make(map[string]bool, len(rs.Attributes)+len(rs.Defenses))`. Same as the internal/gateway sortedSceneIDsUnion entry above and equivalent for the same reason — the hint affects only initial allocation, maps grow as needed, and no read distinguishes a different starting capacity. The negative-hint caveat carries too: this can go negative with an ordinary ruleset (more defenses than attributes), and a runtime-negative MAP hint is UNSPECIFIED rather than a gc deviation — CORRECTED 2026-08-22, this used to assert the spec mandates a panic for maps, which it does not (the clause covers "slices and channels"; see the internal/gateway sortedSceneIDsUnion entry above for the quotation — cited BY NAME since 2026-08-31, because a coordinate in someone else's prose is the half no gate watches and this one had been stale since Task 6 re-keyed that entry). Verdict holds on gc and only on gc. Re-pointed 2026-08-17 from 84:58: maps-as-geometry added a line above it. NOTE THE CONTRAST that makes "capacity hint" insufficient as a reason on its own — the identical-looking mutation at internal/mapdef compile.go:72:38 is a SLICE capacity, where gc DOES panic ("makeslice: cap out of range"), so that one is NOT equivalent and is killed by TestCompileEmitsEveryPlacementOfAMapThatDeclaresMoreThanOne. Check which you are looking at before reusing this reason. (That cross-reference was re-pointed 2026-08-22 from 23:38 to 65:38, then to 68:38, then to 72:38 — THREE moves in two days, each caused by the round that had just re-pointed it adding more comment above it. Nothing about editing a doc comment suggests a mutation reference is downstream of it, which is the whole problem. It had drifted as internal/mapdef's compile.go grew and was ALREADY wrong before any of that: a body reference in someone else's entry, pointing at a file with NO entries of its own, which no gate reads and no key check covers. Column 38 is unchanged throughout and still the `+` of `1+len(m.Placements)`, the same operator position as this entry's own 85:58. Verified against the tree as the last action before committing, which is the only protocol that has held. Re-pointed 2026-08-24 from 85:58 to 86:58, +1, by actor-kind Task 7 adding the contract import this file now needs for ActorKind — one line, far above. Line 86 byte-compared against HEAD's 85 and identical; col 58 read at the new location and is the `+` of `len(rs.Attributes)+len(rs.Defenses)`. The internal/mapdef compile.go:72:38 cross-reference in this entry's body was re-resolved against the tree at the same time and has NOT moved.)

# --- internal/rules -----------------------------------------------------------
#
# Sixteen entries, every one verified by injecting the mutant and running the
# WHOLE suite. Four shapes only, and each is stated with what would END it --
# an equivalence is a claim about the code as it stands, not a permanent
# licence.

internal/rules  expr.go:655:10  CONDITIONALS_BOUNDARY
    `v > m` -> `>=` inside max(): at v == m the branch assigns m the value it already holds. v and m are plain ints returned as an int, with no pointer, map, nil-ness or identity to observe. Same shape as internal/engine apply.go:360:30 (re-pointed 2026-08-31, fix round 1 — see that entry). (Read apply.go:150:30 until 2026-08-23; it was already wrong at HEAD — see the internal/rules/conformance conformance.go:207:11 entry above for what that line actually holds and why the reference drifted.) NOTE the CONDITIONALS_NEGATION sibling on this line is NOT equivalent and IS killed (max(3, 7) pins it). EXPIRES IF vals stops being []int -- with floats, -0.0 makes which duplicate wins observable (max(-0.0, +0.0) returns +0.0 under `>` and -0.0 under `>=`, distinguishable by signbit). NaN does NOT distinguish them: every NaN comparison is false under both. RE-RESOLVED 2026-08-31, fix round 1: this cross-reference read apply.go:335:30 until the engine entry was re-keyed to 332:30 by 2026-08-30-retraction-leaves Task 7 (-3, engine.Apply's EventsRetracted arm). NOT FOUND BY REVIEW AND NOT IN ITS FINDINGS — found by sweeping every file:line inside every body after review raised the same defect against the internal/gateway project.go entry, which is what a caret is for. :335:30 now renders onto a COMMENT. Repaired to :332:30 and rendered: `if res.Max > 0 && computed > int64(res.Max) {`. That makes THREE separate entries whose bodies went stale while their keys stayed right, in one change: a re-point must re-render the whole file, not the entry it touched.

internal/rules  expr.go:663:10  CONDITIONALS_BOUNDARY
    `v < m` -> `<=` inside min(). Same reasoning and the same expiry as :655 above.

internal/rules  expr.go:683:20  CONDITIONALS_BOUNDARY
    floorDiv's `a%b != 0 && (a < 0) != (b < 0)`, the `a < 0` arm -> `a <= 0`. The two differ only at a == 0, and a == 0 makes a%b == 0, which short-circuits before this arm is ever evaluated. Unreachable, not merely untested.

internal/rules  expr.go:683:31  CONDITIONALS_BOUNDARY
    Same line, the `b < 0` arm -> `b <= 0`. Differs only at b == 0, and floorDiv's own `a / b` panics on a zero divisor before reaching this expression.

internal/rules  expr.go:923:13  CONDITIONALS_BOUNDARY
    parseExpr's `p.depth > maxExprDepth` -> `>=`. Only parseExpr and parseFactor increment depth, and the call graph forces strict alternation (Parse enters parseExpr at 0->1; parseTerm does not increment; parsePrimary and parseFuncCall re-enter parseExpr only from parseFactor), so parseExpr observes ODD depths exclusively. maxExprDepth is 200, EVEN, so the two comparisons can never differ. Verified by parity assertions over the suite and 4.4M fuzz executions. NOTE parseFactor's copy at :1002 is NOT equivalent and IS killed by nest(99)/nest(100). EXPIRES IF a third function increments depth, or maxExprDepth becomes odd, or the parseExpr->parseTerm->parseFactor->parsePrimary cycle stops having length 2. That last one is the likeliest and the easiest to miss: parseFactor does not currently call ITSELF, and unary minus -- a grammar gap compile.go:227 already names -- is naturally implemented as exactly that. It would break the parity without touching either of the other two conditions.

internal/rules  expr.go:1125:15  CONDITIONALS_BOUNDARY
    diceOpSuffix's `len(text) > 1 && text[0] == 'd'` -> `>= 1`. The two differ only at len(text) == 1, and the single one-character text that reaches here, "d", returned three lines above. Any other one-character text fails the `text[0] == 'd'` conjunct identically under both.

internal/rules  expr.go:1298:15  CONDITIONALS_BOUNDARY
    `arity.min > 0 && len(args) < arity.min` -> `>= 0`. parseFuncCall seeds args with `first` before this check, so len(args) >= 1 always and the guarded `len(args) < 0` is unreachable. NOTE both `len(args) < arity.min` siblings at :1298:32 are NOT equivalent and both ARE killed, but by DIFFERENT cases, and the distinction is the point: max/min have min 2, so the negation `>=` dies to max(1) (1 >= 2 is false, so the mutant accepts a call the original rejects) while the boundary `<=` survives max(1) -- 1 < 2 and 1 <= 2 both error -- and dies only to the two-argument cases, where 2 < 2 is false and 2 <= 2 is not.

internal/rules  resolve.go:311:27  CONDITIONALS_BOUNDARY
    `len(ability.Effects) > 0` -> `>= 0`. At length 0 the mutant calls applyOutcomes with an empty slice, which iterates nothing and returns (nil, nil, nil) -- identical to skipping it. COUPLED: this depends on applyOutcomes having NO observable statement before its loop. Today the only one is `reason := fmt.Sprintf(...)`, which is pure and discarded when the loop body never runs. A phase-start event, a counter, or any touch of resolveState added above that loop makes this mutant killable, and nothing in resolve.go:311 will say so.

internal/rules  resolve.go:384:8  CONDITIONALS_BOUNDARY
    applyDelta's `nv < 0 { nv = 0 }` -> `<=`. At nv == 0 the branch assigns 0, the value it already holds.

internal/rules  resolve.go:387:60  CONDITIONALS_BOUNDARY
    `nv > upper` -> `>=` in the same clamp. At nv == upper the branch assigns upper, the value it already holds. NOTE the `upper > 0` guard earlier on this line is NOT equivalent and IS killed -- max 0 means UNLIMITED, and `>= 0` makes it clamp to zero instead.

internal/rules  resolve.go:691:8  CONDITIONALS_BOUNDARY
    chebyshevDistance's `dx < 0 { dx = -dx }` -> `<=`. At dx == 0 the branch assigns -0, and -0 == 0 for ints. NOTE the CONDITIONALS_NEGATION sibling is NOT equivalent and IS killed.

internal/rules  resolve.go:695:8  CONDITIONALS_BOUNDARY
    The dy arm of the same absolute value. Same reasoning. NOTE its CONDITIONALS_NEGATION sibling is NOT equivalent and was killed only on 2026-08-05 -- seventeen of eighteen range tests placed both tokens on y=0, so a positive dy had effectively never been exercised.

internal/rules  resolve.go:698:8  CONDITIONALS_BOUNDARY
    `if dx > dy { return dx }; return dy` -> `>=`. At dx == dy the mutant returns dx where the original returns dy, and those are the same value.

internal/rules  compile.go:58:52  ARITHMETIC_BASE
    Map CAPACITY HINT: `make(map[string]bool, len(attrSet)+len(defSet))`. The hint affects only initial allocation -- maps grow as needed and no read distinguishes a different starting capacity. The negative-hint caveat from the internal/gateway sortedSceneIDsUnion entry carries (it was carried from the internal/campaign entries until those were deleted with Undo on 2026-08-31): a runtime-negative MAP hint is UNSPECIFIED rather than a gc deviation — CORRECTED 2026-08-22, this used to assert the spec mandates a panic for maps, which it does not (the clause covers "slices and channels"; see the internal/gateway sortedSceneIDsUnion entry for the quotation — cited by name rather than by coordinate since 2026-08-31) — so this verdict holds on gc and only on gc. CONTRAST internal/adventure compile.go:35, a SLICE hint, where a negative capacity DOES panic and four of five mutants there are killed.

internal/rules  compile.go:395:36  CONDITIONALS_BOUNDARY
    producePhaseForKey's `p > phase { phase = p }` -> `>=`, taking a maximum. At p == phase the branch assigns the value phase already holds.

internal/rules  compile.go:409:35  CONDITIONALS_BOUNDARY
    consumePhaseForAtom's `p < phase { phase = p }` -> `<=`, taking a minimum. Same reasoning as :395.

# ------------------------------------------------------------------------
# internal/sight (visibility Task 1). Gated from the package's first commit,
# which is why these five are the whole list: the other 36 survivors of the
# first run were killed by tests rather than excused here.
#
# All five were checked the way this file's header demands — "is there ANY
# observable that distinguishes them?" — by running each mutant against the
# original over inputs chosen to hit the equality case hard: exhaustively over
# all 43,046,721 coordinate quadruples in [-40,40]^4 for the two integer ones,
# and over 20 million random segment/rect triples drawn from the half-integer
# lattice this package actually uses (plus both zeros and +/-1e-12) for the
# three float ones. NOT a clean sweep taken on faith: the same harness was
# re-run with a mutant gremlins had KILLED (`t0 > t1` -> `>=` at :194) spliced
# in as a control, and it found a difference within 200,000 samples.
#
# RE-POINTED THREE TIMES, all on 2026-08-19: +94 when nine-point sampling and
# the tolerance parameter landed above them, +51 when the early exit did (and a
# sixth entry appeared), +8 when a comment above them was rewritten. Each time
# every new location was READ and confirmed to hold the same expression in
# unchanged surrounding code rather than derived from the offset — the header
# above says to do exactly that, and the +8 move is why: it was caused by
# editing a COMMENT, so nothing about the code changed and nothing but the
# gate would have noticed the keys going stale.
# ------------------------------------------------------------------------

internal/sight  sight.go:300:8  CONDITIONALS_BOUNDARY
    chebyshev's `if dx > dy { return dx }; return dy` -> `>=`: at dx == dy the mutant returns dx where the original returns dy, and those are the same int32. Identical in shape to internal/rules resolve.go:698:8 above, and to the maximum at compile.go:395:36.

internal/sight  sight.go:307:7  CONDITIONALS_BOUNDARY
    abs32's `if n < 0 { return -n }` -> `<=`: at n == 0 the mutant returns -0, and for an int32 that IS 0 — there is no signed zero to observe as there would be for a float. The same entry as internal/gateway authz.go:196:7 above (re-pointed 2026-08-31, fix round 1), in a second copy of abs(); its siblings here are NOT equivalent and ARE killed.

internal/sight  sight.go:338:8  CONDITIONALS_BOUNDARY
    segmentHitsRect's `if a > b { a, b = b, a }` -> `>=` swaps only when a == b, and two float64s that compare equal are bit-identical unless they are +0.0 and -0.0 — which these two cannot be, because both quotients share the divisor d[i] and a zero numerator is always +0.0 (x - x is +0.0 for every finite x, -0.0 included), so a pair of zeros here necessarily carries the SAME sign.

internal/sight  sight.go:341:8  CONDITIONALS_BOUNDARY
    `if a > t0 { t0 = a }` -> `>=` assigns t0 a value equal to the one it already holds; the one case where those are distinguishable is a == -0.0 against t0 == +0.0, and t0 is only ever compared numerically (`t0 > t1`, and the next axis's own `a > t0`) — never returned, formatted, or used as a key — so the sign of that zero has no observable.

internal/sight  sight.go:344:8  CONDITIONALS_BOUNDARY
    `if b < t1 { t1 = b }` -> `<=`, the same clamp on the other end of the parameter range: it assigns t1 the value t1 already holds, and t1's only uses are the numeric `t0 > t1` and the next axis's `b < t1`, so a +0.0/-0.0 substitution is unobservable for exactly the reason given at :341.

internal/sight  sight.go:228:10  CONDITIONALS_BOUNDARY
    `if want <= 0 { want = 1 }` -> `< 0`, which differs only at tolerance 0: the early-exit loop evaluates `reached >= want` ONLY after `reached++`, so reached is at least 1 wherever want is read, and every want <= 1 therefore selects exactly the squares with one reachable sample — 0 and 1 are the same predicate. Verified over 40,000 random scenes x 15 tolerances. COUPLED to that test's placement, deliberately: move `reached >= want` back outside the sample loop and tolerance 0 becomes "every square visible", which kills this mutant and correctly makes this entry go stale rather than silently pre-approving the change. PRE-EXISTING STALENESS REPAIRED 2026-08-31, fix round 1: this entry pointed at "internal/gateway authz.go:190:7 above" while that entry's live key is and was authz.go:189:7 (:190 is `return -n`, the sibling this text calls NOT equivalent). A cross-reference to another ENTRY is the one kind of pointer the key-position self-test cannot see, because it checks keys and not the prose between them. Repaired to :189:7. RE-POINTED AGAIN 2026-08-31, fix round 1: that entry's own key moved again, to authz.go:196:7 (retraction-leaves Task 8's commandRoles/commandName additions, both ABOVE abs()). Repaired to :196:7.
