Invalid v2 fixture (finding R3): a resolution declares a branch label
"effect", which collides with the fixed "ability:<id>:effect" reason string
Resolve stamps on unconditional effect-phase outcomes — so a hit-branch
outcome's testimony would be byte-identical to an unconditional effect's,
a permanent ambiguity in the append-only log. "effect" (and "usage") are
reserved at load like "always". Rejected in decodeResolutionContribution.
