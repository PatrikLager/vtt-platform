Invalid v2 fixture: an int-kind param is bound to a negative value —
must be rejected at the bind site, not left to fail as a cryptic parse
error inside the atom's expression (the grammar has no unary minus).
