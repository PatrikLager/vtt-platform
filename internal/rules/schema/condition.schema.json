{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/PatrikLager/vtt-platform/internal/rules/schema/condition.schema.json",
  "title": "Condition (conditions/*.json)",
  "description": "A named marker abilities can apply/remove. v1 conditions carry no mechanical effect of their own — DM-narrated bookkeeping only. See internal/rules/load.go for the authoritative loader; this document is the external contract for ruleset authors and is cross-checked against loader behavior by internal/rules/schema_test.go.",
  "type": "object",
  "required": ["id", "name"],
  "additionalProperties": false,
  "properties": {
    "id": { "type": "string", "minLength": 1 },
    "name": { "type": "string", "minLength": 1 },
    "description": { "type": "string" }
  }
}
