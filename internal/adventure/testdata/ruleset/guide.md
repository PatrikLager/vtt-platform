# Proving Grounds Mini

Fixture ruleset for internal/adventure tests — attributes `vim`/`vigor`,
defense `brace`, resource `focus`. No conditions, atoms, or abilities: only
`rules.Load`'s manifest/cross-reference shape is exercised here, since
`internal/adventure`'s Load/Compile never touch `rs.Compiled`.
