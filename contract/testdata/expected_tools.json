[
  {
    "description": "Move a token to a new grid position on its scene.",
    "inputSchema": {
      "properties": {
        "reason": {
          "type": "string"
        },
        "to": {
          "properties": {
            "x": {
              "type": "integer"
            },
            "y": {
              "type": "integer"
            }
          },
          "required": [
            "x",
            "y"
          ],
          "type": "object"
        },
        "tokenId": {
          "type": "string"
        }
      },
      "required": [
        "tokenId",
        "to"
      ],
      "type": "object"
    },
    "name": "move_token"
  },
  {
    "description": "Create a new scene with a grid.",
    "inputSchema": {
      "properties": {
        "gridHeight": {
          "type": "integer"
        },
        "gridWidth": {
          "type": "integer"
        },
        "name": {
          "type": "string"
        },
        "objects": {
          "description": "Optional; omit unless placing scenery inline. Scenery is normally authored as part of a map file rather than built field by field here.",
          "items": {
            "properties": {
              "art": {
                "type": "string"
              },
              "at": {
                "properties": {
                  "x": {
                    "type": "integer"
                  },
                  "y": {
                    "type": "integer"
                  }
                },
                "required": [
                  "x",
                  "y"
                ],
                "type": "object"
              },
              "blocksMove": {
                "type": "boolean"
              },
              "blocksSight": {
                "type": "boolean"
              },
              "height": {
                "type": "integer"
              },
              "kind": {
                "type": "string"
              },
              "objectId": {
                "type": "string"
              },
              "rotationDegrees": {
                "type": "integer"
              },
              "width": {
                "type": "integer"
              }
            },
            "required": [
              "objectId",
              "kind",
              "at",
              "width",
              "height",
              "rotationDegrees",
              "blocksSight",
              "blocksMove",
              "art"
            ],
            "type": "object"
          },
          "type": "array"
        },
        "sceneId": {
          "type": "string"
        },
        "tiles": {
          "additionalProperties": {
            "properties": {
              "art": {
                "type": "string"
              },
              "kind": {
                "type": "string"
              },
              "material": {
                "type": "string"
              }
            },
            "required": [
              "kind",
              "material",
              "art"
            ],
            "type": "object"
          },
          "description": "Optional; omit for a bare grid with no terrain. Keys are \"x,y\" (column,row; comma because a dot reads as a decimal) and must cover every square. Scenes with terrain are normally authored as a map file rather than built field by field here.",
          "type": "object"
        }
      },
      "required": [
        "sceneId",
        "name",
        "gridWidth",
        "gridHeight"
      ],
      "type": "object"
    },
    "name": "create_scene"
  },
  {
    "description": "Add an actor to the campaign.",
    "inputSchema": {
      "properties": {
        "actor": {
          "properties": {
            "actorId": {
              "type": "string"
            },
            "attributes": {
              "additionalProperties": {
                "type": "integer"
              },
              "description": "Optional; omit unless a rule module instructs otherwise — moduleData is opaque.",
              "type": "object"
            },
            "controllerId": {
              "description": "Omit or empty = DM/agent-controlled; set a participant id to hand control to a player. Use EITHER this or controllerIds, never both — if they disagree, controllerIds wins and this is overwritten with its first entry.",
              "type": "string"
            },
            "controllerIds": {
              "description": "Optional; omit. Authoritative if set — controllerId is derived from it, so do not set both. The full set of participants who may act as this actor — control is normally granted AFTER creation with grant_actor_control, not seeded here. Setting it seeds the set; leaving it empty means DM/agent only, exactly as an empty controllerId does.",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "moduleData": {
              "description": "Optional; omit unless a rule module instructs otherwise — moduleData is opaque.",
              "type": "object"
            },
            "moduleId": {
              "description": "Optional; omit unless a rule module instructs otherwise — moduleData is opaque.",
              "type": "string"
            },
            "name": {
              "description": "Optional display label for the actor.",
              "type": "string"
            },
            "resources": {
              "additionalProperties": {
                "properties": {
                  "current": {
                    "type": "integer"
                  },
                  "max": {
                    "type": "integer"
                  }
                },
                "required": [
                  "current",
                  "max"
                ],
                "type": "object"
              },
              "description": "Optional; omit unless a rule module instructs otherwise — moduleData is opaque.",
              "type": "object"
            }
          },
          "required": [
            "actorId"
          ],
          "type": "object"
        }
      },
      "required": [
        "actor"
      ],
      "type": "object"
    },
    "name": "add_actor"
  },
  {
    "description": "Place an actor's token on a scene's grid.",
    "inputSchema": {
      "properties": {
        "actorId": {
          "type": "string"
        },
        "position": {
          "properties": {
            "x": {
              "type": "integer"
            },
            "y": {
              "type": "integer"
            }
          },
          "required": [
            "x",
            "y"
          ],
          "type": "object"
        },
        "sceneId": {
          "type": "string"
        },
        "tokenId": {
          "type": "string"
        }
      },
      "required": [
        "tokenId",
        "sceneId",
        "actorId",
        "position"
      ],
      "type": "object"
    },
    "name": "place_token"
  },
  {
    "description": "Start a new play session.",
    "inputSchema": {
      "properties": {
        "name": {
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "name": "start_session"
  },
  {
    "description": "End the current play session.",
    "inputSchema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "end_session"
  },
  {
    "description": "Retract a range of events from the record with a stated reason.",
    "inputSchema": {
      "properties": {
        "fromSequence": {
          "type": "integer"
        },
        "reason": {
          "type": "string"
        },
        "toSequence": {
          "type": "integer"
        }
      },
      "required": [
        "fromSequence",
        "toSequence",
        "reason"
      ],
      "type": "object"
    },
    "name": "retract_events"
  },
  {
    "description": "Use one of the loaded ruleset's abilities as an actor against explicit targets.",
    "inputSchema": {
      "properties": {
        "abilityId": {
          "type": "string"
        },
        "actorId": {
          "type": "string"
        },
        "targetIds": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "actorId",
        "abilityId",
        "targetIds"
      ],
      "type": "object"
    },
    "name": "use_ability"
  },
  {
    "description": "Remove a named condition from an actor (DM-ended durations).",
    "inputSchema": {
      "properties": {
        "actorId": {
          "type": "string"
        },
        "conditionId": {
          "type": "string"
        }
      },
      "required": [
        "actorId",
        "conditionId"
      ],
      "type": "object"
    },
    "name": "remove_condition"
  },
  {
    "description": "Add a story entry to the table's shared narrative — narration, in-character speech (set `as`), or table talk; optionally anchored to the event sequences it narrates.",
    "inputSchema": {
      "properties": {
        "anchorFromSeq": {
          "description": "Optional; omit (0) if this narration is unanchored. Set together with anchorToSeq as a backward-pointing range (both \u003e 0, anchorFromSeq \u003c= anchorToSeq, anchorToSeq before this narration's own sequence) — never set one without the other.",
          "type": "integer"
        },
        "anchorToSeq": {
          "description": "Optional; omit (0) if this narration is unanchored. Set together with anchorFromSeq as a backward-pointing range (both \u003e 0, anchorFromSeq \u003c= anchorToSeq, anchorToSeq before this narration's own sequence) — never set one without the other.",
          "type": "integer"
        },
        "as": {
          "description": "Optional speaker label — set to voice an NPC or PC in character; omit to speak as yourself (table talk).",
          "type": "string"
        },
        "text": {
          "type": "string"
        }
      },
      "required": [
        "text"
      ],
      "type": "object"
    },
    "name": "add_narration"
  },
  {
    "description": "Create or replace a keyed world note (locations, NPCs, quest state) — the campaign's durable memory.",
    "inputSchema": {
      "properties": {
        "key": {
          "type": "string"
        },
        "text": {
          "type": "string"
        },
        "title": {
          "description": "Optional; may be empty.",
          "type": "string"
        }
      },
      "required": [
        "key",
        "text"
      ],
      "type": "object"
    },
    "name": "upsert_note"
  },
  {
    "description": "Delete a world note by key.",
    "inputSchema": {
      "properties": {
        "key": {
          "type": "string"
        }
      },
      "required": [
        "key"
      ],
      "type": "object"
    },
    "name": "delete_note"
  },
  {
    "description": "Load a prepared adventure into the campaign — compiles its scenes, statblocks, notes, and opening narration into one atomic batch of setup events. DM/agent only.",
    "inputSchema": {
      "properties": {
        "adventureId": {
          "type": "string"
        }
      },
      "required": [
        "adventureId"
      ],
      "type": "object"
    },
    "name": "load_adventure"
  },
  {
    "description": "Give a participant control of an actor, so they may move its token and use its abilities. Control is a SET — granting does not take control away from anyone who already has it, and an actor may be controlled by several participants at once. DM/agent only. Note the DM never needs this to act: DM authority is independent of control, so a DM already moves and uses any actor while a player controls it.",
    "inputSchema": {
      "properties": {
        "actorId": {
          "type": "string"
        },
        "participantId": {
          "type": "string"
        }
      },
      "required": [
        "actorId",
        "participantId"
      ],
      "type": "object"
    },
    "name": "grant_actor_control"
  },
  {
    "description": "Take a participant out of an actor's control set. Used to REASSIGN a character — when a player leaves the table for good, or a character changes hands — never to let the DM act, which needs no revocation. DM/agent may revoke anyone; a player may revoke only themselves.",
    "inputSchema": {
      "properties": {
        "actorId": {
          "type": "string"
        },
        "participantId": {
          "type": "string"
        }
      },
      "required": [
        "actorId",
        "participantId"
      ],
      "type": "object"
    },
    "name": "revoke_actor_control"
  },
  {
    "description": "Change what a participant is ALLOWED to do. Someone who joined through the shared link arrives as a spectator and can only watch; promote them to \"player\" so they can be given a character and act. role accepts ONLY \"player\" or \"spectator\" — promotion can never reach dm or agent, because the join link would otherwise be a route to full authority in two steps. Minting a DM is a deliberate out-of-band act (`vtt invite`). This changes IDENTITY, not the campaign, so it writes no event.",
    "inputSchema": {
      "properties": {
        "participantId": {
          "type": "string"
        },
        "role": {
          "type": "string"
        }
      },
      "required": [
        "participantId",
        "role"
      ],
      "type": "object"
    },
    "name": "promote_participant"
  },
  {
    "description": "Open or close the table's shared join link. While it is OPEN, the next few people holding the link can join as spectators without an invite; while it is CLOSED the link is inert and admits nobody, which is how a campaign starts. door must be \"JOIN_DOOR_OPEN\" or \"JOIN_DOOR_CLOSED\" — leaving it unset is refused rather than guessed at, because guessing wrong either admits strangers or locks the table out mid-session. DM/agent only. Changes operational state, not the campaign, so it writes no event.",
    "inputSchema": {
      "properties": {
        "admitLimit": {
          "description": "Optional; omit for the default of 8. How many people THIS opening may admit before the door stops letting anyone new in — a bound on what a leaked link can cost, since closing the door later does NOT revoke anyone already through it. Opening the door again starts a fresh budget.",
          "type": "integer"
        },
        "door": {
          "enum": [
            "JOIN_DOOR_OPEN",
            "JOIN_DOOR_CLOSED"
          ],
          "type": "string"
        }
      },
      "required": [
        "door"
      ],
      "type": "object"
    },
    "name": "set_join_door"
  },
  {
    "description": "Replace the shared join link's secret. Use this when a link has LEAKED: the old link stops admitting anyone, while everybody already at the table keeps their own credential and their characters. Independent of the door — rotating does not open or close it. DM/agent only. Writes no event.",
    "inputSchema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "rotate_join_link"
  },
  {
    "description": "Open the door at a scene square. Refused unless the square's tile is a door (maps-as-geometry spec §3.3, §6).",
    "inputSchema": {
      "properties": {
        "at": {
          "properties": {
            "x": {
              "type": "integer"
            },
            "y": {
              "type": "integer"
            }
          },
          "required": [
            "x",
            "y"
          ],
          "type": "object"
        },
        "sceneId": {
          "type": "string"
        }
      },
      "required": [
        "sceneId",
        "at"
      ],
      "type": "object"
    },
    "name": "open_door"
  },
  {
    "description": "Close the door at a scene square. Refused unless the square's tile is a door (maps-as-geometry spec §3.3, §6).",
    "inputSchema": {
      "properties": {
        "at": {
          "properties": {
            "x": {
              "type": "integer"
            },
            "y": {
              "type": "integer"
            }
          },
          "required": [
            "x",
            "y"
          ],
          "type": "object"
        },
        "sceneId": {
          "type": "string"
        }
      },
      "required": [
        "sceneId",
        "at"
      ],
      "type": "object"
    },
    "name": "close_door"
  },
  {
    "description": "Load a standalone map into the campaign — compiles its terrain and scenery into one SceneCreated plus one TokenPlaced per placement the map declares, atomically. DM/agent only. Unlike load_adventure, a map brings no actors, notes or opening narration — just terrain and placements; a placement's actor must already exist (add_actor it first) or the whole load is rejected.",
    "inputSchema": {
      "properties": {
        "mapId": {
          "type": "string"
        }
      },
      "required": [
        "mapId"
      ],
      "type": "object"
    },
    "name": "load_map"
  }
]
